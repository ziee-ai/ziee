# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
playwright-report/
test-results/
.test-workers.json
.test-configs/
dist-e2e/

# Visual testing (Layer B baselines are machine/font-specific — bless per
# environment with `--update-snapshots`, never commit a local machine's PNGs;
# Layer C cache/report are generated artifacts).
tests/e2e/visual/**/*-snapshots/
.visual-judge-cache.json
visual-judge-report.json
