// Shim → @ziee/shell. The accent presets (design-system tokens) live in the
// shell package now.
export {
  ACCENT_PRESETS,
  DEFAULT_ACCENT,
  ACCENT_ORDER,
  applyAccent,
} from '@ziee/shell/theme/accentPresets'
export type {
  AccentVariant,
  AccentPresetDef,
  AccentPreset,
} from '@ziee/shell/theme/accentPresets'
