// Shim → @ziee/shell. Reads the app-registered `Stores.AppLayout.nativeScroll`
// through a typed view.
export { DivScrollY } from '@ziee/shell/components/DivScrollY'
export type { DivScrollYProps } from '@ziee/shell/components/DivScrollY'
