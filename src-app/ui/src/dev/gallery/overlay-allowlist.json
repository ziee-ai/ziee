{
  "$comment": "Overlay-render gate allow-list. Every overlay HOST (controlled Dialog/Drawer/Sheet) and self-opening TRIGGER (Confirm/Popover/menu) under src/modules must be EITHER rendered open in overlays.tsx OR listed here with a reason. A new overlay that is neither wired nor listed fails `npm run check` (node scripts/gen-overlay-registry.mjs --check). Prefer wiring an open-state entry; use this list only for overlays that are structurally hard to render standalone (deeply nested in a page/section, responsive-only, or an inline confirm reached via a row/card action). See OVERLAY_FINDINGS.md.",
  "hosts": {
    "modules/chat/core/components/ChatRightPanel": "File-preview Drawer that only appears below the md page-width breakpoint when a file/tab is opened into the right panel (store-driven mobileDrawerOpen). Responsive- + interaction-gated; the wide-viewport side-panel form is exercised on the chat surface. Tracked for a narrow-viewport open recipe.",
    "modules/chat/components/PaneManagerDrawer": "Small-screen pane-manager Drawer (FB-26) \u2014 opens ONLY below the md breakpoint via the pane-header 'Panes' button (store-driven Stores.SplitView.paneManagerOpen); at the gallery's desktop viewport that button is the one-click split affordance instead, so the drawer is not openable in the standard canvas. Needs multi-pane runtime state (SplitView panes); verified via the 14-split-chat/mobile-panes e2e (open / focus-pane / close-pane / open-another). Tracked for a narrow-viewport open recipe.",
    "modules/memory/components/sections/MyMemoriesSection": "Memory-detail Drawer + delete Confirm open on a memory-row action within the section. Section rendered on the memory page; the panel has seeded loading/empty/error coverage. Tracked for a row-open recipe.",
    "modules/projects/components/ProjectKnowledgeSection": "Knowledge-file Drawer opens on a knowledge-row action inside the project-detail section. Project detail is rendered in the gallery (seeded deep-state); interaction-gated. Tracked for a recipe.",
    "modules/user/components/group/UserGroupsSettings": "Renders the (separately wired) EditUserGroupDrawer plus an inline group-row Drawer; the standalone drawer is covered by overlay-edit-user-group-drawer. Page rendered in the gallery. Tracked.",
    "modules/chat/extensions/schedule/components/ScheduleLoopDialog": "Composer 'Schedule or loop this chat' Drawer (Group E, ITEM-18/20) \u2014 local open-state, opens on a click of the toolbar button inside the live chat composer and only when the chat has a saved conversation to bind to. Same composer-overlay pattern as MicButton/ChatInput; reuses the gallery-wired ScheduledTaskFormDrawer's ScheduleBuilder/ModelField primitives. Tracked for a composer interaction recipe."
  },
  "triggers": {
    "modules/user/components/user/UserGroupsDrawer": "User-groups management Drawer opened from the user detail/edit surface (not a standalone story). Self-opening from the user admin flow; exercised via the users admin e2e (07-users). Tracked for a dedicated open recipe."
  }
}
