// Shim → @ziee/shell. The dependency-pure drag/keyboard resize handle moved to
// `@ziee/shell/components/ResizeHandle` (reusable by any SDK-consuming app). This
// `@/`-path shim is kept so existing importers stay unchanged.
export { ResizeHandle } from '@ziee/shell/components/ResizeHandle'
