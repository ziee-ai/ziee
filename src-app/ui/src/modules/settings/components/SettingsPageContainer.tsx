// Shim → @ziee/shell. The generic settings card/title container (reads the
// app-registered `AppLayout.nativeScroll` through a typed seam view).
export { SettingsPageContainer } from '@ziee/shell/settings/SettingsPageContainer'
