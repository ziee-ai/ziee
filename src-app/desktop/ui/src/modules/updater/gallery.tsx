/**
 * Desktop dev-gallery seed for the `updater` module — renders from the shared crawl
 * base; ownership marker so the completeness gate accounts for it.
 */
import type { ModuleGallery } from '@/dev/gallery/support'

export const gallery: ModuleGallery = { crawlOnly: true }
