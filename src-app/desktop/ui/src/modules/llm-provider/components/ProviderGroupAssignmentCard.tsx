/**
 * Desktop Override: ProviderGroupAssignmentCard
 *
 * Returns null because all providers are automatically assigned to all groups
 * in the desktop app via the AutoAssignProviderHandler event handler.
 */
export function ProviderGroupAssignmentCard() {
  return null
}
