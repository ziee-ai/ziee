# Production runtime configuration template for ziee web backend.
# Copy to `prod.yaml` and override sensitive values per deployment.

# Application settings
app:
  data_dir: "/var/lib/ziee/data"

# PostgreSQL — external recommended for prod.
postgresql:
  use_embedded: false
  external:
    host: "postgres.internal"
    port: 5432
    username: "ziee"
    password: "REPLACE_ME"
    database: "ziee"
  pool:
    max_connections: 50
    min_connections: 5
    acquire_timeout_secs: 10
    idle_timeout_secs: 60
    max_lifetime_secs: 1800

# HTTP server
server:
  host: "0.0.0.0"
  port: 9000
  api_prefix: "/api"
  # Per-peer-IP rate limit (tower-governor token bucket). Optional —
  # default fallback is `per_second: 50, burst_size: 500`. Tighten
  # for production once you've measured normal SPA cold-load demand
  # (~15-25 parallel calls). Behind a reverse proxy that NATs all
  # users to one IP, raise these or switch to a different key
  # extractor (PeerIpKeyExtractor buckets per source IP).
  rate_limit:
    per_second: 50
    burst_size: 500
  cors:
    allow_origins: ["https://chat.example.com"]
    allow_methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
    allow_headers: ["Content-Type", "Authorization"]

# Logging
logging:
  level: "info"
  format: "json"

# JWT — MUST override `secret` per deployment.
jwt:
  secret: "REPLACE_ME_WITH_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS"
  issuer: "ziee"
  audience: "ziee-api"
  access_token_expiry_hours: 24
  refresh_token_expiry_days: 30

# Code sandbox (bwrap-isolated execution; built-in MCP server).
# Production deployment requirements:
#   - bubblewrap installed on the host
#   - rootfs squashfs mounted via squashfuse at `rootfs_path`
#   - delegated cgroup parent at `cgroup_parent` (owned by server uid)
# Without those, leave `enabled: false`.
code_sandbox:
  enabled: false
  rootfs_path: "/var/lib/ziee/sandbox-rootfs/current"
  cgroup_parent: "/sys/fs/cgroup/ziee-sandbox.slice"
