// Project module core — shared infrastructure used by the project module
// itself and by sibling modules that contribute via the project-extension
// system.
//
// Today this just hosts the extension framework. As the project module
// grows shared types/utils, they can join here.

pub mod extension;
