[package]
name = "sandbox-vm-protocol"
version = "0.1.0"
edition = "2021"
description = "Host<->guest vsock control protocol for the macOS/WSL2 code-sandbox microVM backend"
publish = false

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
