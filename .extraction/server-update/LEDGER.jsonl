{"id":"server-update-01","angle":"equivalence","finding":"types.rs moved BYTE-FOR-BYTE — `diff <(git show 4a2391732:server/src/modules/server_update/types.rs) sdk/crates/ziee-server-update/src/types.rs` empty (exit 0). UpdateStatusResponse's 8 fields + derives + doc comments unchanged. permissions.rs differs by ONLY the two PermissionCheck/PermissionList import lines.","status":"verified"}
{"id":"server-update-02","angle":"equivalence","finding":"checker.rs correctly RETAINED in ziee: moving it would resolve env!(CARGO_PKG_VERSION) (cache seed current_version + is_newer comparison) to the crate's 0.0.0 instead of ziee's version — an observable /api/server-update/status regression. Its config-default test names crate::core::config::UpdateCheckConfig. No behavioral change to the checker (it's untouched except the super::types path via the shim).","status":"verified"}
{"id":"server-update-03","angle":"canonical-equality","finding":"schemars keys UpdateStatusResponse by short ident (unchanged by the crate move); the JSDoc doc-comments flow through identically (schemars carries them). Regenerated ui+desktop: types.ts BYTE-IDENTICAL, openapi.json canonically-equal (jq -S) vs baseline. Generated paths reverted via git checkout.","status":"verified"}
{"id":"server-update-04","angle":"api-surface","finding":"OperationId ServerUpdate.getStatus + the 200/401 responses + the 403 example (built in the retained handlers.rs via with_permission::<(ServerUpdateRead,)>) are unchanged. The permission string server_update::read is byte-identical, so the UI Permissions enum (scraped from the 403 example) still gains ServerUpdateRead. The moved 1 test asserts exactly this (names()/permissions()/descriptions()).","status":"verified"}
{"id":"server-update-05","angle":"shim-transparency","finding":"server_update/mod.rs keeps `mod checker/handlers/routes;` + registration and does `pub use ziee_server_update::{permissions, types};` so super::types::UpdateStatusResponse (checker+handlers) + super::permissions::ServerUpdateRead (handlers) resolve. ziee + ziee-desktop compile exit 0; zero call-site edits outside mod.rs+Cargo.toml.","status":"verified"}
{"id":"server-update-06","angle":"cross-crate-resolution","finding":"ziee-server-update Cargo.toml deps (ziee-identity + serde + schemars) version-match the ziee server catalog → single src-app/Cargo.lock unifies. sdk `cargo check --workspace` exit 0; ziee/ziee-desktop check exit 0.","status":"verified"}
{"id":"server-update-07","angle":"boundary","finding":"ziee-server-update names NO app type + NO DB: types=serde/schemars; permissions=ziee_identity. No crate::modules, no sqlx/query!, no build.rs, no env!(CARGO_PKG_VERSION) (that stayed with checker in ziee). The RequirePermissions handlers + checker (version+config coupling) + AppModule registration correctly stayed app-side. Build-DB-free.","status":"verified"}
{"id":"server-update-08","angle":"test-fidelity","finding":"cargo test -p ziee-server-update => 1 passed (server_update_403_example_carries_the_read_permission — pins the tuple names()/permissions()/descriptions() feeding the UI Permissions enum). Moved verbatim with permissions.rs. The checker.rs tests (semver/extract_release/set_enabled/config-default) stayed in ziee with checker.rs.","status":"verified"}
{"id":"server-update-09","angle":"desktop-parity","finding":"cargo check -p ziee-desktop exit 0; desktop --generate-openapi yields types.desktop.ts BYTE-IDENTICAL + openapi.desktop.json canonically-equal vs baseline. The desktop forces update_check off (own updater) but the wire type + permission surface are identical regardless.","status":"verified"}
