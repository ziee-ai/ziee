{"id":"sbx-01","angle":"equivalence","finding":"PHASE 1: the 4 support crates moved BYTE-FOR-BYTE via `git mv` (no source edit). `git diff` of every crate body across the move is rename-only; the sole Cargo.toml change is that `[lints] workspace = true` now resolves against sdk/Cargo.toml's new `[workspace.lints]` (mirrored 1:1 from src-app). Rust crate names unchanged → all `use sandbox_vm_protocol::/sandbox_seccomp::` in code_sandbox resolve unchanged, zero source edits.","status":"verified"}
{"id":"sbx-02","angle":"build-gate","finding":"PHASE 1 gates all green: `cargo check -p ziee`=0 (3 pre-existing unrelated warnings), `cargo check -p ziee-desktop`=0, `cd sdk && cargo check --workspace`=0 (stock, no DATABASE_URL). Confirmed each of the 4 crates checks explicitly (`cargo check -p sandbox-vm-protocol -p sandbox-seccomp -p sandbox-guest-agent -p sandbox-vm-launcher` Finished).","status":"verified"}
{"id":"sbx-03","angle":"path-deps","finding":"src-app/server/Cargo.toml repointed both consumed deps: sandbox-seccomp (optional, code_sandbox_seccomp feature, cfg(linux)) and sandbox-vm-protocol (cfg(any(linux,macos,windows))) → ../../sdk/crates/. The 4 crates' internal cross-refs (../sandbox-vm-protocol, ../sandbox-seccomp) stay valid: all 4 relocated together as siblings. src-app workspace members list drops the 4.","status":"verified"}
{"id":"sbx-04","angle":"submodule-integrity","finding":"sdk is a git SUBMODULE (gitlink, mode 160000), not a subtree. The initial `git mv` crossed the submodule boundary and corrupted the top-level index (staged `D sdk` + renames INTO the submodule). Unwound via `git reset HEAD .`; verified sdk restored as a 160000 gitlink. Correct protocol: commit the crate moves INSIDE the sdk repo; the top-level ziee repo stages the src-app deletions + Cargo edits + the bumped sdk gitlink.","status":"verified"}
{"id":"sbx-05","angle":"boundary","finding":"PHASE 2 STOP: the engine boundary on the CURRENT base is messier than the parked Increment-2 spec. The whole engine-candidate set's EXTERNAL crate:: surface is small (AppError, CodeSandboxConfig, get_app_data_dir, lit_search x2), but the parked 14-substitution list OMITS 5 real couplings: (A) lit_search /lit reverse-dep INSIDE build_bwrap_argv; (B) execute.rs→handlers::apply_workspace_mode; (C) mac_vm direct runtime_mount::READY read; (D) VM-backend embedded guest-agent staging (embedded::ensure/wsl2_agent_embedded/helper_service, uses get_app_data_dir); (E) streaming.rs progress-callback fetch + is_flavor_cached/is_fetch_in_flight. See BOUNDARY.md.","status":"blocker"}
{"id":"sbx-06","angle":"security","finding":"Hardening byte-identical audit is PENDING Phase-2 execution (nothing moved yet → no before→after argv to assert). The METHOD is preserved: for fixed (rootfs_mount_dir, CodeSandboxResourceLimits, ctx, command, caps), `assert_eq!(build_bwrap_argv(before), build_bwrap_argv(after))` (pure fn), plus empty `git diff` over the cgroup write_controller region + the SeccompPipe::install region. BLOCKER found by this audit: build_bwrap_argv currently contains a `crate::modules::lit_search` reverse-dep (the /lit --ro-bind, sandbox.rs ~L809-825) — the most security-sensitive function references an app module, so it cannot move to a build-DB-free crate until the /lit bind is lifted to a caller-injected extra_ro_bind (argv output stays byte-identical; builder SOURCE changes). No isolation weakened — the move is deferred, not faked.","status":"pending"}
{"id":"sbx-07","angle":"build-db-free","finding":"PHASE 2 target: `grep -rn 'query!|query_as!|Repos|code_sandbox_versions|code_sandbox_settings' sdk/crates/ziee-sandbox/src` must be empty. Feasible: the engine's only DB-shaped coupling is via the two module globals (runtime_mount READY / runtime_fetch / resource_limits_cache CACHE) + version_manager's in-mem registry, all hoistable behind the injected RootfsProvider/ResourceLimitsProvider. CodeSandboxResourceLimits keeps `#[derive(sqlx::FromRow)]` behind a macros-only sqlx dep (no DATABASE_URL) so ziee's query_as mapping survives. NOT yet created on this base (parked Inc-1 scaffold exists only in sdk-sandbox-wt).","status":"pending"}
{"id":"sbx-08","angle":"version_manager-split","finding":"The version_manager.rs split (91 KB) is feasible: ~10 DB sites (query!/query_as!/Repos) live in the registry region (L1-1370); the in-mem registry + fs helpers to move are localized in the tail (InflightKind@1374, InflightGuard@1382, register_mount@1427, acquire_inflight@1456, wipe@1816, sentinel@1853). DB half STAYS; the tail's TYPES move to seam vocabulary; ziee re-exports. Matches parked S-4. Real surgery on a large file, not a transcription.","status":"pending"}
{"id":"sbx-09","angle":"cross-platform","finding":"Only linux_bwrap is runtime-verifiable here; mac_vm/wsl2 are #[cfg(target_os=macos/windows)] → NOT compiled on this Linux host. Boundary items C+D (READY global, embedded guest-agent staging) live on those arms, so a wrong seam-rewrite would NOT trip the Linux cargo-check gate but WOULD silently break the mac/windows builds (verified only on those hosts). Moving ~250 KB of under-specified, un-verifiable VM-backend code and claiming a pass is the specific risk that motivates the Phase-2 STOP.","status":"blocker"}
{"id":"sbx-10","angle":"golden","finding":"PHASE 1: no handlers/OpenAPI touched → OpenAPI spec + types.ts unaffected (leaf-crate relocation only). PHASE 2 preserves this — every route/handler/model STAYS in ziee, so golden stays byte-identical both surfaces; to be confirmed at Phase-2 execution (regen, diff, `git checkout --`).","status":"verified-phase1"}
{"id":"sbx-11","angle":"security","finding":"HARDENING BYTE-IDENTICAL AUDIT (Linux, load-bearing) — EXECUTED + PASSING. Coupling A (the /lit reverse-dep inside build_bwrap_argv) lifted to caller-injected SandboxContext.extra_ro_binds. New test sandbox::tests::lit_bind_lift_is_byte_identical asserts on the REAL build_bwrap_argv Vec<String>: (1) empty extra_ro_binds emits NO /lit token (inline block truly removed); (2) injected (host_view,/lit) emits `--ro-bind-try <host_view> /lit` contiguous; (3) positioned AFTER the /proc masks and BEFORE the prlimit tail (the exact old all_ro_binds.push slot); (4) the ONLY delta vs the bare argv is the 3 injected tokens — every other token byte-for-byte identical. Result: 23 passed 0 failed. cgroup/prlimit/seccomp emission untouched (self-contained: ziee_core::AppError + resource_limits + sandbox_seccomp crate only). NO isolation weakened; NO hardening-equivalence break exists → STOP condition does not apply.","status":"verified"}
{"id":"sbx-12","angle":"build-gate","finding":"`cargo check -p ziee`=0 after the /lit lift (3 pre-existing unrelated warnings — McpRepository dead_code). CARGO_TARGET_DIR=/data/pbya/ziee/tmp/sdk-sbxengine-target, DATABASE_URL=...:54321. Caught + fixed a 10th SandboxContext construction site the code_sandbox-scoped explorer missed: workflow/dispatch.rs:800 (workflow SandboxDispatcher) — populated with the identical lit computation (ziee-side) so the workflow sandbox-step /lit bind is byte-identical too, not silently dropped.","status":"verified"}
{"id":"sbx-13","angle":"boundary-resolution","finding":"All 5 extended-seam couplings resolved (design in TRANSFORMS.md): A=/lit lift (EXECUTED+VERIFIED). B=apply_workspace_mode → relocate the pure per-OS chmod into the engine (DB-free). C=MOOT (explorers confirmed VM backends do NOT read runtime_mount::READY — only a stale comment at mac_vm.rs:153; the original item C over-modeled it; no accessor needed). D=guest-agent staging → a set-once engine app_data_dir global (set by ziee init) read by embedded.rs/wsl2_agent_embedded.rs do_extract — chosen over a 3rd provider trait because those two do_extract bodies are the ONLY app_data consumers and keeping ensure() arg-free means ZERO signature churn on the un-verifiable mac/win arms. E=streaming.rs STAY (rootfs-policy: is_flavor_cached/is_fetch_in_flight/progress-callback ensure_fetched).","status":"verified"}
{"id":"sbx-14","angle":"seam-design","finding":"Provider seam finalized: ResourceLimitsProvider{async load_from_db()->Limits} (only get()'s Repos read injected; snapshot_or_defaults() stays byte-identical DB-free) + RootfsProvider{ensure_rootfs_ready, cache_dir, evict_by_version_flavor, ensure_fetched, ensure_fetched_format (progress as Box<dyn Fn>)}. register_mount/acquire_inflight/InflightGuard/MountedArtifact/wipe/sentinel are DB-FREE → MOVE to engine registry (not provider methods); mac/wsl version_manager::register_mount → ziee_sandbox::registry::register_mount. CodeSandboxState drops `pool`, gains rootfs+limits Arcs; the 5 ziee-side &CodeSandboxState-pool sites (runtime_mount:228,424 / runtime_fetch:164 / version_handlers:166,235) refactor to explicit fields (all Linux-verifiable).","status":"verified"}
{"id":"sbx-15","angle":"version_manager-split","finding":"Split boundary confirmed by explorer against line numbers: DB half STAYS (runtime sqlx::query with code_sandbox_versions/code_sandbox_settings SQL literals → would trip the build-DB-free grep). DB-FREE tail MOVES: MountedArtifact@1341, InflightKind@1374, InflightGuard@1382(+Drop), register_mount@1427, acquire_inflight@1456, mounted_artifact@1472/list_mounted_artifacts@1478, wipe_install_caches_for_conversation@1816, consume_workspace_sentinel@1853 + MOUNTED_ARTIFACTS/DRAINING_ARTIFACTS statics + sentinel consts + WipeSentinel/WipeResult/wipe_install_caches_in_root. resource_limits.rs splits: struct+Update+validate→engine (FromRow behind macros-only sqlx), get_resource_limits@190/update_resource_limits@227 (query_as over code_sandbox_settings)→STAY ziee.","status":"pending-move"}
{"id":"sbx-16","angle":"cross-platform","finding":"mac_vm(cfg macos)/wsl2(cfg windows) arms compile-gated here, runtime-verified on the human's hosts. The /lit carry-forward onto guest argv (extra_ro_binds: ctx.extra_ro_binds.clone() in both guest_ctx re-derivations) is byte-identical to the old inline path (which computed the same host conversation_view_dir from the same conversation_id). Exact mac/win verify commands (cargo check + tier4/tier6 with ZIEE_SANDBOX_ROOTFS) documented in BOUNDARY.md for the human. The physical VM-arm seam-rewrite (register_mount→registry, ensure_fetched→provider, app_data_dir global) is specified in TRANSFORMS.md step 4.","status":"pending-move"}
{"id":"sbx-17","angle":"golden","finding":"OpenAPI + types.ts unaffected: every route/handler/model STAYS ziee-side (handlers.rs, routes.rs, version_handlers.rs, models.rs untouched by the engine boundary). The /lit lift touches no serialized wire type — SandboxContext is an internal per-call struct, never in the OpenAPI surface. SandboxAvailability::PoolMissing kept (never-hit after de-pool) so the versions-admin enum wire form is byte-identical. Golden regen byte-identical to be re-confirmed at crate-move execution.","status":"verified-nochange"}
{"id":"sbx-18","angle":"drift+scope-honesty","finding":"DRIFT-1: 0 (the executed /lit lift matches TRANSFORMS.md exactly). FIX_ROUND-1: 0 (no audit fixes needed on the lift). SCOPE HONESTY: this session EXECUTED+VERIFIED coupling A (the security crux + #1 move prerequisite) and RESOLVED the design for B-E into a turnkey mechanical spec (TRANSFORMS.md). The physical sdk/crates/ziee-sandbox crate creation + ~25-file move + version_manager split + provider wiring is an all-or-nothing-compile follow-up NOT physically carved this session — specified in full but not executed. Reported transparently rather than risking a broken tree by a partial move.","status":"partial"}
{"id":"sbx-19","angle":"execution","finding":"PHYSICAL CARVE EXECUTED. Created build-DB-free crate sdk/crates/ziee-sandbox (edition 2024; deps ziee-core+ziee-framework+sandbox-vm-protocol[all3]+sandbox-seccomp[linux,opt,feature code_sandbox_seccomp]+windows-sys/-service/winreg[win]+async-trait/tokio/uuid/once_cell/dashmap/axum/serde/serde_json/schemars/chrono/sha2/tracing/rmcp/libc + sqlx{derive,macros-only,NO postgres/runtime}). git mv (byte-identical) 32 files: cgroup,probes,sandbox,mcp_spawn,mount_provider,workflow_staging,config,types,models,resource_limits,resource_limits_cache,tools/execute + backend/** tree. Created engine lib.rs,tools/mod.rs,sandbox_config.rs,provider.rs,registry.rs and ziee providers.rs.","status":"done"}
{"id":"sbx-20","angle":"splits","finding":"version_manager(2295L): DB half STAYS; registry tail MOVED to registry.rs (MountedArtifact+wait_until_drained method, InflightKind, InflightGuard+Drop, MOUNTED_ARTIFACTS/DRAINING_ARTIFACTS statics raised pub, register_mount/acquire_inflight/mounted_artifact/list_mounted_artifacts, WIPE/SENTINEL consts + WipeSentinel/WipeResult pub + wipe_install_caches_*/consume_workspace_sentinel + 5 tests; verified 0 query!/Repos). resource_limits: value types+validate+tests MOVE; the 2 CodeSandboxRepository DB methods MOVE to ziee repository.rs. CodeSandboxConfig (struct+Default+rootfs_path/workspace_root/public_file_origin impls+2 default fns+public_file_origin_tests) MOVED to sandbox_config.rs; core/config.rs -> pub use ziee_sandbox::sandbox_config::CodeSandboxConfig. provider-vocab (EnsureOutcome,EvictOutcome from runtime_mount; FetchProgress/Phase/Outcome/RootfsFormat/FetchError from runtime_fetch) MOVED to provider.rs, ziee re-imports; ReadyError stays. apply_workspace_mode MOVED handlers.rs->tools/execute.rs.","status":"done"}
{"id":"sbx-21","angle":"seams","finding":"3 seams on de-pool'd CodeSandboxState{config,loopback_url,workspace_root,host_caps,rootfs:Arc<dyn RootfsProvider>,limits:Arc<dyn ResourceLimitsProvider>,guest_agent:Arc<dyn GuestAgentProvider>,manual Debug}. RootfsProvider{ensure_rootfs_ready,cache_dir,evict_by_version_flavor,ensure_fetched,ensure_fetched_format,shutdown}; ResourceLimitsProvider{load_from_db}; GuestAgentProvider (DEVIATION from TRANSFORMS app_data_dir-global: embedded.rs+wsl2_agent_embedded.rs STAY in ziee because include_bytes!(env!(CARGO_MANIFEST_DIR)/binaries/..) is server-build.rs-staged; sync, reached via config::get_state() since mac_vm/wsl2 call sites are sync assoc-fns w/ no state param). Ziee providers.rs: ZieeRootfsProvider{pool,host_caps,config}/ZieeResourceLimitsProvider{pool}/ZieeGuestAgentProvider delegate to refactored-to-explicit-fields runtime_mount/runtime_fetch/repository/embedded. resource_limits_cache::get(&dyn ResourceLimitsProvider).","status":"done"}
{"id":"sbx-22","angle":"evict-reachability","finding":"evict_artifact/shutdown resolution: READY/MOUNTED mount statics + evict_by_version_flavor + shutdown OWNED by ziee runtime_mount (stay); only active().evict_artifact callers are ziee version_manager. Chose hybrid option-(b): RootfsProvider gained evict_by_version_flavor+shutdown; engine backend-trait default evict_artifact + linux_bwrap::shutdown route through crate::config::get_state().rootfs.* (engine-owned global), NOT naming runtime_mount. active().evict_artifact dispatch unchanged so mac/wsl2 VM-teardown overrides keep byte-identical per-OS behavior (pure literal option-b would have regressed VM teardown).","status":"done"}
{"id":"sbx-23","angle":"gates","finding":"ALL 6 GATES GREEN (independently re-run by orchestrator): (1) cargo check -p ziee = 0 (only 3 pre-existing warnings KB_MAX_DOCUMENTS_DEFAULT/is_active/list_enabled_for_health_check; no new). (2) cargo check -p ziee-desktop = 0. (3) cd sdk && cargo check --workspace = 0. (4) build-DB-free grep sdk/crates/ziee-sandbox/src = clean (only comments). (5) cargo test -p ziee-sandbox = 64 passed 0 failed incl sandbox::tests::lit_bind_lift_is_byte_identical + all tier-1 argv + registry/sandbox_config/types/resource_limits tests. (6) OpenAPI+types.ts: zero git delta (handlers stay). HARDENING BYTE-DIFF: normalized diff of sandbox.rs HEAD-vs-engine shows ONLY import-path + seam-call rewrites (L118-145) + test-provider stubs; entire build_bwrap_argv body (L146-1388: bwrap flags/cgroup/seccomp/prlimit/mounts) BYTE-IDENTICAL. No isolation weakened.","status":"done"}
{"id":"sbx-24","angle":"cross-platform-caveat","finding":"Only Linux linux_bwrap path compile-verified here. mac_vm(cfg macos)/wsl2(cfg windows) seam rewrites (state.rootfs.{cache_dir,ensure_fetched,ensure_fetched_format}, crate::registry::register_mount, config::get_state().guest_agent staging, resource_limits_cache::get(state.limits), /lit extra_ro_binds carry-forward) are #[cfg]-out on Linux -> need mac/Windows-host runtime verification (tier4/tier6 with ZIEE_SANDBOX_ROOTFS) per BOUNDARY.md. Also: server Cargo code_sandbox_seccomp now FORWARDS to ziee-sandbox/code_sandbox_seccomp (leaving dep:sandbox-seccomp would silently disable seccomp in default build); removed now-unused direct sandbox-seccomp/sandbox-vm-protocol server deps.","status":"done"}
