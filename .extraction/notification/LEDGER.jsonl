{"id":"notification-01","angle":"equivalence","finding":"models.rs moved BYTE-FOR-BYTE — `diff <(git show 4a2391732:server/src/modules/notification/models.rs) sdk/crates/ziee-notification/src/models.rs` empty (exit 0). Notification (+is_unread), NewNotification builder (new/body/silent/task/workflow_run/conversation), NotificationPage, UnreadCount unchanged. permissions.rs differs by ONLY the PermissionCheck import.","status":"verified"}
{"id":"notification-02","angle":"migration-integrity","finding":"Both migrations (202607140190_notification_schema.sql, 202607144180_notification_fkeys.sql) moved BYTE-FOR-BYTE to crates/ziee-notification/migrations/ — original filenames+content, so sqlx checksums + version numbers preserved. App-side migrations/ dir deleted, so each file is globbed EXACTLY ONCE via sdk/crates/*. The N7 composition is proven: ziee's build.rs provisioned the merged build DB (incl. these two from the crate) and cargo check -p ziee (10 query_as! verify) exit 0.","status":"verified"}
{"id":"notification-03","angle":"equivalence","finding":"repository.rs correctly RETAINED in ziee: its 10 sqlx::query_as! verify against the FULL merged build DB, and the notification fkeys reference users/conversations/scheduled_tasks/workflow_runs (other modules) — a standalone crate build DB would fail those FKs (unlike self-contained ziee-auth). So repository stays; ziee-notification needs NO build.rs/build DB. repository reaches the moved Notification/NewNotification via super::models (shim), byte-identical queries.","status":"verified"}
{"id":"notification-04","angle":"equivalence","finding":"events.rs correctly RETAINED: create_and_emit names the CONCRETE crate::modules::sync::{Audience, SyncAction, SyncEntity(::Notification), publish} — app-owned sync enums. Moving it would need a ziee-auth-style injected seam (a rewrite), so it stays. The Audience::owner/origin=None emit + super::repository::insert call are unchanged.","status":"verified"}
{"id":"notification-05","angle":"canonical-equality","finding":"schemars keys Notification/NotificationPage/UnreadCount by short ident (unchanged by the crate move); doc comments flow through as JSDoc identically. Regenerated ui+desktop: types.ts BYTE-IDENTICAL, openapi.json canonically-equal (jq -S) vs baseline. Generated paths reverted via git checkout.","status":"verified"}
{"id":"notification-06","angle":"shim-transparency","finding":"notification/mod.rs keeps `pub mod events/handlers/prune/repository/routes;` + registration and does `pub use ziee_notification::{models, permissions};` so super::models (repository/events/handlers), super::permissions (handlers), and scheduler/dispatch.rs's crate::modules::notification::models::NewNotification all resolve. ziee + ziee-desktop compile exit 0; zero call-site edits outside mod.rs+Cargo.toml.","status":"verified"}
{"id":"notification-07","angle":"cross-crate-resolution","finding":"ziee-notification Cargo.toml deps (ziee-identity + serde/serde_json/schemars + sqlx[postgres,macros,chrono,uuid,json,time] for FromRow (no query!) + chrono + uuid) version-match the ziee server catalog → single src-app/Cargo.lock unifies. sdk `cargo check --workspace` exit 0; ziee/ziee-desktop check exit 0. No build.rs → sqlx macros compile without a per-crate DATABASE_URL.","status":"verified"}
{"id":"notification-08","angle":"boundary","finding":"ziee-notification names NO app type + issues NO query!: models=chrono/schemars/serde/uuid/sqlx::FromRow; permissions=ziee_identity. No crate::modules, no query!, no build.rs. The schema-bound repository (query_as!), sync-coupled events (SyncEntity), RequirePermissions handlers, prune, and AppModule registration correctly stayed app-side. Build-DB-free crate that CARRIES the schema (migrations/).","status":"verified"}
{"id":"notification-09","angle":"test-fidelity","finding":"cargo test -p ziee-notification => 0 tests (models.rs + permissions.rs had no #[cfg(test)] units; the builder/serde behavior is exercised by ziee's retained integration tests). cargo check -p ziee-notification + the app-side repository query_as! verification (cargo check -p ziee) both exit 0 — the compile-time proof the moved types match the moved schema.","status":"verified"}
{"id":"notification-10","angle":"desktop-parity","finding":"cargo check -p ziee-desktop exit 0; desktop --generate-openapi yields types.desktop.ts BYTE-IDENTICAL + openapi.desktop.json canonically-equal vs baseline. The desktop crate embeds the same ziee lib + the same merged migration set (its build.rs points at the same ziee_build_<key>), so the moved migrations compose identically.","status":"verified"}
