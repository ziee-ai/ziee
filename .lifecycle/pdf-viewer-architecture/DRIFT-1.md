# DRIFT-1 — implementation vs plan

Audited the implementation against PLAN.md / DECISIONS.md / TESTS.md after the
first implementation pass (backend green + regen; `npm run check` green in BOTH
ui workspaces; pure unit tests green).

- **DRIFT-1.1** — verdict: impl-wins — PLAN's ITEM-6/7/8 implied a `pdfjs-header.tsx` toolbar in the header slot. In reality the file viewer's header (`headerActions`) and body are INDEPENDENT components that coordinate only via the store; DEC-6 keeps the PDFViewer controller as component-local state. So the page-nav/zoom/find toolbar is co-located INSIDE `pdfjs-body.tsx` (a sticky bar above the scroll container), which is where the controller lives. Scope unchanged (all controls delivered). PLAN's ITEM-6/7/8 wording + the module.tsx comment already reflect this.
- **DRIFT-1.2** — verdict: impl-wins — no separate `pdfjs-header.tsx` was created; the PDF entry reuses the existing `PdfHeader` (which is just the Download button — identical to what a new file would contain). Fewer files, no behavior change. The `## Files to touch` list named `pdfjs-header.tsx`; it was unneeded.
- **DRIFT-1.3** — verdict: resolved — the `search.ts` pure helper named in an earlier PLAN revision was eliminated: PDF.js's `PDFFindController` owns match enumeration/highlight/next-prev (DEC-10/DEC-11). PLAN's Files list + TESTS were amended (search covered by e2e TEST-11) before implementation.
- **DRIFT-1.4** — verdict: impl-wins — the planned `module.test.ts` unit test for the viewer-registry split can't run: `node --test` strips TS types but can't transpile the JSX in `module.tsx`, and can't resolve `@/` aliases. ITEM-10's routing is proven end-to-end by the e2e (TEST-8, which asserts the PDF.js toolbar renders for a `.pdf`). TESTS.md was amended (TEST-7 removed; ITEM-10 → TEST-8).
- **DRIFT-1.5** — verdict: resolved — teardown uses `loadingTask.destroy()` (aborts network + tears down the document + worker), because pdfjs v6's `PDFDocumentProxy` has no `destroy()` method (only `cleanup()`). Same lifecycle guarantee as DEC-5 intended; a type-driven refinement, not a behavior change.
- **DRIFT-1.6** — verdict: none — `just openapi-regen` produced a large positional (key-order) diff in both `openapi.json` files (~1900 lines) but the SEMANTIC content delta is ONLY the new `/api/files/{file_id}/raw` operation: verified by sorting both specs with `json.dumps(sort_keys=True, indent=1)` and diffing — every changed line is under the `/raw` path block (`File.getRaw`, BlobType 200 response) with nothing unrelated. Mechanically-generated; excluded from the coverage law.
- **DRIFT-1.7** — verdict: resolved — the new `PdfJsBody` conditional states forced gallery gate updates not itemised in PLAN: `stateCoverage.ts` (pdfjs-body:delayed/error allow-list entries, mirroring every sibling viewer body) + `coverage.ts` (pdfjs-body surface = `kind:'via'`) + regenerated `testIds.generated.ts` / `galleryCoverage.generated.ts` / `stateMatrix.generated.ts`. All part of ITEM-11's intent ("add gallery cells so check:state-matrix passes"); `npm run check` is green in both workspaces.

**Unresolved drifts:** 0
