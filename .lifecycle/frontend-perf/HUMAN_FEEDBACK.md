# frontend-perf — HUMAN_FEEDBACK (living ledger)

- **FB-1** [status: resolved] — "I see it still load LlmRepositoryDrawer and LlmModelDownloadNotifications" (on the logged-out login page, after ITEM-2's route-prefetch gating) → These are always-mounted ROOT components (not route chunks), so route-prefetch gating didn't cover them. Added `shouldMount` gates: `LlmRepositoryDrawer` → open-state (ITEM-5, mirrors the sibling drawer; also fixes its `GET /api/llm-repositories`-on-every-route bug); `LlmModelDownloadNotifications` → `usePermission(LlmModelsDownloadsRead)`; `NotificationToastListener` → `usePermission(NotificationsRead)`. Login page now loads only 2 JS chunks (entry + RouterComponent). Verified authenticated users with the perm still mount them. [generalizable: yes — a lazy always-mounted ROOT component (ComponentRegistration) that is only relevant when authenticated/permitted MUST carry a `shouldMount` auth/permission gate, else its chunk loads on the logged-out login page; route-level prefetch gating does NOT cover root components.]
