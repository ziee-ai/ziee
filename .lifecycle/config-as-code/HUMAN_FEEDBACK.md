# HUMAN_FEEDBACK — config-as-code

- **FB-1** [status: resolved] — "Oh for the MCP servers we want to add, please make sure that their modes are all auto (let the LLM decide)" → `usage_mode: auto` is now pinned explicitly on BOTH the create path AND the `enforce` re-sync path in `reconcile_mcp_server` (the update path previously sent `None` = "don't touch", which would have let a drifted value survive an enforce boot), documented in `config/desired-state.yaml`, and asserted per-server in TEST-5. [generalizable: no — feature-specific]
- **FB-2** [status: resolved] — "After you finish with everything, build an image and start a container at port 8090 with a root admin and a user, so that I can test before merging, note that container, not binary" → the `ziee-web` image is built from this branch and a container runs on **:8090** (compose project `ziee-cac`, its own Postgres + volumes, so it never touches the live :8080 stack), seeded by the reconciler with the root admin + a regular user. Recorded in DEC-13; the branch is NOT merged and cleanup (branch, worktree, container, image) is on request. [generalizable: yes — when a feature's payoff is a deploy behavior, hand the human a RUNNING container on a free port rather than a green test log]
- **FB-3** [status: wontfix] — Raised BY the audit, surfaced rather than silently actioned (DEC-8 + the audit-vs-user-decision rule): with `assistants::*` removed, the Settings→Assistants section, its route and its nav entry are correctly gone for regular users — but the **chat composer still shows the assistant picker**, which is not permission-gated and now dead-ends at "No assistants available" for those users. → **wontfix in this branch**, because you scoped the feature explicitly: *"look at the settings, there is a subsection named Assistants, we just need to make that permission to false for users so that they cannot see that, that's all, no delete anything, just set permission in the db"*. No product UI code was touched. The fix, if you want it, is the standard one-line `usePermission(Permissions.AssistantsRead)` gate on the picker (it deletes no feature; admins keep everything) — say the word and it ships as a follow-up. [generalizable: yes — when an audit surfaces a consequence of a decision the human explicitly made, record it as feedback and ASK; never quietly reverse the decision]
