# HUMAN_FEEDBACK — voice-model-mgmt

- **FB-2** [status: resolved] — "how about the runtime backend? spawn audit agents to see how lacking it is compared to llm runtime. It looks like this feature wasn't doing it correctly. Add that to human feedback about checking existing code base." → Ran a 3-agent blind parity audit of `modules/voice/**` vs `llm_local_runtime` (RUNTIME_PARITY_AUDIT.md); orchestrator re-verified the load-bearing lifecycle finding against source (F1/F2 real but MEDIUM, not HIGH; voice is AHEAD on download verification — not aligned down). Per user decision (DEC-18) ALL 11 findings (Tier A+B+C) folded into the plan as ITEM-24..33 + TEST-28..37. [generalizable: yes — before building ON an existing feature/module, AUDIT that module against its most-mature sibling for parity + correctness FIRST; never assume the code you're extending is itself correct or complete just because it exists and compiles]

- **FB-1** [status: resolved] — "The prompt earlier wasn't clear. I am talking about the voice dictation" (in answer to "model download+upload already exists for LLM models; what's the real scope?") → Re-scoped the feature from LLM models to the **voice dictation (whisper) module**: let a user DOWNLOAD a whisper model from a source and UPLOAD their own whisper model files, mirroring (a) the LLM-provider add-model download/upload UX and (b) the voice module's existing runtime-version (engine-binary) download-with-progress pattern. Survey re-pointed at `modules/voice/**`. [generalizable: yes — when a feature request's premise ("X doesn't exist yet") is contradicted by an end-to-end survey, surface the evidence and confirm the real gap BEFORE writing plan artifacts, rather than inventing a duplicate]
