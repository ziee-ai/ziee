# TESTS — knowledge-base-retrieval

Every ITEM (1–42) is covered by ≥1 TEST; user-visible UI items carry `tier: e2e`.
Real-path — mocks are LOOPBACK HTTP boundaries (rerank/embed engines), never
in-process behavior stubs. Tests strengthened per the adversarial audit: the
reranker proves promotion-from-outside-top_k; geometry proves divergent-cleaned-text
relocation; index-status proves the real worker emit; owner-scope proves cross-user
denial on the tool path. No cosmetic tests; no `#[ignore]`-to-green.

## Part R — reranker

- **TEST-1** (tier: unit) [covers: ITEM-1] file: `src-app/server/ai-providers/src/providers/openai.rs` — asserts: `rerank` POSTs `{model,query,documents}` to `/v1/rerank`, parses `results[{index,score}]`; trait default → "unsupported" for anthropic/gemini.
- **TEST-2** (tier: unit) [covers: ITEM-2] file: `src-app/server/src/modules/memory/engine/capability.rs` — asserts: `rerank_unsupported_reason` gates on `capabilities.rerank`; `"rerank"` in `ALLOWED_CAPABILITIES`; hub→llm_model map carries `rerank`.
- **TEST-3** (tier: unit) [covers: ITEM-4] file: `src-app/server/src/modules/llm_local_runtime/deployment/local.rs` — asserts: `llamacpp_argv` emits `--reranking`+`--pooling rank` iff `reranking=true`.
- **TEST-4** (tier: integration) [covers: ITEM-3, ITEM-1] file: `src-app/server/tests/file_rag/rerank_dispatch_test.rs` — asserts: `dispatch::rerank` against a **loopback `/v1/rerank` stub** returns reordered `(index,score)`; errors when the model lacks `rerank`; auto-starts a local reranker model.
- **TEST-5** (tier: integration) [covers: ITEM-4] file: `src-app/server/tests/llm_local_runtime/rerank_proxy_test.rs` — asserts: proxy forwards `/api/local-llm/v1/rerank` → engine `/v1/rerank` (stub-engine route) with bearer rewrite; rerank-capable model launches with flags.
- **TEST-6** (tier: integration) [covers: ITEM-5, ITEM-6] file: `src-app/server/tests/file_rag/rerank_settings_test.rs` — asserts: migration 135 columns; GET/PUT round-trip; range 400; probe-rerank rejects a non-rerank model; DB CHECK rejects candidate_k=201.
- **TEST-7** (tier: integration) [covers: ITEM-7] file: `src-app/server/tests/file_rag/rerank_retrieval_test.rs` — asserts: via a loopback rerank stub, a doc ranked **#20 by hybrid but #1 by the reranker is promoted into the final top_k (<20)** — proving candidate-pool expansion, not a top_k reshuffle; `truncated` recomputed; disabled → unchanged; rerank error → pre-rerank order.

## Part H — hub reranker model

- **TEST-8** (tier: unit) [covers: ITEM-8, ITEM-9] file: `hub/scripts/validate.py` — asserts: in the cloned `ziee-ai/hub`, `bge-reranker-v2-m3-gguf.yaml` validates against the updated schema (with `capabilities.rerank`) and the schema accepts `rerank` in both versions.
- **TEST-9** (tier: integration) [covers: ITEM-10, ITEM-2] file: `src-app/server/tests/hub/rerank_model_seed_test.rs` — asserts: the vendored seed index contains the reranker model with `capabilities.rerank=true`, and importing it yields an `llm_models` row with `capabilities.rerank=true` (the hub→llm_model path); `seed_index_version_matches_const` still green.

## Part I — file_rag indexing observability

- **TEST-10** (tier: integration) [covers: ITEM-11] file: `src-app/server/tests/knowledge_base/mod.rs` — asserts: [DRIFT: the doc index_status derivation is SQL-side (COALESCE(status,'pending')) + a Rust rollup, so it is exercised as an integration test] `file_index_state` status enum round-trips; the KB status derivation maps state+chunk_count → `pending|indexing|indexed|failed|no_text`.
- **TEST-11** (tier: integration) [covers: ITEM-12] file: `src-app/server/tests/file_rag/index_state_emit_test.rs` — asserts: ingesting a doc with a **loopback embed stub** drives the real background index worker to completion and **emits `sync:file_index_state` (→`indexed`) owner-scoped**; a no-text file lands `no_text`; a forced embed error lands `failed` (not a silent log). This is the emit source the live UI depends on.
- **TEST-12** (tier: integration) [covers: ITEM-13] file: `src-app/server/tests/file_rag/reindex_test.rs` — asserts: a `failed`/`no_text` file re-indexed via the reindex trigger reaches `indexed` when text is present.

## Part K — KB backend

- **TEST-13** (tier: unit) [covers: ITEM-16, ITEM-15] file: `src-app/server/src/modules/knowledge_base/permissions.rs` — asserts: permission strings match migration 134.
- **TEST-14** (tier: unit) [covers: ITEM-16] file: `src-app/server/src/modules/knowledge_base/mod.rs` — asserts: deterministic `knowledge_base_server_id()`.
- **TEST-15** (tier: unit) [covers: ITEM-18] file: `src-app/server/src/modules/knowledge_base/repository.rs` — asserts: `KB_MAX_DOCUMENTS==2000`; document_count is a live COUNT (no stored column).
- **TEST-16** (tier: unit) [covers: ITEM-20] file: `src-app/server/src/modules/knowledge_base/tools.rs` — asserts: tool schemas + grounded-answer instruction; empty scope → no hits; `structuredContent` capped at 1 MB.
- **TEST-17** (tier: unit) [covers: ITEM-21] file: `src-app/server/src/modules/knowledge_base/chat_extension/knowledge_base.rs` — asserts: order==23; attach flag + note only when tool-capable AND ≥1 KB; never chunk injection.
- **TEST-18** (tier: unit) [covers: ITEM-21] file: `src-app/server/src/modules/mcp/chat_extension/mcp.rs` — asserts: `auto_attach_builtin_ids` push iff flag; `is_builtin_server_id(kb id)` true.
- **TEST-19** (tier: unit) [covers: ITEM-21] file: `src-app/server/src/modules/sync/event.rs` — asserts: `KnowledgeBase`/`KnowledgeBaseDocument`/`FileIndexState` wire strings.
- **TEST-20** (tier: integration) [covers: ITEM-14, ITEM-19] file: `src-app/server/tests/knowledge_base/crud_test.rs` — asserts: CRUD; per-user unique-name conflict; cascade delete removes join rows; **derived document_count stays correct after an EXTERNAL file delete** (cascade) — no drift.
- **TEST-21** (tier: integration) [covers: ITEM-18, ITEM-19] file: `src-app/server/tests/knowledge_base/documents_test.rs` — asserts: attach existing, bulk upload (ingested+attached), detach; 2001st → 422; **re-dropping a duplicate (same checksum) is skipped-and-reported, not double-attached**; add→remove→re-add at the cap boundary tracks the count; paginated list-with-status.
- **TEST-22** (tier: integration) [covers: ITEM-18, ITEM-19] file: `src-app/server/tests/knowledge_base/shared_chunks_test.rs` — asserts: file F in KB-A and KB-B (and a conversation); **remove F from KB-A → F still retrievable via KB-B and `file_chunks` row-count unchanged**; **delete KB-A → F + chunks survive**. (Data-integrity landmine from the audit.)
- **TEST-23** (tier: integration) [covers: ITEM-18] file: `src-app/server/tests/knowledge_base/attach_existing_reindex_test.rs` — asserts: attaching an existing file that has 0 chunks triggers reindex → it becomes searchable.
- **TEST-24** (tier: integration) [covers: ITEM-16, ITEM-19] file: `src-app/server/tests/knowledge_base/permissions_test.rs` — asserts: no `use` → 403; foreign KB → 404 (`get_by_id_and_user`); Users member succeeds.
- **TEST-25** (tier: integration) [covers: ITEM-20, ITEM-18] file: `src-app/server/tests/knowledge_base/search_scope_test.rs` — asserts: FTS-only (no embed model) `search_knowledge` scoped to exactly the KB's files; **user A calling `search_knowledge` with user B's kb_id (and a MIXED own+foreign array) returns ONLY own-KB hits, never B's chunks** (cross-user tool leak guard); indexing_incomplete signal when not fully indexed.
- **TEST-26** (tier: integration) [covers: ITEM-20, ITEM-7] file: `src-app/server/tests/knowledge_base/search_reranked_test.rs` — asserts: with embed + rerank enabled + loopback stub, `search_knowledge` returns reranked hits with full provenance (file/page/char-span/score).
- **TEST-27** (tier: integration) [covers: ITEM-18, ITEM-19] file: `src-app/server/tests/knowledge_base/attachment_test.rs` — asserts: attach conv+project; union read-through; detach; foreign→404; `kb_attachment_targets`; `indexing-status` endpoint returns the summary + 404 for a foreign KB.
- **TEST-28** (tier: integration) [covers: ITEM-21] file: `src-app/server/tests/knowledge_base/sync_emit_test.rs` — asserts: mutations emit owner-scoped `KnowledgeBase`/`KnowledgeBaseDocument`, never to another user.
- **TEST-29** (tier: integration) [covers: ITEM-16, ITEM-20] file: `src-app/server/tests/knowledge_base/mcp_test.rs` — asserts: JSON-RPC init+tools/list; `search_knowledge` gates on `use` (403); built-in row registered at loopback.
- **TEST-30** (tier: integration) [covers: ITEM-20, ITEM-21] file: `src-app/server/tests/knowledge_base/agent_retrieval_real_llm_test.rs` — asserts: a tool-capable model with a KB attached **actually fires `search_knowledge` (verified against the `mcp_tool_calls`/transcript) and answers from a SYNTHETIC un-guessable fact** present only in the KB doc. Requires tools+embed; skips only without a key/bridge.

## Part C — ingest-time geometry + highlight (backend)

- **TEST-31** (tier: unit) [covers: ITEM-22] file: `src-app/server/src/modules/file/processing/pdf.rs` — asserts: `clean_extracted_text` emits a geometry array **the same length as the cleaned string**, and for a page whose cleaning **diverges** (collapsed multi-space, a hyphenated line-break word, a stripped running header) a cleaned `[start,end)` span maps to boxes that **bound the CORRECT passage** (not merely non-empty); landscape rects are geometrically correct under rotation.
- **TEST-32** (tier: integration) [covers: ITEM-23, ITEM-24] file: `src-app/server/tests/file/geometry_persist_test.rs` — asserts: geometry is persisted at ingest and re-derived for a chunk span; the backfill captures geometry for a pre-existing file.
- **TEST-33** (tier: integration) [covers: ITEM-25] file: `src-app/server/tests/file/text_rects_test.rs` — asserts: `text-rects` returns correct fraction rects for a real chunk span; **a non-PDF (docx) file → 200 `{rects:[]}` (page-level fallback, not 500)**; gated `FilesRead` + `get_by_id_and_user` (403 no-perm; foreign file → 404).

## Part K-UI / C-UI / R-UI — frontend

- **TEST-34** (tier: unit) [covers: ITEM-27] file: `src-app/ui/src/modules/knowledge-base/stores/KnowledgeBases.store.ts` — asserts: create/delete reducers; `sync:knowledge_base` self-gates on `KnowledgeBaseUse`.
- **TEST-35** (tier: unit) [covers: ITEM-27, ITEM-31] file: `src-app/ui/src/modules/knowledge-base/stores/KnowledgeBaseDetail.store.ts` — asserts: `sync:file_index_state` patches a single doc's status (no refetch); `sync:file` (external delete) refetches the list/count; status maps incl. `no_text`.
- **TEST-36** (tier: unit) [covers: ITEM-35, ITEM-36, ITEM-37] file: `src-app/ui/src/modules/knowledge-base/chat-extension/components/SearchKnowledgeToolResultCard.tsx` — asserts: [DRIFT-1.2: no `[n]` chips — the tool cites by file/page in prose, so the transparency card is the citation surface] the static `contentMatch` claims only `search_knowledge` tool_results; `structuredContent.hits`→per-passage rows `{filename,page,score,content}`; the empty (`hits:[]`→"No passages matched") and indexing-incomplete (`searchable<total`→banner) mappers; the `openSource` deep-link builds a `kb_source` panel payload `{fileId,page,charStart,charEnd}`.
- **TEST-37** (tier: e2e) [covers: ITEM-26, ITEM-28, ITEM-29, ITEM-30] file: `src-app/ui/tests/e2e/14-knowledge-base/kb-list.spec.ts` — asserts: "Knowledge" nav → `/knowledge`; empty state; create via drawer; card status chip; detail overview + retrieval-mode line + **direct search box returns hits**; rename; delete.
- **TEST-38** (tier: e2e) [covers: ITEM-31, ITEM-13] file: `src-app/ui/tests/e2e/14-knowledge-base/kb-documents.spec.ts` — asserts: folder + multi upload; per-doc badge reaches `indexed` (real stubbed embed path); a `failed` row **Retry → indexed**; a `no_text` doc shows the advisory; **duplicate re-drop shows "already in this KB"**; **oversize/too-many shows an itemized reject toast**; bulk retry-all-failed; cap counter; multi-select remove; empty/loading.
- **TEST-39** (tier: e2e) [covers: ITEM-32, ITEM-33] file: `src-app/ui/tests/e2e/14-knowledge-base/kb-attach-chat.spec.ts` — asserts: `+`→picker (empty→link to `/knowledge`); "Knowledge · N" pill + per-KB remove; project-inherited read-only chips; persists across reload.
- **TEST-40** (tier: e2e) [covers: ITEM-35, ITEM-36, ITEM-37] file: `src-app/ui/tests/e2e/14-knowledge-base/kb-citation-flow.spec.ts` — asserts: [DRIFT-1.2: transparency card is the citation surface, no `[n]` chips] the `search_knowledge` transparency card renders per-passage rows (source·page·score) (and **empty → "No passages matched"**; **indexing-incomplete banner** when set); "Open source" opens the right-panel `kb_source` viewer at the cited page **with a highlight box** and **page-only for an office doc**.
- **TEST-41** (tier: e2e) [covers: ITEM-34] file: `src-app/ui/tests/e2e/14-knowledge-base/kb-project-extension.spec.ts` — asserts: the "Knowledge bases" knowledge-kind binds a KB and shows it in the inline preview.
- **TEST-42** (tier: e2e) [covers: ITEM-38] file: `src-app/ui/tests/e2e/14-knowledge-base/reranker-admin.spec.ts` — asserts: [DRIFT: `rerank` is an additive capability flag, not mutually exclusive with chat] the Reranker section renders on `/settings/file-rag`; with no rerank-capable model the **hub nudge** shows; selecting a model + enabling + candidate-k persist (PUT round-trip reflected after reload).
- **TEST-43** (tier: e2e) [covers: ITEM-41, ITEM-28, ITEM-30, ITEM-31] file: `src-app/ui/tests/e2e/visual/knowledge-base.states.spec.ts` — asserts: gallery renders KB list (loaded/empty/error), detail (loaded/not-found/indexing), documents panel (empty/uploading/indexing/failed/no_text/reject), picker, transparency panel (empty/populated/incomplete), highlight overlay (portrait/landscape/office-fallback), reranker section (with/without model), **narrow-viewport** variants — zero runtime HIGH; Layer-A axe; for `ui` and `desktop/ui`.

## Part X — cross-cutting

- **TEST-44** (tier: integration) [covers: ITEM-39, ITEM-17] file: `src-app/server/src/openapi/emit_ts.rs` — asserts: `types_ts_parity` green (Knowledge*/Rerank*/text-rects/FileIndexState regenerated) for the server binary; desktop via `npm run check (desktop/ui)` (ITEM-40).
- **TEST-45** (tier: unit) [covers: ITEM-40] file: `src-app/desktop/ui/src/api-client/types.ts` — asserts: [DRIFT-1.3: desktop is a thin shell that omits the chat/knowledge UI surface, so there is NO desktop KB module; the desktop binary embeds the server, so its api-client must expose the KB/File REST surface] the regenerated desktop api-client contains `KnowledgeBase.listConversation`/`listProject` and types `File.getTextRects` as `TextRectsResponse` (confirmed by `npm run check (desktop/ui): PASS`).
- **TEST-46** (tier: unit) [covers: ITEM-42] file: `src-app/server/tests/knowledge_base/mod.rs` — asserts: `CLAUDE.md` contains the "Knowledge Base" header, names `search_knowledge`, the `rerank` capability, and the hub-delivery + file_index_state notes.
