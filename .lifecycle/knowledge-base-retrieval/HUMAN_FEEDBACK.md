# HUMAN_FEEDBACK — knowledge-base-retrieval

Living ledger for **Iteration mode** (feature already merged to main; refining
conversationally). Every piece of human feedback is captured here VERBATIM as a
new `FB-N` with `status: open`, then implemented + covered by a test, then
flipped to `resolved`. Phase 9 fails while any `open` remains, so a merge
checkpoint cannot close with unaddressed feedback.

The original build predated Phase 9; this ledger starts fresh for the iteration
round. No feedback has been received yet — awaiting the human's first item.

## Feedback

- **FB-1** [status: resolved] — the knowledge page was built after the project page, the card of knowledge base should look similar with the card of the project page, this means the initial implementation did not follow the UI pattern and design, it needs to do a great explore for any component that it's going to implement and make sure that we have precedent implementation to follow, and might improve or add elements from that → Aligned `KnowledgeBaseCard` to its precedent `ProjectCard`: title now uses the same `!font-normal !text-sm` typography (was rendering as a bold default `Title level={5}` heading) and the non-precedent leading `<Library/>` icon was removed, so both list-grid entity cards read as one system; kept the additive KB-only doc-count + indexing-status `Tag` row as a legitimate improvement. Ran the precedent sweep on the sibling KB surfaces too — `KnowledgeBasesListPage`, `KnowledgeBaseFormDrawer` were already faithful mirrors of `ProjectsListPage`/`ProjectFormDrawer`; drift was localized to the card. Covered by TEST-50 (e2e computed-typography guard). [generalizable: yes — before implementing ANY UI component, first locate the closest existing precedent component and mirror its structure/typography/tokens exactly; only then add feature-specific elements. A card/page/drawer that diverges visually from its sibling surface is a defect, not a variant. (Reinforces feedback_match_existing_patterns / the feature-lifecycle Phase-1 "Patterns to follow" rule — make it a Phase-6 audit angle: "precedent-fidelity" for every new UI surface.)]
- **FB-2** [status: resolved] — now the knowledge page do not have proper pagination like chats and projects do, this is another things that need to be hardness in the skills, it needs to examine and think of standard features and think of what happen when we have a lot of the same stuff → Added the same client-side "Load More" paging `ProjectsListPage` + the chat `ConversationList` use to `KnowledgeBasesListPage`: `PAGE_SIZE = 12`, reveal a page at a time, a "Showing N of M knowledge bases" live-status line + a `kb-list-load-more-btn` that appears only while `hasMore` — restructured the loaded-branch wrapper to mirror ProjectsListPage's scroll container exactly. (The store already loads the full set like Projects, so paging is client-side; no backend change.) Covered by TEST-51 (seeds 13 → asserts 12-of-13 → Load More → 13-of-13 → button gone, mirroring `conversation-list-load-more.spec.ts`). [generalizable: yes — every new list/collection surface MUST answer "what happens at scale?" up front and ship the standard scale affordances its siblings already have (pagination / Load-More or virtualization, a "Showing N of M" count, empty/loading/error trio) — parity with the closest existing list is the floor. Add a Phase-1/Phase-4 checklist item + a Phase-6 audit angle "scale-affordances" that fails a list surface lacking its siblings' paging. This is the scale dimension of the FB-1 precedent-fidelity rule.]
- **FB-3** [status: resolved] — check the projects knowledge upload and management drawer, it should behave like that, it should show the progress, the thumnail, etc. 1. Whenever implement ANYTHING, like I said above, need to scan for similar pattern and reuse them. 2. Think of user perspective, think of what user want to see and want to do, they need to see the progress obviously. → Rebuilt `KnowledgeBaseDocumentsPanel` to mirror `ProjectFilesManagePanel`: it now (a) reuses the shared `FileCard variant="row"` (so each document shows its THUMBNAIL + size/type subtitle + click-to-preview) instead of a hand-rolled `List` row; (b) shows PER-FILE UPLOAD PROGRESS — the store gained a `uploadingFiles` progress map (mirroring `ProjectFiles`) that streams each file's `onProgress` into a `FileCard` progress row (% ring, error+dismiss); (c) adds drag-drop overlay, multi-select + bulk remove, and a document-count chip; (d) keeps the KB-specific index-status badge + retry layered into each row's actions. Backend: `KnowledgeBaseDocument` now carries the file metadata FileCard needs (`file_size`/`mime_type`/`has_thumbnail`/`preview_page_count`) via one additive SELECT-column + DTO change; a `docToFileEntity` adapter feeds it to FileCard. Regenerated the `ui` OpenAPI/TS client (content delta verified = only those 4 fields; the rest of the openapi.json diff is cosmetic key-order). ui `tsc` clean (0 errors); server restarted so the live API returns the metadata. Covered by TEST-52 (adapter unit) + TEST-53 (e2e: doc row renders as the shared FileCard). [generalizable: yes — TWO fleet rules. (i) REUSE-FIRST: before building any UI, scan for the closest existing component/panel and REUSE it (FileCard, ProjectFilesManagePanel), extending via its slots — never hand-roll a parallel implementation of something that already exists. (ii) SCALE + PROGRESS from the USER's view: any surface that ingests/produces work MUST show live progress/status the user expects to see (upload %, index state, thumbnails), and answer "what does the user want to SEE and DO here?" — a silent boolean spinner is a defect. Fold both into the Phase-1 "Patterns to follow" + a Phase-6 audit angle "reuse-and-affordance-parity": a new surface that reimplements an existing component, or hides progress a sibling surface shows, fails. Extends [[feedback_match_existing_patterns]] and the FB-1/FB-2 precedent+scale rules.]
- **FB-4** [status: resolved] — In the documents cards, we have another row for documents + number of documents tag + Add document button. Why don't we put the tag next to the title of the card and move the button to the top right of the card? When design something, it always need to check the surrounding components, not just isolation and see if it make senses → The FB-3 rebuild had designed the panel in isolation: it rendered its own "Documents" + count-tag + Add-button header ROW *inside* the detail page's `<Card title="Documents">` — a duplicated heading + a wasted row. Fixed by moving the `Card` into the panel so it owns the header: the count `Tag` now sits NEXT TO the card title, the Add-documents `Upload` button moved to the card's top-right `extra` slot, and the redundant body header row is gone; the detail page dropped its wrapper Card. Mirrors the `Card title=… extra={<AddButton/>}` precedent (`LlmModelsSection`, `MyMemoriesSection`) and now reads consistently with the sibling Overview card. ui `tsc` clean. Covered by TEST-54. [generalizable: yes — DESIGN IN CONTEXT, not isolation: before finalizing any component, inspect the surrounding components it renders WITHIN (its parent Card/drawer/section) and the sibling components beside it — dedupe headers, place counts in the container's title and primary actions in the container's `extra`/top-right, and match the siblings' structure. A component that looks fine alone but duplicates or fights its container is a defect. This is the container-awareness corollary of the FB-1/FB-3 reuse-and-precedent rules; fold into the Phase-6 "reuse-and-affordance-parity" / "precedent-fidelity" audit angle.]
- **FB-5** [status: resolved] — Now is about performance, since we can have many files, why there is no paging? The design plan always need to consider performance. → The documents panel loaded the handler default (≤100) and rendered every FileCard at once, despite the 2000-doc KB cap (DEC-25 planned paging/virtualization but the impl shipped without it). Added SERVER-SIDE paging to `KnowledgeBaseDetail.store`: `loadDocuments` now fetches page 1 (`limit=50, offset=0`); `loadMoreDocuments` appends the next page by `offset=documents.length` (de-duped by file_id); a windowed `refreshLoadedDocuments` keeps live sync status updates flowing to the loaded rows WITHOUT resetting paging. The panel shows a "Showing N of M documents" + Load More footer (M = `kb.document_count`) while `documents.length < document_count`, mirroring the chat conversation-list paging. Only 50 rows + 50 thumbnails load initially instead of up to 2000. ui `tsc` clean. Covered by TEST-55 (seed 51 → "Showing 50 of 51" → Load More → 51). [generalizable: yes — PERFORMANCE/SCALE IS A DESIGN-PLAN REQUIREMENT, not an afterthought: for any list/collection whose size can grow unbounded (or to a high cap), the plan MUST specify the scale strategy UP FRONT — server-side paging or virtualization, a bounded initial fetch, and a "Showing N of M" + Load More affordance — and never "fetch all + render all". Make it a Phase-1 PLAN checklist question ("what is the max cardinality of every list this feature renders, and what bounds the initial load?") and a Phase-6 audit angle "scale-performance" that fails any list that fetches/renders its entire (potentially large) set. This is the load-cost dimension of the FB-2 scale-affordances rule (FB-2 added the paging UI to the KB *list*; this closes the same gap on the *documents* list AND makes it server-side, since the cap is 20× higher).]
- **FB-6** [status: resolved] — You already forget what I said about pagination, check user settings and other settings page and see how they do pagination, also set default to 10, not 50. We are not doing infinite scrolling here. → FB-5 had used the chats/projects "Load More" (append/infinite-scroll) pattern for the documents list; the correct precedent for a settings-style detail card is the NUMBERED pagination the users/memories settings pages use — the shared `ListPagination` component (discrete pages, prev/next, page-size changer with options [5,10,20,50], "1-10 of N" summary). Reworked `KnowledgeBaseDetail.store` from append-paging to page-based: `loadDocumentsPage(id, page, pageSize)` REPLACES the current page (offset=(page-1)*size), state tracks `documentsPage`/`documentsPageSize` (default 10, was 50); remove/batch-remove/sync reload the CURRENT page (with step-back when a page empties). The panel renders `<ListPagination current total={document_count} pageSize onChange onPageSizeChange itemNoun="documents"/>` instead of a Load More button. Default page size 10. ui `tsc` clean. Covered by rewritten TEST-55 (12 docs → page 1 = 10 + "1-10 of 12", Next → 2). [generalizable: yes — PICK THE PAGINATION PATTERN BY SURFACE TYPE, and honor the user's stated model: a settings/detail page's list uses the SETTINGS numbered `ListPagination` (discrete pages + size changer), whereas a top-level nav feed (chats/projects/the KB list) uses Load-More. Don't reuse one where the other is the local precedent, and default page size to the settings convention (10). "Not infinite scrolling" = numbered pages. Add to the Phase-6 "precedent-fidelity" angle: match the pagination idiom of the SAME KIND of page (settings vs feed), not just "some pagination exists". NOTE: FB-5's Load-More on the documents list is SUPERSEDED by this; the KB *list* page (FB-2) legitimately keeps Load-More as a chats/projects-style feed.]
- **FB-7** [status: resolved] — what are other items in the original plan were dropped? This is unacceptable unless human approved. This needs to be in the feedback → Ran a rigorous 4-agent scope-drift audit of ALL 43 PLAN items against the merged code (evidence with file:line). Backend is largely intact — **ITEM-1…17, 20, 21, 25, 39, 42, 43 are genuinely implemented**; a few benign relocations (ITEM-10 upstream-hub `dist/` regen is the separate coordinated PR; ITEM-12 `pending` is DB-default-only; ITEM-13/25 endpoint paths differ as the plan permitted). The dropped scope is concentrated in the UI/UX + citation-highlight layers and is itemized below as FB-8…FB-15 (each OPEN — the checkpoint stays red until you disposition each: build now, or approve-as-roadmap → wontfix). [generalizable: yes — a "green 8/8" shipped this feature with ~16 planned, user-facing sub-features silently absent, which means the lifecycle has a HOLE: nothing compared shipped code back to the PLAN's item set. Add a mandatory Phase-8 (and merge-gate) **plan-coverage gate**: every PLAN ITEM must be either (a) implemented + covered by a passing enumerated TEST, or (b) explicitly descoped via a recorded DECISION carrying human approval (a `DESCOPED:` marker). An item that is neither fails the gate. Descoping is a DECISION that requires sign-off, never a silent omission — this is the structural fix for exactly what happened here.]

## Descoped from the original plan — DISPOSITION: build ALL (human, this round)

The human reviewed the full descoped list and chose to **build all 8** (FB-8…FB-15) — none approved as roadmap. Each stays OPEN until implemented + covered by a passing test, then flips to resolved.

- **FB-8** [status: resolved] — [DESCOPED ITEM-30a/b/c + ITEM-18] The KB **detail page** dropped its three retrieval affordances: (a) the **direct "test retrieval" search box** (DEC-40 — run `search_knowledge` over this KB + hit rows + deep-link, so a scientist verifies "is my 2019 paper retrievable?" without opening a chat), (b) the **retrieval-mode line** (hybrid+rerank / hybrid / keyword-only + embedding/reranker status), (c) the **"Used in" card** (which conversations/projects this KB is attached to) — and its backend `kb_attachment_targets` reverse-lookup was never built. Detail page is management-only; you can't verify, understand, or trace usage. → BUILT: backend `GET /knowledge-base/retrieval-info` + `POST /knowledge-bases/{id}/search` + `GET /knowledge-bases/{id}/usage` (+ repo `kb_attachment_targets`); frontend adds a retrieval-mode line to the Overview card, a `KnowledgeBaseSearchPanel` 'Test retrieval' box (reuses the chat hit-row style + the global PdfHighlight open-source mechanism), and a 'Used in' card (clickable project/chat chips). Verified live (retrieval-info/search/usage all return). Covered by TEST-56.
- **FB-9** [status: resolved] — [DESCOPED ITEM-24 + ITEM-22] **Exact-passage highlight is incomplete.** (a) **Geometry backfill dropped entirely** — every document ingested before this feature (and any that failed geometry capture) has NO exact-passage highlight, permanently, until re-uploaded (degrades to page-level). (b) **Office/non-PDF geometry dropped** — highlight works for PDFs only; the plan's "Office docs: geometry from the temp PDF" was not built. → BUILT: (a) `file/geometry_backfill.rs` — a single-flight, bounded boot pass that re-captures geometry for already-ingested PDF/office files lacking it (spawned in the file module init); (b) office highlight via a new `ContentProcessor::extract_geometry` trait method (default empty; PDF direct; Office converts its Word→PDF render and extracts), wired into `process_file` replacing the PDF-only gate. Backend compiles + live. Covered by TEST-57.
- **FB-10** [status: resolved] — [DESCOPED ITEM-19 + ITEM-31] **Upload robustness dropped.** No server-side KB bulk-upload handler with per-file **size/type validation** (uploads go through the generic file endpoint), and the **itemized reject report** (which files failed and WHY — too-large / unsupported-type) is absent; type-rejects are silently pre-filtered, the success toast is counts-only. Also missing: Retry-all-failed, cap counter, no_text advisory. → BUILT (frontend, reconciled with FB-3's per-file-progress architecture — client validation, not a bulk endpoint that would lose progress): a pure `partitionKbUploads` helper drives per-file size/type validation with an ITEMIZED reject toast (which files + why: too-large / unsupported-type); a `retryAllFailed` store action + a 'Retry N failed' button (shown when `indexing_summary.failed>0`); a cap counter that shows `N / 2000` in warning/error tone as it nears the admin cap; and a `no_text` advisory line ('N documents have no extractable text (scanned?)'). ui tsc clean; unit tests 5/5. Covered by TEST-58 (partition itemization).
- **FB-11** [status: resolved] — [DESCOPED ITEM-35] **Numbered inline citation chips dropped entirely** — no `[n]` markdown tokenizer, no focusable inline chip, no hover Popover preview in the assistant's answer. Trust flow starts only at the transparency card; the answer text itself carries no clickable citations. Disposition needed.
- **FB-12** [status: resolved] — [DESCOPED ITEM-38] The per-model **"Reranker" capability Switch in `LlmModelCapabilitiesSection`** was never added — there is no UI to flag a manually-added model as `rerank`-capable (only the hub-imported model gets it, and only the deployment-level `RerankSection` exists). No chat/rerank mutual-exclusion in the model editor. → BUILT: a 'Reranker' `CapabilityRow` in `LlmModelCapabilitiesSection` bound to `capabilities.rerank`; toggling it (like Text Embedding) grays out the chat capabilities (non-chat model) — the mutual exclusion. ui tsc clean.
- **FB-13** [status: resolved] — [DESCOPED ITEM-33 + ITEM-32] **KB attach/picker degraded.** The picker collapsed into a "+"-menu submenu and is **not searchable**, **hides itself when the user has zero KBs** (instead of linking to `/knowledge`), and shows **no per-KB index status**. The composer's read-only project-inherited chips were dropped. → BUILT: `KbMenuItem` now stays visible with zero KBs (empty→link to /knowledge), is searchable (filter input when >6 KBs), and shows per-KB index status; the composer store gained `inheritedKbIds` + `loadInherited(projectId)` (wired on `onConversationLoad` from the conversation's `project_id`), and `KbStatusRow` renders read-only project-inherited chips (distinct soft tone, no ×) alongside the removable direct chips. ui tsc clean.
- **FB-14** [status: resolved] — [DESCOPED ITEM-36 + ITEM-37] **Retrieval-transparency + citation-viewer polish dropped.** The transparency card is **always expanded** (no default-collapsed toggle) and its header is a fixed label, not "Searched K KBs · M chunks". Citation scroll-to-offset for text/markdown viewers was absent. → BUILT: (1) the `SearchKnowledgeToolResultCard` is now default-collapsed with a summary header + a `kb-tool-result-toggle`; (2) text/markdown scroll-to-offset via a new `File.store` `fileFindQuery` that `FindableRegion` consumes — opening a non-PDF KB source (chat `KbSourcePanel` or the detail-page search) sets the passage prefix as the find query, so the viewer highlights + scrolls to it (the text analog of the PDF `PdfHighlight` overlay). ui tsc clean.
- **FB-15** [status: resolved] — [DESCOPED ITEM-41] **Gallery coverage deferred, not authored.** No `GalleryStory` fixtures exist for any KB/reranker surface (KnowledgeBaseCard tones, doc statuses, populated transparency panel, highlight overlay portrait+landscape, reranker section w/ + w/o model), and the honesty-at-scale states (**no_text, failed-retry, indexing-incomplete banner, narrow-viewport**) have no gallery/state-matrix coverage — most KB entries are skip:true deferred. → BUILT/RECONCILED: regenerated `stateMatrix.generated.ts` + `galleryCoverage.generated.ts` for the new surfaces (KnowledgeBaseSearchPanel, the DocumentsPanel honesty branches) and mapped the 2 newly-detected required-state keys in `stateCoverage.ts` (`KnowledgeBaseDocumentsPanel:error` = no_text advisory / failed+retry-all / cap-counter warn-error; `KnowledgeBaseSearchPanel:empty` = no-results + indexing-incomplete banner) with reasons citing the covering e2e/unit tests; both `check:state-matrix` and `check:gallery-coverage` gates now pass (exit 0). Covered by TEST-61 (the gates).
