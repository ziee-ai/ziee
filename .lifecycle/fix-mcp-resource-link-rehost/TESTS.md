# TESTS — enumerated up front

Backend-only diff (`src-app/server/**`). No `src-app/ui/**` / `src-app/desktop/ui/**` path is
touched and no new permission is introduced, so **no `tier: e2e` or `[negative-perm]` spec is
required** (frontend gate + A10 do not apply). Every ITEM below maps to ≥1 TEST; ITEM-6 is
`[DESCOPED]` with an approved disposition in DECISIONS.md (exempt from coverage).

- **TEST-1** (tier: unit) [covers: ITEM-1] file: `src-app/server/src/modules/mcp/resource_link.rs` — asserts: `trusted_hosts_from_servers` filters on `is_built_in`: a built-in loopback server `(true, "http://127.0.0.1:41111/…")` is EXCLUDED; an admin-registered system server `(false, "http://host.docker.internal:18122/mcp")` is INCLUDED (the regression this fixes); a user external `(false, "http://172.21.0.1:9004/mcp")` is INCLUDED; a stdio `(false, None)` is skipped; result is sorted+deduped and `127.0.0.1` is absent.
- **TEST-2** (tier: unit) [covers: ITEM-3] file: `src-app/server/src/modules/mcp/resource_link.rs` — asserts: `choose_fetch_policy("http://host.docker.internal:18123/x", &["host.docker.internal"], false, false)` returns `PrivateScoped` (host-match selects the private-allowing policy); the same host with an empty trusted set returns `Public`.
- **TEST-3** (tier: integration) [covers: ITEM-1, ITEM-2, ITEM-3] file: `src-app/server/tests/mcp/resource_link_test.rs` — asserts: register a SYSTEM server (`is_system=true, is_built_in=false`) whose `url` host is a loopback mock (standing in for the private artifact host), assign it to the user's group, derive `trusted_hosts` via the new `list_accessible_result_link_hosts` accessor (proving the redacted URL's host survives), then `persist_links` over an external `resource_link` on that host yields `outcome.saved.len()==1` with a stamped `file_id`. Negative control in the same test: an external link on an UNREGISTERED private host is rejected (not saved).
- **TEST-4** (tier: integration) [covers: ITEM-2, ITEM-3] file: `src-app/server/tests/mcp/resource_link_test.rs` — asserts: `list_accessible_result_link_hosts(user_id)` returns the admin-registered system server's host (redaction bypassed) AND omits it once the row is flipped to `is_built_in=true` (built-in exclusion holds through the accessor). Also exercises the SHARED call-site glue `result_link_trusted_hosts(emitter_is_built_in, user_id)` used by all 3 `persist_links` sites: an external emitter → the registered system host; a built-in emitter → empty set.
- **TEST-5** (tier: unit) [covers: ITEM-4] file: `src-app/server/src/modules/mcp/chat_extension/mcp.rs` — asserts: `tool_system_guidance` (with a `get_resource_link` tool present) and `saved_artifact_hidden_content_guidance` now instruct the model to use the ziee-provided URL for a file another tool handed it and to never fetch/forward the raw MCP host URL nor rewrite/substitute the host; existing asserted substrings (`prefer using these tools`, `SHORT-LIVED`, `VERBATIM`, `never reuse a URL from an earlier turn`, `127.0.0.1/localhost`, `DRS`) still pass.
- **TEST-6** (tier: unit) [covers: ITEM-5] file: `src-app/server/src/modules/code_sandbox/handlers.rs` — asserts: the `get_resource_link` tool description contains the new bridge-case clause (a tool handed you a file as a URL you pulled into the sandbox → call `get_resource_link` on the local filename), while the existing asserted substrings (`SHORT-LIVED`, `get_resource_link AGAIN`, `VERBATIM`, `DRS`, `localhost`) still pass and the negative guard (does NOT contain `already reachable exactly as given`) stays satisfied.
