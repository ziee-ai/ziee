# Design fidelity — plan vs DESIGN.md invariants

- **INV-1** — fidelity: UPHELD — ITEM-2/ITEM-3/ITEM-4 rewrite every refusal `spawn_background` can emit so each names the argument, what is expected, and a copyable literal-JSON example, held to the same three-element standard `common/tool_args.rs::conformance::assert_actionable` already enforces for shape refusals. ITEM-6/ITEM-7's flavor refusals list the valid flavors, which is the enum form of "what is expected".
- **INV-2** — fidelity: UPHELD — ITEM-1 makes a `kind` supplied inside `spec` resolve the kind instead of being dropped in favour of `unwrap_or("subagent")`, which is the exact "supplied argument silently replaced by the default" the invariant forbids. The contradictory-pair case is refused rather than resolved by preference, so no supplied value is ever discarded silently. ITEM-9 removes the consumed key rather than leaving a shadow copy that a later reader could resolve differently.
- **INV-3** — fidelity: UPHELD — ITEM-5 gives the enum ONE canonical check; ITEM-6 applies it before the `workflow_runs` row exists and ITEM-7 before `execute_command_stream`, both strictly upstream of `install_version`'s `format!("ziee-sandbox-rootfs-{arch}-{flavor}.{ext}")`. The design's scope boundary (the admin install path keeps its safe-token check so a future flavor stays installable) is honoured: no check is added inside `install_version`.
