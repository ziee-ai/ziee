commit 29325bd9cdd247fd40a33cf9034cdc726e00b437
Author: pbya <pbya@tinnguyen-lab.com>
Date:   Sat Jul 18 10:56:50 2026 -0400

    refactor(mcp): move shared MCP tool-call chokepoint to mcp/agent_tool_call + de-dup adapters
    
    Fixes two HIGH shared-code findings from the blind audit:
    
    (1) MODULARITY / §9 DAG: chat's agent host imported its MCP-call chokepoint
        (call_mcp_tool / resolve_tool_server / McpCallScope / McpToolCallError /
        CancelSignal / ChatCallCtx) from workflow::dispatch — a lateral chat<-workflow
        coupling for what is SHARED MCP infra. Moved the whole chokepoint + the
        built-in NAME->id map into a new shared module mcp/agent_tool_call.rs. Both the
        workflow dispatcher AND the chat agent host now import it from mcp/, not from
        each other. Only 'impl CancelSignal for registry::RunHandle' stays in workflow
        (orphan rule: workflow owns RunHandle). workflow::dispatch re-exports for its
        own internal callers.
    
    (2) MAINTAINABILITY + LATENT BUG: split_tool_name + mcp_to_agent_result were
        copy-pasted in chat resolver.rs and workflow agent_dispatch.rs and had DIVERGED
        — chat computed 'terminal' from the audience:['user'] annotation, workflow
        hardcoded terminal:false. De-duplicated into ONE shared pair in
        mcp/agent_tool_call.rs and reconciled onto the correct audience-computed value.
        workflow's terminal:false was a latent bug: the workflow agent-step ignored the
        audience-terminal signal (a user-audience tool result should end the turn).
    
    flag-OFF unchanged (chat path uses execute_tool, not call_mcp_tool; the move is
    behavior-preserving; workflow's terminal fix affects both flags equally). Lib +
    tests compile; lib unit tests 10/10.

 .lifecycle/agent-core/LEDGER.jsonl                 |  88 ++---
 .../src/modules/chat/agent_host/dispatcher.rs      |   2 +-
 src-app/server/src/modules/chat/agent_host/gate.rs |   2 +-
 .../server/src/modules/chat/agent_host/resolver.rs |  62 +--
 src-app/server/src/modules/mcp/agent_tool_call.rs  | 440 +++++++++++++++++++++
 src-app/server/src/modules/mcp/mod.rs              |   1 +
 .../server/src/modules/workflow/agent_dispatch.rs  |  50 +--
 src-app/server/src/modules/workflow/dispatch.rs    | 375 +-----------------
 8 files changed, 518 insertions(+), 502 deletions(-)

commit 29325bd9cdd247fd40a33cf9034cdc726e00b437
Author: pbya <pbya@tinnguyen-lab.com>
Date:   Sat Jul 18 10:56:50 2026 -0400

    refactor(mcp): move shared MCP tool-call chokepoint to mcp/agent_tool_call + de-dup adapters
    
    Fixes two HIGH shared-code findings from the blind audit:
    
    (1) MODULARITY / §9 DAG: chat's agent host imported its MCP-call chokepoint
        (call_mcp_tool / resolve_tool_server / McpCallScope / McpToolCallError /
        CancelSignal / ChatCallCtx) from workflow::dispatch — a lateral chat<-workflow
        coupling for what is SHARED MCP infra. Moved the whole chokepoint + the
        built-in NAME->id map into a new shared module mcp/agent_tool_call.rs. Both the
        workflow dispatcher AND the chat agent host now import it from mcp/, not from
        each other. Only 'impl CancelSignal for registry::RunHandle' stays in workflow
        (orphan rule: workflow owns RunHandle). workflow::dispatch re-exports for its
        own internal callers.
    
    (2) MAINTAINABILITY + LATENT BUG: split_tool_name + mcp_to_agent_result were
        copy-pasted in chat resolver.rs and workflow agent_dispatch.rs and had DIVERGED
        — chat computed 'terminal' from the audience:['user'] annotation, workflow
        hardcoded terminal:false. De-duplicated into ONE shared pair in
        mcp/agent_tool_call.rs and reconciled onto the correct audience-computed value.
        workflow's terminal:false was a latent bug: the workflow agent-step ignored the
        audience-terminal signal (a user-audience tool result should end the turn).
    
    flag-OFF unchanged (chat path uses execute_tool, not call_mcp_tool; the move is
    behavior-preserving; workflow's terminal fix affects both flags equally). Lib +
    tests compile; lib unit tests 10/10.

diff --git a/.lifecycle/agent-core/LEDGER.jsonl b/.lifecycle/agent-core/LEDGER.jsonl
index fe5cda5b4..bf25e90e1 100644
--- a/.lifecycle/agent-core/LEDGER.jsonl
+++ b/.lifecycle/agent-core/LEDGER.jsonl
@@ -1,44 +1,44 @@
-{"angle":"security","file":"src-app/server/src/modules/workflow/dispatch.rs","line":1157,"severity":"high","finding":"call_mcp_tool uuid-branch used the model-supplied server_id directly, skipping the accessible-set check the legacy path enforced — a prompt-injected tool name could execute on a server the user's groups aren't assigned to, using that server's admin secret headers (cross-group authz bypass)","status":"fixed"}
-{"angle":"correctness","file":"src-app/agent-core/src/core.rs","line":530,"severity":"high","finding":"a tool execution error propagated via ? and aborted the whole turn, leaving the persisted tool_use with no tool_result → next-turn history is an orphan tool_use the provider rejects","status":"fixed"}
-{"angle":"correctness","file":"src-app/agent-core/src/core.rs","line":331,"severity":"high","finding":"cross-request approval resume double-executed: the RegistryBridge runs MCP before_llm_call which itself executes approved tools on resume, while the loop's native resume-execution (last_pending_assistant) also executed them from stale history → tool ran twice then re-prompted forever","status":"fixed"}
-{"angle":"correctness","file":"src-app/agent-core/src/core.rs","line":514,"severity":"high","finding":"a partial suspend (mixed auto+prompt tools in one round) broke on the first suspend, stranding later tools with neither a result nor a pending row → orphaned tool_use + lost approval on resume","status":"fixed"}
-{"angle":"perms","file":"src-app/server/src/modules/chat/agent_host/gate.rs","line":323,"severity":"medium","finding":"the denial check failed OPEN — a transient DB error on get_denied_tools_for_branch fell through to decide_pure, so in AutoApprove mode a denied tool could auto-execute","status":"fixed"}
-{"angle":"concurrency","file":"src-app/server/src/modules/chat/agent_host/gate.rs","line":312,"severity":"medium","finding":"the single-use approval claim discarded delete_tool_approval's bool — a concurrent resume could read the same approved row and both return Auto (double-execute a mutating tool)","status":"fixed"}
-{"angle":"security","file":"src-app/server/src/modules/chat/agent_host/resolver.rs","line":378,"severity":"medium","finding":"is_trusted name-fallback used builtin_server_id_by_name (which includes code_sandbox/control_mcp) instead of is_builtin_server_id — a name-prefixed code_sandbox tool would auto-approve code execution (latent; name path unreachable today)","status":"fixed"}
-{"angle":"resource-lifecycle","file":"src-app/server/src/modules/chat/agent_host/dispatcher.rs","line":382,"severity":"medium","finding":"the spawned generation task releases the slot + terminal frame + cancel token explicitly at the tail with no RAII/Drop guard — a panic in the task permanently latches begin_generation ('reply already generating') + leaks the cancel-token entry + hangs the SSE stream","status":"confirmed"}
-{"angle":"error-handling","file":"src-app/server/src/modules/chat/agent_host/event_sink.rs","line":118,"severity":"low","finding":"new finish_reason wire strings for cap stops (max_steps/token_cap/timeout) are outside the legacy vocabulary (stop/cancelled) a client may switch on — cosmetic, chat caps effectively never fire","status":"confirmed"}
-{"angle":"modularity","file":"src-app/server/src/modules/chat/agent_host/resolver.rs","line":52,"severity":"high","finding":"chat imports its MCP-call chokepoint (call_mcp_tool/resolve_tool_server/McpCallScope/CancelSignal) from workflow::dispatch — two sibling feature modules coupled laterally; call_mcp_tool is shared MCP infra that belongs in mcp/, not workflow","status":"confirmed"}
-{"angle":"maintainability","file":"src-app/server/src/modules/chat/agent_host/resolver.rs","line":143,"severity":"high","finding":"split_tool_name + mcp_to_agent_result are copy-pasted between chat resolver.rs and workflow agent_dispatch.rs and have already diverged (chat computes terminal from audience; workflow hardcodes terminal:false)","status":"confirmed"}
-{"angle":"maintainability","file":"src-app/server/src/modules/chat/agent_host/gate.rs","line":82,"severity":"medium","finding":"namespaced-tool __-splitting is re-implemented in 4 places (resolver split_tool_name, gate split_server_tool, workflow split_tool_name, call_mcp_tool uuid-parse) with no single owner for the <server_id>__<tool> convention","status":"confirmed"}
-{"angle":"api","file":"src-app/server/src/modules/workflow/dispatch.rs","line":1134,"severity":"medium","finding":"call_mcp_tool takes 9 positional params incl a bare enforce_conversation_disabled:bool (a security gate) + two Option<String> — boolean/Option soup, trivial to transpose or flip the gate silently","status":"confirmed"}
-{"angle":"api","file":"src-app/server/src/modules/chat/agent_host/dispatcher.rs","line":58,"severity":"medium","finding":"ChatAgentTurn is a 16-public-field struct with no constructor; provider/model_name/model_id/provider_id must stay mutually consistent (one create_provider_from_model_id call) but nothing enforces it","status":"confirmed"}
-{"angle":"correctness","file":"src-app/agent-core/src/event_sink_stopreason","line":515,"severity":"medium","finding":"a gate-suspended turn emits Stopped(Halted) which the sink maps to finish_reason 'cancelled', conflating user-stop with awaiting-approval — StopReason has no Suspended variant","status":"confirmed"}
-{"angle":"simplification","file":"src-app/agent-core/src/extension.rs","line":61,"severity":"medium","finding":"AgentExtension::on_delta is never called by the loop (streaming drives only DeltaSink); dead trait method that misleads the next porter","status":"confirmed"}
-{"angle":"simplification","file":"src-app/agent-core/src/budget.rs","line":44,"severity":"medium","finding":"Budget per_step_token_cap / step_over_cap are never consulted by the loop (only unit-tested) — dead accounting carried through every Budget::new third arg","status":"confirmed"}
-{"angle":"dead-code","file":"src-app/agent-core/src/types.rs","line":187,"severity":"low","finding":"AgentTurnRequest.inputs / TurnContext.inputs (DEC-19) is threaded end-to-end but consumed by no host (both pass Null; chat carries its request via RegistryBridge.send_request) — over-engineered seam with zero current consumer","status":"confirmed"}
-{"angle":"dead-code","file":"src-app/agent-core/src/types.rs","line":201,"severity":"low","finding":"AgentEvent::ToolNotification is handled by the sink but never emitted by the loop (ToolProvider has no sink access) — dead variant + dead sink branch","status":"confirmed"}
-{"angle":"security","file":"src-app/server/src/modules/workflow/agent_dispatch.rs","line":347,"severity":"high","finding":"workflow agent host is_trusted uses builtin_server_id_by_name (includes code_sandbox/control) -> a kind:agent step's code_sandbox call is auto-approved (Trusted->Auto), bypassing reviewer/human gate; chat twin correctly uses is_builtin_server_id which excludes them","status":"confirmed"}
-{"angle":"correctness","file":"src-app/server/src/modules/chat/agent_host/gate.rs","line":112,"severity":"med","finding":"resolve_bare_tool_server returns the FIRST accessible server advertising the bare tool name; legacy recover_server_id_for_bare_name returns None for a name advertised by >=2 servers (never guesses) -> a bare-name approval can persist the wrong server_id and resume against the wrong server","status":"confirmed"}
-{"angle":"tests-reality","file":"src-app/server/tests/agent/model_resolver_test.rs","line":230,"severity":"med","finding":"TEST-41 claims to cover the new ChatModelResolver/WorkflowModelResolver RBAC but never sets ZIEE_CHAT_AGENT_CORE=1, so it runs the LEGACY path -> the new resolver denial code is untested","status":"confirmed"}
-{"angle":"tests-reality","file":"src-app/server/tests/mcp/agent_core_multiturn_bridge_test.rs","line":1110,"severity":"med","finding":"multiturn test asserts history.contains('purple-turtle-42') which turn-1's tool_result already wrote -> the assertion passes even if turn-2 context recall is completely broken (does not verify its headline claim)","status":"confirmed"}
-{"angle":"tests-quality","file":"src-app/server/tests/chat/agent_core_parity_test.rs","line":770,"severity":"med","finding":"process-global unsafe set_var('ZIEE_CHAT_AGENT_CORE','1') with remove_var only at the end -> a panic skips teardown leaving the flag ON for subsequent tests in the same binary; --test-threads=6 default flips concurrent chat tests onto the agent-core path (cross-test contamination)","status":"confirmed"}
-{"angle":"api-friendliness","file":"src-app/server/src/modules/workflow/dispatch.rs","line":1148,"severity":"med","finding":"call_mcp_tool has 10 positional params incl two adjacent Option<String> (review_classification, idempotency_key) swappable with no compile error -> silent journaling corruption","status":"confirmed"}
-{"angle":"api-friendliness","file":"src-app/server/src/modules/chat/agent_host/resolver.rs","line":332,"severity":"low","finding":"chat passes Some(idem) as idempotency_key but call_mcp_tool applies it only when source==Workflow -> chat idempotency key is silently dropped (misleading no-op)","status":"confirmed"}
-{"angle":"maintainability","file":"src-app/server/src/modules/chat/agent_host/mod.rs","line":5,"severity":"med","finding":"mod.rs 'integration contract' says chat tools flow via ChatToolProvider::list from tool_scope server NAMEs; actually RegistryBridge before_llm_call does request.tools=all_tools (overwrite) after the loop already called list -> ChatToolProvider::list opens a session+list_tools RPC per server per iteration and discards it; doc contract is wrong (uuid scheme, not names)","status":"confirmed"}
-{"angle":"modularity","file":"src-app/server/src/modules/workflow/dispatch.rs","line":1134,"severity":"med","finding":"shared call_mcp_tool + chat-only ChatCallCtx live in the workflow module; chat imports crate::modules::workflow::dispatch -> DAG inversion (chat depends on workflow for its core MCP path). PRE-EXISTING coupling (call_mcp_tool was already shared here); refactor added ChatCallCtx","status":"confirmed"}
-{"angle":"state-management","file":"src-app/agent-core/src/core.rs","line":414,"severity":"low","finding":"mid-stream cancellation drops the partially-accumulated assistant message without transcript.append -> reload shows an empty assistant turn (streamed-vs-persisted divergence for a stopped generation)","status":"confirmed"}
-{"angle":"perf","file":"src-app/server/src/modules/chat/agent_host/gate.rs","line":357,"severity":"low","finding":"ChatApprovalPolicy::decide runs 2 DB queries (approved+denied) per tool-call decision per step even with no pending approvals; legacy classification was in-memory","status":"confirmed"}
-{"angle":"concurrency","file":"src-app/agent-core/src/fanout.rs","line":83,"severity":"low","finding":"fan_out returns Err on first child error while remaining spawned children run detached (JoinHandle drop != abort); CancelToken not fired -> wasted model calls. NOTE: fanout is DESCOPED (no production caller) so not a live path","status":"confirmed"}
-{"angle":"security","file":"src-app/server/src/modules/workflow/dispatch.rs","line":2264,"severity":"low","finding":"broadened is_built_in accessibility relies on 'per-tool authz enforced downstream for every built-in' but no test asserts a permission-lacking user is denied via the agent path (unverified invariant, no concrete escalation found)","status":"confirmed"}
-{"angle":"tests-reality","file":"src-app/server/tests/agent/verification_test.rs","line":733,"severity":"low","finding":"dump.contains('not_found') is near-trivially true (not_found is in the citations tool schema); the real anchor is the count==0 bibliography check","status":"rejected-minor"}
-{"angle":"api-contract","file":"src-app/agent-core/src/ports.rs","line":1,"severity":"none","finding":"ports.rs 6 traits are domain-neutral (Uuid/ai_providers/AppError only), no host/chat/DB leak; a new consumer can implement without editing the crate","status":"rejected-clean"}
-{"angle":"correctness","file":"src-app/server/src/modules/workflow/agent_dispatch.rs","line":355,"severity":"low","finding":"is_trusted false-denies read-only built-ins (elicitation/knowledge_base/skill/tool_result) not in builtin_server_id_by_name (only 7 of 10 mapped) -> a workflow agent step using them routes through review instead of auto-bypass. PRE-EXISTING (before the Phase-7 fix too: None.is_some()=false); conservative-safe (more approvals, not a security hole); my fix only correctly changed code_sandbox","status":"rejected-preexisting-conservative"}
-{"angle":"maintainability","file":"src-app/server/src/modules/chat/agent_host/resolver.rs","line":294,"severity":"low","finding":"fn-level #[allow(unused_variables)] over-suppresses (only idem is unused) -> a future dead local in call() would be silenced","status":"confirmed"}
-{"angle":"api-friendliness","file":"src-app/server/tests/common/mod.rs","line":15,"severity":"low","finding":"AgentCoreFlag #[must_use] does not fire on `let _ = AgentCoreFlag::on()` (drops immediately, flag off during test body); all 6 callers use a named binding so latent not live","status":"accepted-latent"}
-{"angle":"tests-reality","file":"src-app/server/tests/path_resolution.rs","line":153,"severity":"medium","finding":"PRE-EXISTING compile error: `config.postgresql.embedded.expect()` moves a non-Copy field out of a borrow -> the whole path_resolution test binary failed to build (0 tests ran). Fixed with `.as_ref()`. Real bug (deterministic, not model-flaky). red: build error -> green: 6 passed","status":"fixed-preexisting"}
-{"angle":"tests-reality","file":"src-app/server/tests/memory_mcp/mod.rs","line":252,"severity":"medium","finding":"PRE-EXISTING deterministic OFF failure: test_recall_requires_memory_enabled assumed memory is disabled-by-default, but the schema seeds enabled=true (202607140185_memory_schema.sql:17 DEFAULT true + 202607145050_memory_seed.sql:4). Recall's MEMORY_DISABLED guard (handlers.rs:415) is CORRECT; the test never actually disabled memory so recall returned {memories:[]}. Fixed: test now sets enabled=false via the DB then asserts the guard fires. red: FAILED -> green: ok","status":"fixed-preexisting"}
-{"angle":"tests-reality","file":"src-app/server/tests/chat/test_single_assistant_message_architecture.rs","line":357,"severity":"medium","finding":"PRE-EXISTING deterministic OFF failure: helper parsed GET /conversations/{id}/messages as Vec<Value>, but the endpoint returns PaginatedMessages object {messages,has_more_before,has_more_after} (routes.rs:37->get_conversation_history -> Json<PaginatedMessages>). Stale test. Fixed to extract .messages. red: 'invalid type: map, expected a sequence' -> green: ok","status":"fixed-preexisting"}
-{"angle":"tests-reality","file":"src-app/server/tests/mcp/mcp_streaming_workflow_test.rs","line":746,"severity":"medium","finding":"PRE-EXISTING deterministic OFF failure: `body.as_array()` with stale comment 'API returns array directly' on the same PaginatedMessages endpoint. Fixed to extract .messages. red: FAILED -> green: ok","status":"fixed-preexisting"}
-{"angle":"tests-reality","file":"src-app/server/tests/mcp/elicitation_mcp_test.rs","line":236,"severity":"low","finding":"PRE-EXISTING deterministic OFF failure: BUILTIN_SUFFIXES allow-list missing __run_js (js_tool auto-attaches for tool-capable chats), so the 'only built-in tools attach' assertion failed on the run_js tool. Product behavior correct; stale allow-list. Fixed: added __run_js. red: FAILED -> green: ok","status":"fixed-preexisting"}
-{"angle":"correctness","file":"src-app/server/src/modules/file/available_files.rs","line":292,"severity":"medium","finding":"PRE-EXISTING (on main, NOT agent-core) deterministic cluster: 8 agentic_chat tests fail because attach_files_mcp is gated on manifest_available = !files.is_empty() (available_files.rs:292 -> file.rs:117), so files_mcp write tools (create_file/edit_file) never attach in an EMPTY conversation -> the model cannot author the first file (chicken-and-egg); combined memory+files tests hit the same. Also the StubChat harness has no create_file plan arm. Root-caused but NOT fixed here: the fix (decouple files_mcp tool-attach from file-presence) is a core-chat product-behavior change with wide blast radius (changes the attached-tool set for EVERY conversation, breaks other committed assertions e.g. the elicitation empty-config test) and a genuine product decision, outside the agent-core diff. SURFACED for a scoping decision (see PREEXISTING_BUGS.md).","status":"reported-needs-scoping"}
-{"angle":"tests-reality","file":"src-app/server/tests/mcp/tool_call_history_test.rs","line":528,"severity":"info","finding":"NOT a code bug: chat_path_tool_call_records_source_chat fails at the `tool_starts > 0` GUARD (the test itself documents this distinguishes 'an LLM/setup miss' from a recording bug). The weak local Qwen bridge did not emit the tool call. MODEL-FLAKY, classified not-a-bug.","status":"classified-model-flaky"}
+{"angle": "security", "file": "src-app/server/src/modules/workflow/dispatch.rs", "line": 1157, "severity": "high", "finding": "call_mcp_tool uuid-branch used the model-supplied server_id directly, skipping the accessible-set check the legacy path enforced \u2014 a prompt-injected tool name could execute on a server the user's groups aren't assigned to, using that server's admin secret headers (cross-group authz bypass)", "status": "fixed"}
+{"angle": "correctness", "file": "src-app/agent-core/src/core.rs", "line": 530, "severity": "high", "finding": "a tool execution error propagated via ? and aborted the whole turn, leaving the persisted tool_use with no tool_result \u2192 next-turn history is an orphan tool_use the provider rejects", "status": "fixed"}
+{"angle": "correctness", "file": "src-app/agent-core/src/core.rs", "line": 331, "severity": "high", "finding": "cross-request approval resume double-executed: the RegistryBridge runs MCP before_llm_call which itself executes approved tools on resume, while the loop's native resume-execution (last_pending_assistant) also executed them from stale history \u2192 tool ran twice then re-prompted forever", "status": "fixed"}
+{"angle": "correctness", "file": "src-app/agent-core/src/core.rs", "line": 514, "severity": "high", "finding": "a partial suspend (mixed auto+prompt tools in one round) broke on the first suspend, stranding later tools with neither a result nor a pending row \u2192 orphaned tool_use + lost approval on resume", "status": "fixed"}
+{"angle": "perms", "file": "src-app/server/src/modules/chat/agent_host/gate.rs", "line": 323, "severity": "medium", "finding": "the denial check failed OPEN \u2014 a transient DB error on get_denied_tools_for_branch fell through to decide_pure, so in AutoApprove mode a denied tool could auto-execute", "status": "fixed"}
+{"angle": "concurrency", "file": "src-app/server/src/modules/chat/agent_host/gate.rs", "line": 312, "severity": "medium", "finding": "the single-use approval claim discarded delete_tool_approval's bool \u2014 a concurrent resume could read the same approved row and both return Auto (double-execute a mutating tool)", "status": "fixed"}
+{"angle": "security", "file": "src-app/server/src/modules/chat/agent_host/resolver.rs", "line": 378, "severity": "medium", "finding": "is_trusted name-fallback used builtin_server_id_by_name (which includes code_sandbox/control_mcp) instead of is_builtin_server_id \u2014 a name-prefixed code_sandbox tool would auto-approve code execution (latent; name path unreachable today)", "status": "fixed"}
+{"angle": "resource-lifecycle", "file": "src-app/server/src/modules/chat/agent_host/dispatcher.rs", "line": 382, "severity": "medium", "finding": "the spawned generation task releases the slot + terminal frame + cancel token explicitly at the tail with no RAII/Drop guard \u2014 a panic in the task permanently latches begin_generation ('reply already generating') + leaks the cancel-token entry + hangs the SSE stream", "status": "confirmed"}
+{"angle": "error-handling", "file": "src-app/server/src/modules/chat/agent_host/event_sink.rs", "line": 118, "severity": "low", "finding": "new finish_reason wire strings for cap stops (max_steps/token_cap/timeout) are outside the legacy vocabulary (stop/cancelled) a client may switch on \u2014 cosmetic, chat caps effectively never fire", "status": "confirmed"}
+{"angle": "modularity", "file": "src-app/server/src/modules/chat/agent_host/resolver.rs", "line": 52, "severity": "high", "finding": "chat imports its MCP-call chokepoint (call_mcp_tool/resolve_tool_server/McpCallScope/CancelSignal) from workflow::dispatch \u2014 two sibling feature modules coupled laterally; call_mcp_tool is shared MCP infra that belongs in mcp/, not workflow | FIXED: moved the chokepoint (call_mcp_tool + resolve_tool_server + builtin name map + McpCallScope/McpToolCallError/CancelSignal/ChatCallCtx) to a new shared mcp/agent_tool_call.rs; chat (resolver/gate/dispatcher) + workflow both import from mcp/; only impl CancelSignal for RunHandle stays in workflow (owns the type). \u00a79 DAG satisfied.", "status": "fixed"}
+{"angle": "maintainability", "file": "src-app/server/src/modules/chat/agent_host/resolver.rs", "line": 143, "severity": "high", "finding": "split_tool_name + mcp_to_agent_result are copy-pasted between chat resolver.rs and workflow agent_dispatch.rs and have already diverged (chat computes terminal from audience; workflow hardcodes terminal:false) | FIXED: de-duplicated into mcp/agent_tool_call.rs (single split_tool_name + mcp_to_agent_result). DIVERGENCE RECONCILED: unified on chat's audience-computed `terminal` (workflow's hardcoded terminal:false was a LATENT BUG \u2014 it ignored the audience:['user'] terminal signal on the workflow agent-step path). Real bug fix.", "status": "fixed"}
+{"angle": "maintainability", "file": "src-app/server/src/modules/chat/agent_host/gate.rs", "line": 82, "severity": "medium", "finding": "namespaced-tool __-splitting is re-implemented in 4 places (resolver split_tool_name, gate split_server_tool, workflow split_tool_name, call_mcp_tool uuid-parse) with no single owner for the <server_id>__<tool> convention", "status": "confirmed"}
+{"angle": "api", "file": "src-app/server/src/modules/workflow/dispatch.rs", "line": 1134, "severity": "medium", "finding": "call_mcp_tool takes 9 positional params incl a bare enforce_conversation_disabled:bool (a security gate) + two Option<String> \u2014 boolean/Option soup, trivial to transpose or flip the gate silently", "status": "confirmed"}
+{"angle": "api", "file": "src-app/server/src/modules/chat/agent_host/dispatcher.rs", "line": 58, "severity": "medium", "finding": "ChatAgentTurn is a 16-public-field struct with no constructor; provider/model_name/model_id/provider_id must stay mutually consistent (one create_provider_from_model_id call) but nothing enforces it", "status": "confirmed"}
+{"angle": "correctness", "file": "src-app/agent-core/src/event_sink_stopreason", "line": 515, "severity": "medium", "finding": "a gate-suspended turn emits Stopped(Halted) which the sink maps to finish_reason 'cancelled', conflating user-stop with awaiting-approval \u2014 StopReason has no Suspended variant", "status": "confirmed"}
+{"angle": "simplification", "file": "src-app/agent-core/src/extension.rs", "line": 61, "severity": "medium", "finding": "AgentExtension::on_delta is never called by the loop (streaming drives only DeltaSink); dead trait method that misleads the next porter", "status": "confirmed"}
+{"angle": "simplification", "file": "src-app/agent-core/src/budget.rs", "line": 44, "severity": "medium", "finding": "Budget per_step_token_cap / step_over_cap are never consulted by the loop (only unit-tested) \u2014 dead accounting carried through every Budget::new third arg", "status": "confirmed"}
+{"angle": "dead-code", "file": "src-app/agent-core/src/types.rs", "line": 187, "severity": "low", "finding": "AgentTurnRequest.inputs / TurnContext.inputs (DEC-19) is threaded end-to-end but consumed by no host (both pass Null; chat carries its request via RegistryBridge.send_request) \u2014 over-engineered seam with zero current consumer", "status": "confirmed"}
+{"angle": "dead-code", "file": "src-app/agent-core/src/types.rs", "line": 201, "severity": "low", "finding": "AgentEvent::ToolNotification is handled by the sink but never emitted by the loop (ToolProvider has no sink access) \u2014 dead variant + dead sink branch", "status": "confirmed"}
+{"angle": "security", "file": "src-app/server/src/modules/workflow/agent_dispatch.rs", "line": 347, "severity": "high", "finding": "workflow agent host is_trusted uses builtin_server_id_by_name (includes code_sandbox/control) -> a kind:agent step's code_sandbox call is auto-approved (Trusted->Auto), bypassing reviewer/human gate; chat twin correctly uses is_builtin_server_id which excludes them", "status": "confirmed"}
+{"angle": "correctness", "file": "src-app/server/src/modules/chat/agent_host/gate.rs", "line": 112, "severity": "med", "finding": "resolve_bare_tool_server returns the FIRST accessible server advertising the bare tool name; legacy recover_server_id_for_bare_name returns None for a name advertised by >=2 servers (never guesses) -> a bare-name approval can persist the wrong server_id and resume against the wrong server", "status": "confirmed"}
+{"angle": "tests-reality", "file": "src-app/server/tests/agent/model_resolver_test.rs", "line": 230, "severity": "med", "finding": "TEST-41 claims to cover the new ChatModelResolver/WorkflowModelResolver RBAC but never sets ZIEE_CHAT_AGENT_CORE=1, so it runs the LEGACY path -> the new resolver denial code is untested", "status": "confirmed"}
+{"angle": "tests-reality", "file": "src-app/server/tests/mcp/agent_core_multiturn_bridge_test.rs", "line": 1110, "severity": "med", "finding": "multiturn test asserts history.contains('purple-turtle-42') which turn-1's tool_result already wrote -> the assertion passes even if turn-2 context recall is completely broken (does not verify its headline claim)", "status": "confirmed"}
+{"angle": "tests-quality", "file": "src-app/server/tests/chat/agent_core_parity_test.rs", "line": 770, "severity": "med", "finding": "process-global unsafe set_var('ZIEE_CHAT_AGENT_CORE','1') with remove_var only at the end -> a panic skips teardown leaving the flag ON for subsequent tests in the same binary; --test-threads=6 default flips concurrent chat tests onto the agent-core path (cross-test contamination)", "status": "confirmed"}
+{"angle": "api-friendliness", "file": "src-app/server/src/modules/workflow/dispatch.rs", "line": 1148, "severity": "med", "finding": "call_mcp_tool has 10 positional params incl two adjacent Option<String> (review_classification, idempotency_key) swappable with no compile error -> silent journaling corruption", "status": "confirmed"}
+{"angle": "api-friendliness", "file": "src-app/server/src/modules/chat/agent_host/resolver.rs", "line": 332, "severity": "low", "finding": "chat passes Some(idem) as idempotency_key but call_mcp_tool applies it only when source==Workflow -> chat idempotency key is silently dropped (misleading no-op)", "status": "confirmed"}
+{"angle": "maintainability", "file": "src-app/server/src/modules/chat/agent_host/mod.rs", "line": 5, "severity": "med", "finding": "mod.rs 'integration contract' says chat tools flow via ChatToolProvider::list from tool_scope server NAMEs; actually RegistryBridge before_llm_call does request.tools=all_tools (overwrite) after the loop already called list -> ChatToolProvider::list opens a session+list_tools RPC per server per iteration and discards it; doc contract is wrong (uuid scheme, not names)", "status": "confirmed"}
+{"angle": "modularity", "file": "src-app/server/src/modules/workflow/dispatch.rs", "line": 1134, "severity": "med", "finding": "shared call_mcp_tool + chat-only ChatCallCtx live in the workflow module; chat imports crate::modules::workflow::dispatch -> DAG inversion (chat depends on workflow for its core MCP path). PRE-EXISTING coupling (call_mcp_tool was already shared here); refactor added ChatCallCtx | FIXED: chokepoint + ChatCallCtx moved to mcp/agent_tool_call.rs; workflow re-exports for its own internal callers.", "status": "fixed"}
+{"angle": "state-management", "file": "src-app/agent-core/src/core.rs", "line": 414, "severity": "low", "finding": "mid-stream cancellation drops the partially-accumulated assistant message without transcript.append -> reload shows an empty assistant turn (streamed-vs-persisted divergence for a stopped generation)", "status": "confirmed"}
+{"angle": "perf", "file": "src-app/server/src/modules/chat/agent_host/gate.rs", "line": 357, "severity": "low", "finding": "ChatApprovalPolicy::decide runs 2 DB queries (approved+denied) per tool-call decision per step even with no pending approvals; legacy classification was in-memory", "status": "confirmed"}
+{"angle": "concurrency", "file": "src-app/agent-core/src/fanout.rs", "line": 83, "severity": "low", "finding": "fan_out returns Err on first child error while remaining spawned children run detached (JoinHandle drop != abort); CancelToken not fired -> wasted model calls. NOTE: fanout is DESCOPED (no production caller) so not a live path", "status": "confirmed"}
+{"angle": "security", "file": "src-app/server/src/modules/workflow/dispatch.rs", "line": 2264, "severity": "low", "finding": "broadened is_built_in accessibility relies on 'per-tool authz enforced downstream for every built-in' but no test asserts a permission-lacking user is denied via the agent path (unverified invariant, no concrete escalation found)", "status": "confirmed"}
+{"angle": "tests-reality", "file": "src-app/server/tests/agent/verification_test.rs", "line": 733, "severity": "low", "finding": "dump.contains('not_found') is near-trivially true (not_found is in the citations tool schema); the real anchor is the count==0 bibliography check", "status": "rejected-minor"}
+{"angle": "api-contract", "file": "src-app/agent-core/src/ports.rs", "line": 1, "severity": "none", "finding": "ports.rs 6 traits are domain-neutral (Uuid/ai_providers/AppError only), no host/chat/DB leak; a new consumer can implement without editing the crate", "status": "rejected-clean"}
+{"angle": "correctness", "file": "src-app/server/src/modules/workflow/agent_dispatch.rs", "line": 355, "severity": "low", "finding": "is_trusted false-denies read-only built-ins (elicitation/knowledge_base/skill/tool_result) not in builtin_server_id_by_name (only 7 of 10 mapped) -> a workflow agent step using them routes through review instead of auto-bypass. PRE-EXISTING (before the Phase-7 fix too: None.is_some()=false); conservative-safe (more approvals, not a security hole); my fix only correctly changed code_sandbox", "status": "rejected-preexisting-conservative"}
+{"angle": "maintainability", "file": "src-app/server/src/modules/chat/agent_host/resolver.rs", "line": 294, "severity": "low", "finding": "fn-level #[allow(unused_variables)] over-suppresses (only idem is unused) -> a future dead local in call() would be silenced", "status": "confirmed"}
+{"angle": "api-friendliness", "file": "src-app/server/tests/common/mod.rs", "line": 15, "severity": "low", "finding": "AgentCoreFlag #[must_use] does not fire on `let _ = AgentCoreFlag::on()` (drops immediately, flag off during test body); all 6 callers use a named binding so latent not live", "status": "accepted-latent"}
+{"angle": "tests-reality", "file": "src-app/server/tests/path_resolution.rs", "line": 153, "severity": "medium", "finding": "PRE-EXISTING compile error: `config.postgresql.embedded.expect()` moves a non-Copy field out of a borrow -> the whole path_resolution test binary failed to build (0 tests ran). Fixed with `.as_ref()`. Real bug (deterministic, not model-flaky). red: build error -> green: 6 passed", "status": "fixed-preexisting"}
+{"angle": "tests-reality", "file": "src-app/server/tests/memory_mcp/mod.rs", "line": 252, "severity": "medium", "finding": "PRE-EXISTING deterministic OFF failure: test_recall_requires_memory_enabled assumed memory is disabled-by-default, but the schema seeds enabled=true (202607140185_memory_schema.sql:17 DEFAULT true + 202607145050_memory_seed.sql:4). Recall's MEMORY_DISABLED guard (handlers.rs:415) is CORRECT; the test never actually disabled memory so recall returned {memories:[]}. Fixed: test now sets enabled=false via the DB then asserts the guard fires. red: FAILED -> green: ok", "status": "fixed-preexisting"}
+{"angle": "tests-reality", "file": "src-app/server/tests/chat/test_single_assistant_message_architecture.rs", "line": 357, "severity": "medium", "finding": "PRE-EXISTING deterministic OFF failure: helper parsed GET /conversations/{id}/messages as Vec<Value>, but the endpoint returns PaginatedMessages object {messages,has_more_before,has_more_after} (routes.rs:37->get_conversation_history -> Json<PaginatedMessages>). Stale test. Fixed to extract .messages. red: 'invalid type: map, expected a sequence' -> green: ok", "status": "fixed-preexisting"}
+{"angle": "tests-reality", "file": "src-app/server/tests/mcp/mcp_streaming_workflow_test.rs", "line": 746, "severity": "medium", "finding": "PRE-EXISTING deterministic OFF failure: `body.as_array()` with stale comment 'API returns array directly' on the same PaginatedMessages endpoint. Fixed to extract .messages. red: FAILED -> green: ok", "status": "fixed-preexisting"}
+{"angle": "tests-reality", "file": "src-app/server/tests/mcp/elicitation_mcp_test.rs", "line": 236, "severity": "low", "finding": "PRE-EXISTING deterministic OFF failure: BUILTIN_SUFFIXES allow-list missing __run_js (js_tool auto-attaches for tool-capable chats), so the 'only built-in tools attach' assertion failed on the run_js tool. Product behavior correct; stale allow-list. Fixed: added __run_js. red: FAILED -> green: ok", "status": "fixed-preexisting"}
+{"angle": "correctness", "file": "src-app/server/src/modules/file/available_files.rs", "line": 292, "severity": "medium", "finding": "PRE-EXISTING (on main, NOT agent-core) deterministic cluster: 8 agentic_chat tests fail because attach_files_mcp is gated on manifest_available = !files.is_empty() (available_files.rs:292 -> file.rs:117), so files_mcp write tools (create_file/edit_file) never attach in an EMPTY conversation -> the model cannot author the first file (chicken-and-egg); combined memory+files tests hit the same. Also the StubChat harness has no create_file plan arm. Root-caused but NOT fixed here: the fix (decouple files_mcp tool-attach from file-presence) is a core-chat product-behavior change with wide blast radius (changes the attached-tool set for EVERY conversation, breaks other committed assertions e.g. the elicitation empty-config test) and a genuine product decision, outside the agent-core diff. SURFACED for a scoping decision (see PREEXISTING_BUGS.md).", "status": "reported-needs-scoping"}
+{"angle": "tests-reality", "file": "src-app/server/tests/mcp/tool_call_history_test.rs", "line": 528, "severity": "info", "finding": "NOT a code bug: chat_path_tool_call_records_source_chat fails at the `tool_starts > 0` GUARD (the test itself documents this distinguishes 'an LLM/setup miss' from a recording bug). The weak local Qwen bridge did not emit the tool call. MODEL-FLAKY, classified not-a-bug.", "status": "classified-model-flaky"}
diff --git a/src-app/server/src/modules/chat/agent_host/dispatcher.rs b/src-app/server/src/modules/chat/agent_host/dispatcher.rs
index f770cdc7c..ecd574010 100644
--- a/src-app/server/src/modules/chat/agent_host/dispatcher.rs
+++ b/src-app/server/src/modules/chat/agent_host/dispatcher.rs
@@ -39,7 +39,7 @@ use crate::common::AppError;
 use crate::modules::chat::agent_host::event_sink::ChatEventSink;
 use crate::modules::chat::agent_host::gate::ChatHumanGate;
 use crate::modules::chat::agent_host::resolver::{ChatCancel, ChatModelResolver, ChatToolProvider};
-use crate::modules::workflow::dispatch::CancelSignal;
+use crate::modules::mcp::agent_tool_call::CancelSignal;
 use crate::modules::chat::agent_host::transcript::ChatTranscriptStore;
 use crate::modules::chat::core::extension::{ExtensionRegistry, StreamContext};
 use crate::utils::cancellation::CancellationToken;
diff --git a/src-app/server/src/modules/chat/agent_host/gate.rs b/src-app/server/src/modules/chat/agent_host/gate.rs
index e2892ab61..2bf45207f 100644
--- a/src-app/server/src/modules/chat/agent_host/gate.rs
+++ b/src-app/server/src/modules/chat/agent_host/gate.rs
@@ -420,7 +420,7 @@ impl HumanGate for ChatHumanGate {
         // usable id; otherwise the resume path (`execute_approved_tools_sync`) hits
         // "No server_id in approval record" and never executes the approved tool.
         if server_id.is_none() && !server_str.is_empty() {
-            server_id = crate::modules::workflow::dispatch::resolve_tool_server(self.user_id, &server_str)
+            server_id = crate::modules::mcp::agent_tool_call::resolve_tool_server(self.user_id, &server_str)
                 .await
                 .ok();
         }
diff --git a/src-app/server/src/modules/chat/agent_host/resolver.rs b/src-app/server/src/modules/chat/agent_host/resolver.rs
index 3dd0b83b8..5afd62153 100644
--- a/src-app/server/src/modules/chat/agent_host/resolver.rs
+++ b/src-app/server/src/modules/chat/agent_host/resolver.rs
@@ -52,9 +52,12 @@ use tokio::sync::mpsc::UnboundedSender;
 use uuid::Uuid;
 
 use crate::common::AppError;
-use crate::modules::workflow::dispatch::{
-    call_mcp_tool, resolve_tool_server, CancelSignal, McpCallScope,
-    McpToolCallError,
+// Shared MCP tool-call chokepoint lives in `mcp::agent_tool_call` (§9 DAG:
+// shared infra — the chat host imports it from `mcp/`, NOT laterally from the
+// workflow feature module).
+use crate::modules::mcp::agent_tool_call::{
+    call_mcp_tool, mcp_to_agent_result, resolve_tool_server, split_tool_name, CancelSignal,
+    ChatCallCtx, McpCallScope, McpToolCallError,
 };
 use crate::utils::cancellation::CancellationToken;
 
@@ -137,54 +140,9 @@ impl CancelSignal for ChatCancel {
 // Tool provider (chat flavor — mirrors McpToolProvider, DEC-17)
 // ============================================================
 
-/// Split a namespaced tool name `"<server>__<tool>"` (as emitted by
-/// [`ChatToolProvider::list`]) back into `(server_name, tool_name)`. Splits on the
-/// FIRST `__` (mirrors the chat/workflow `server_id__name` scheme).
-fn split_tool_name(name: &str) -> (String, String) {
-    match name.find("__") {
-        Some(idx) => (name[..idx].to_string(), name[idx + 2..].to_string()),
-        None => (String::new(), name.to_string()),
-    }
-}
-
-/// Flatten an MCP `ToolResult` into an `agent_core::ToolResult`: concatenate its
-/// text blocks into one `Text` block (mirrors the workflow twin's
-/// `mcp_to_agent_result`), preserving `is_error` + `structured_content`.
-fn mcp_to_agent_result(r: crate::modules::mcp::client::traits::ToolResult) -> ToolResult {
-    let mut text = String::new();
-    for c in &r.content {
-        if c.content.get("type").and_then(|v| v.as_str()) == Some("text") {
-            if let Some(t) = c.content.get("text").and_then(|v| v.as_str()) {
-                if !text.is_empty() {
-                    text.push('\n');
-                }
-                text.push_str(t);
-            }
-        }
-    }
-    // No text blocks (e.g. an image-only or structured-only result) → stringify
-    // the raw content so the model still sees something actionable.
-    if text.is_empty() && !r.content.is_empty() {
-        text = serde_json::to_string(&r.content).unwrap_or_default();
-    }
-    // Audience-only bypass (parity with the MCP extension's `execute_tool`): if any
-    // content block is annotated `audience: ["user"]` EXACTLY, the tool's output is
-    // meant for the user, not the model — the turn ends with it (no continuation).
-    let terminal = r.content.iter().any(|c| {
-        c.content
-            .get("annotations")
-            .and_then(|a| a.get("audience"))
-            .and_then(|v| v.as_array())
-            .map(|arr| arr.len() == 1 && arr[0].as_str() == Some("user"))
-            .unwrap_or(false)
-    });
-    ToolResult {
-        content: vec![ContentBlock::Text { text }],
-        is_error: r.is_error,
-        structured_content: r.structured_content,
-        terminal,
-    }
-}
+// `split_tool_name` + `mcp_to_agent_result` now live in the shared
+// `mcp::agent_tool_call` (de-duplicated with the workflow host; the shared
+// `mcp_to_agent_result` keeps this host's audience-computed `terminal`).
 
 /// The chat agent's tool surface — the conversation's attached servers (server
 /// NAMES on `ToolScope.servers`), resolved to MCP tools and routed through the
@@ -328,7 +286,7 @@ impl ToolProvider for ChatToolProvider {
             // Route through the shared chokepoint WITH the chat context so a
             // sampling server gets a `new_with_sampling` session + the journal row
             // carries branch/message/tool_use (parity with the legacy path).
-            Some(crate::modules::workflow::dispatch::ChatCallCtx {
+            Some(ChatCallCtx {
                 branch_id: self.branch_id,
                 message_id: self.message_id,
                 tool_use_id: call.id.clone(),
diff --git a/src-app/server/src/modules/mcp/agent_tool_call.rs b/src-app/server/src/modules/mcp/agent_tool_call.rs
new file mode 100644
index 000000000..8716ad502
--- /dev/null
+++ b/src-app/server/src/modules/mcp/agent_tool_call.rs
@@ -0,0 +1,440 @@
+//! Shared MCP tool-call chokepoint (moved here from `workflow::dispatch` so it is
+//! SHARED INFRA both the workflow dispatcher AND the chat agent host import from
+//! `mcp/` — neither feature module imports the other for it; §9 DAG rule).
+//!
+//! Owns: the built-in NAME->id map, `resolve_tool_server`, `McpCallScope` (the
+//! minimal run identity extracted from `RunContext`), `McpToolCallError`, the
+//! `CancelSignal` seam (its one workflow impl — `RunHandle` — stays in
+//! `workflow::dispatch`, which owns that type), `ChatCallCtx`, the `call_mcp_tool`
+//! path, and the two agent-core adapters (`split_tool_name` / `mcp_to_agent_result`)
+//! previously copy-pasted in both hosts.
+
+use agent_core::ToolResult;
+use ai_providers::ContentBlock;
+use async_trait::async_trait;
+use serde_json::Value;
+use uuid::Uuid;
+
+use crate::common::AppError;
+
+/// Resolve a `tool` step's `server` NAME to a server id the running user may
+/// call. Built-ins resolve by stable name (their OWN permission still gates the
+/// call); user/system servers resolve within the user's accessible enabled set.
+/// Pure NAME → built-in server-id mapping used by `resolve_tool_server`.
+/// Extracted so the workflow↔built-in wiring (incl. the workflow→memory MCP
+/// seam) is unit-testable without a DB. `None` for non-built-in names.
+pub(crate) fn builtin_server_id_by_name(server_name: &str) -> Option<Uuid> {
+    match server_name {
+        "web_search" => Some(crate::modules::web_search::web_search_server_id()),
+        "bio" => Some(crate::modules::bio_mcp::bio_mcp_server_id()),
+        "lit_search" => Some(crate::modules::lit_search::lit_search_server_id()),
+        "citations" => Some(crate::modules::citations::citations_server_id()),
+        "memory" => Some(crate::modules::memory_mcp::memory_mcp_server_id()),
+        "files" => Some(crate::modules::files_mcp::files_mcp_server_id()),
+        "code_sandbox" => Some(crate::modules::code_sandbox::code_sandbox_server_id()),
+        _ => None,
+    }
+}
+
+pub(crate) async fn resolve_tool_server(
+    user_id: Uuid,
+    server_name: &str,
+) -> Result<Uuid, AppError> {
+    use crate::core::Repos;
+    let builtin = builtin_server_id_by_name(server_name);
+    if let Some(id) = builtin {
+        // Built-in: allowed iff registered + enabled. The server's own
+        // permission (bio::query / web_search::use / ...) gates the call.
+        match Repos.mcp.get_any_server(id).await? {
+            Some(s) if s.enabled => return Ok(id),
+            _ => {
+                return Err(AppError::forbidden(
+                    "WORKFLOW_TOOL_SERVER_NOT_ACCESSIBLE",
+                    format!("built-in server '{server_name}' is not enabled"),
+                ));
+            }
+        }
+    }
+    // User-owned / group-assigned system server, by name (enabled + accessible).
+    let servers = crate::modules::mcp::chat_extension::helpers::get_all_accessible_config(
+        Repos.pool(),
+        user_id,
+    )
+    .await?;
+    if let Some(s) = servers
+        .into_iter()
+        .find(|s| s.name == server_name && s.enabled)
+    {
+        return Ok(s.id);
+    }
+    Err(AppError::forbidden(
+        "WORKFLOW_TOOL_SERVER_NOT_ACCESSIBLE",
+        format!("server '{server_name}' is not accessible to this user"),
+    ))
+}
+
+/// The minimal run identity `call_mcp_tool` needs — extracted from `RunContext`
+/// so the shared MCP call path is reachable from BOTH the `ToolDispatcher` (which
+/// has a full `RunContext`) and the agent host's `McpToolProvider` (which holds
+/// only these three fields).
+#[derive(Debug, Clone, Copy)]
+pub(crate) struct McpCallScope {
+    pub user_id: Uuid,
+    pub conversation_id: Option<Uuid>,
+    pub run_id: Uuid,
+}
+
+/// Outcome of the shared MCP call path (DEC-17).
+pub(crate) enum McpToolCallError {
+    /// The run's cancel handle fired mid-call.
+    Cancelled,
+    /// Any other failure (server inaccessible, disabled, session/RPC error).
+    Failed(String),
+}
+
+/// The shared MCP tool-call path (ITEM-21 / DEC-17) — resolve the server NAME,
+/// apply the conversation/default disabled-server gate (only when
+/// `enforce_conversation_disabled`), open a recording session linked to the run,
+/// and invoke the tool with the given (already-rendered) `args`. Returns the
+/// resolved `server_id` (the caller needs it for `resource_link` persistence) +
+/// the raw MCP `ToolResult`.
+///
+/// `ToolDispatcher` calls this with `enforce_conversation_disabled = true`
+/// (behaviour-preserving — same gate it applied inline before the extraction).
+/// The workflow agent host's `McpToolProvider` also passes `true`; the chat host
+/// (a later stage) passes `false` to preserve its current non-enforcement.
+/// A cancellation signal `call_mcp_tool` can await, decoupled from the workflow
+/// `RunHandle` so a non-workflow host (chat's stop-generation) can supply its own
+/// token without owning a `RunHandle`. `RunHandle` implements it directly, so the
+/// workflow call sites are unchanged in behavior.
+#[async_trait]
+pub(crate) trait CancelSignal: Send + Sync {
+    /// Resolves when cancellation is requested (or immediately if already cancelled).
+    async fn cancelled(&self);
+}
+
+/// Chat-turn context threaded into the shared MCP call chokepoint by the
+/// agent-core CHAT host so it routes through the SAME machinery the legacy path
+/// uses: (a) a sampling server gets an ephemeral `new_with_sampling` session (so
+/// server→host sampling round-trips fire), and (b) the recorded `mcp_tool_calls`
+/// row carries the real `branch_id`/`message_id`/`tool_use_id` context (journaling
+/// parity). Workflow callers pass `None` → the pooled, chat-context-free path is
+/// byte-identical to before.
+pub(crate) struct ChatCallCtx {
+    pub branch_id: Uuid,
+    pub message_id: Uuid,
+    pub tool_use_id: String,
+    pub model_id: Uuid,
+}
+
+pub(crate) async fn call_mcp_tool(
+    scope: &McpCallScope,
+    server_name: &str,
+    tool_name: &str,
+    args: Value,
+    enforce_conversation_disabled: bool,
+    cancel: &dyn CancelSignal,
+    // Chat-only context (sampling session + journal linkage); `None` for workflow.
+    chat_ctx: Option<ChatCallCtx>,
+    // ITEM-12: reviewer risk classification (`low`/`high`/`critical`) stamped
+    // onto the recorded `mcp_tool_calls` row (`None` for the plain tool step).
+    review_classification: Option<String>,
+    // ITEM-16: stable per-call idempotency key `<run_id>:<turn>:<ordinal>` threaded
+    // into the MCP call context so an in-flight side-effecting call is identifiable
+    // on resume (`None` for the plain tool step).
+    idempotency_key: Option<String>,
+    // Which surface the recorded `mcp_tool_calls` row attributes this call to
+    // (`Workflow` for the workflow tool step + agent host; `Chat` for the chat
+    // agent host, so its rows read `chat` like today's chat tool calls).
+    source: crate::modules::mcp::tool_calls::models::McpToolCallSource,
+) -> Result<(Uuid, crate::modules::mcp::client::traits::ToolResult), McpToolCallError> {
+    // The namespaced prefix is either a server NAME (workflow's `McpToolProvider`
+    // list) or a server ID uuid (chat's MCP extension namespaces tools as
+    // `<server_id>__<tool>`). Accept both: a parseable uuid is the id directly;
+    // otherwise resolve the name.
+    let server_id = match uuid::Uuid::parse_str(server_name) {
+        Ok(id) => {
+            // SECURITY: re-validate the (model-supplied) server id against the acting
+            // user's accessible set — a built-in (enabled) OR a server the user's
+            // groups are assigned to. Without this the raw-uuid path would let a
+            // prompt-injected tool name reach an arbitrary server_id the user isn't
+            // assigned to, executing with that server's admin-configured secret
+            // headers (cross-group authz bypass). Mirrors the legacy accessible-set
+            // check at `mcp.rs` (execute_approved_tools_sync).
+            let accessible = match crate::core::Repos.mcp.get_any_server(id).await {
+                // A BUILT-IN server (control/skill/workflow/memory/… — `is_built_in`,
+                // some also `is_system`) is accessible to any user when enabled; its
+                // per-tool authz is enforced downstream at the JSON-RPC handler
+                // (`control::use`, etc.). Some built-ins (control) are NOT in the
+                // approval-bypass `is_builtin_server_id` set AND are `is_system` (so
+                // redacted out of `get_all_accessible_config`), which previously made
+                // them unreachable on the agent-core chat path — the legacy
+                // `execute_tool` path treats built-ins as accessible, so match it.
+                Ok(Some(s))
+                    if s.enabled
+                        && (s.is_built_in
+                            || crate::modules::mcp::chat_extension::mcp::is_builtin_server_id(id)) =>
+                {
+                    true
+                }
+                // External / user-registered server: require it in the user's
+                // accessible set (unchanged — the cross-group-authz-bypass guard).
+                _ => crate::modules::mcp::chat_extension::helpers::get_all_accessible_config(
+                    crate::core::Repos.pool(),
+                    scope.user_id,
+                )
+                .await
+                .map(|servers| servers.iter().any(|s| s.id == id && s.enabled))
+                .unwrap_or(false),
+            };
+            if !accessible {
+                return Err(McpToolCallError::Failed(format!(
+                    "server '{id}' is not accessible to this user"
+                )));
+            }
+            id
+        }
+        Err(_) => match resolve_tool_server(scope.user_id, server_name).await {
+            Ok(id) => id,
+            Err(e) => return Err(McpToolCallError::Failed(e.to_string())),
+        },
+    };
+
+    // E8: reject a server OR a specific tool the user disabled. Conversation-
+    // scoped when a conversation is present; otherwise the user's DEFAULT MCP
+    // disabled set (the scheduled/standalone case). Fail CLOSED on any DB error
+    // (security gate). Skipped entirely when `enforce_conversation_disabled` is
+    // false (the chat host's current behavior — DEC-17).
+    if enforce_conversation_disabled {
+        if let Some(conv_id) = scope.conversation_id {
+            let settings = match crate::core::repository::Repos
+                .mcp_settings
+                .get(crate::modules::mcp::settings::models::McpScope::Conversation(conv_id))
+                .await
+            {
+                Ok(s) => s,
+                Err(e) => {
+                    return Err(McpToolCallError::Failed(format!(
+                        "tool: could not resolve server policy: {e}"
+                    )));
+                }
+            };
+            if let Some(settings) = settings {
+                let disabled: Vec<
+                    crate::modules::mcp::chat_extension::approval::models::DisabledServer,
+                > = serde_json::from_value(settings.disabled_servers).unwrap_or_default();
+                if disabled.iter().any(|d| {
+                    d.server_id == server_id
+                        && (d.is_server_disabled() || d.is_tool_disabled(tool_name))
+                }) {
+                    return Err(McpToolCallError::Failed(format!(
+                        "tool '{tool_name}' on server '{server_name}' is disabled in this conversation"
+                    )));
+                }
+            }
+        } else {
+            let defaults = match crate::modules::mcp::chat_extension::defaults::repository::get_user_defaults(
+                crate::core::Repos.pool(),
+                scope.user_id,
+            )
+            .await
+            {
+                Ok(d) => d,
+                Err(e) => {
+                    return Err(McpToolCallError::Failed(format!(
+                        "tool: could not resolve user MCP defaults: {e}"
+                    )));
+                }
+            };
+            if let Some(defaults) = defaults {
+                let disabled = defaults.get_disabled_servers();
+                if disabled.iter().any(|d| {
+                    d.server_id == server_id
+                        && (d.is_server_disabled() || d.is_tool_disabled(tool_name))
+                }) {
+                    return Err(McpToolCallError::Failed(format!(
+                        "tool '{tool_name}' on server '{server_name}' is disabled in your default MCP settings"
+                    )));
+                }
+            }
+        }
+    }
+
+    let manager = match crate::modules::mcp::client::manager::global() {
+        Some(m) => m,
+        None => {
+            return Err(McpToolCallError::Failed(
+                "MCP session manager not initialized".into(),
+            ));
+        }
+    };
+    // Session selection. The CHAT host (chat_ctx = Some) routes through the SAME
+    // shared machinery the legacy path uses: a `supports_sampling` server gets an
+    // ephemeral `new_with_sampling` session (server→host sampling round-trips), and
+    // the pooled path carries the real branch/message/tool_use context (journaling
+    // parity). Workflow (chat_ctx = None) is unchanged — pooled, no chat context.
+    let session: std::sync::Arc<tokio::sync::RwLock<crate::modules::mcp::client::session::McpSession>> =
+        if let Some(cc) = &chat_ctx {
+            // Lightweight lookup (works for built-in loopback servers too, unlike
+            // `resolve_server_for_session` which is for external un-redacted URLs)
+            // to decide the sampling-vs-pooled branch WITHOUT an early-return that
+            // would skip `call_tool` (and thus the journal recording) for built-ins.
+            let supports_sampling = crate::core::Repos
+                .mcp
+                .get_any_server(server_id)
+                .await
+                .ok()
+                .flatten()
+                .map(|s| s.supports_sampling)
+                .unwrap_or(false);
+            if supports_sampling {
+                let server_row = match manager.resolve_server_for_session(server_id).await {
+                    Ok(s) => s,
+                    Err(e) => return Err(McpToolCallError::Failed(format!("tool: resolve server: {e}"))),
+                };
+                // Sampling server → fresh ephemeral session WITH the host-LLM
+                // handler (parity with legacy `execute_tool`). No pooled fallback:
+                // a pooled sampling session deadlocks the SSE round-trip.
+                let handler: std::sync::Arc<dyn crate::modules::mcp::sampling::handler::SamplingHandler> =
+                    match crate::modules::mcp::sampling::handler::ChatSamplingHandler::new(cc.model_id, scope.user_id).await {
+                        Ok(h) => std::sync::Arc::new(h),
+                        Err(e) => return Err(McpToolCallError::Failed(format!("tool: sampling handler init: {e}"))),
+                    };
+                let mut s = match crate::modules::mcp::client::session::McpSession::new_with_sampling(server_row.clone(), handler).await {
+                    Ok(s) => s,
+                    Err(e) => return Err(McpToolCallError::Failed(format!("tool: sampling session: {e}"))),
+                };
+                s.set_call_context(crate::modules::mcp::tool_calls::models::McpCallContext {
+                    user_id: Some(scope.user_id),
+                    conversation_id: scope.conversation_id,
+                    branch_id: Some(cc.branch_id),
+                    message_id: Some(cc.message_id),
+                    tool_use_id: Some(cc.tool_use_id.clone()),
+                    source,
+                    server_name: server_row.name.clone(),
+                    is_built_in: server_row.is_built_in,
+                    ..Default::default()
+                });
+                std::sync::Arc::new(tokio::sync::RwLock::new(s))
+            } else {
+                match manager
+                    .get_or_create_with_context(
+                        server_id,
+                        scope.user_id,
+                        scope.conversation_id,
+                        Some(cc.branch_id),
+                        Some(cc.message_id),
+                        Some(cc.tool_use_id.clone()),
+                        source,
+                    )
+                    .await
+                {
+                    Ok(s) => s,
+                    Err(e) => return Err(McpToolCallError::Failed(format!("tool: open session: {e}"))),
+                }
+            }
+        } else {
+            match manager
+                .get_or_create_with_context(
+                    server_id,
+                    scope.user_id,
+                    scope.conversation_id,
+                    None, // branch_id
+                    None, // message_id — a workflow run has no chat message
+                    None, // tool_use_id — not an LLM ContentBlock::ToolUse
+                    source,
+                )
+                .await
+            {
+                Ok(s) => s,
+                Err(e) => return Err(McpToolCallError::Failed(format!("tool: open session: {e}"))),
+            }
+        };
+
+    let call = async {
+        let mut guard = session.write().await;
+        // E4: link the recorded `mcp_tool_calls` row to this run — WORKFLOW only.
+        // `mcp_tool_calls.workflow_run_id` FKs `workflow_runs`; a CHAT `run_id` is
+        // the assistant message id, not a workflow run, so setting it would
+        // FK-violate the insert (and silently drop the recording). The chat row is
+        // instead owner/conversation-scoped via the session context.
+        if matches!(
+            source,
+            crate::modules::mcp::tool_calls::models::McpToolCallSource::Workflow
+        ) {
+            guard.set_workflow_run(scope.run_id);
+            // ITEM-16: the idempotency key is persisted as the row's `tool_use_id`
+            // (unused for a workflow call). A chat call's real tool_use_id comes
+            // from the LLM ContentBlock, so don't overwrite it with the key here.
+            if let Some(key) = idempotency_key {
+                guard.set_idempotency_key(key);
+            }
+        }
+        // ITEM-12: carry the reviewer classification onto the journal row.
+        guard.set_review_classification(review_classification);
+        guard.call_tool(tool_name, args, None, None, None).await
+    };
+    let result = tokio::select! {
+        r = call => r,
+        _ = cancel.cancelled() => return Err(McpToolCallError::Cancelled),
+    };
+    match result {
+        Ok(r) => Ok((server_id, r)),
+        Err(e) => Err(McpToolCallError::Failed(format!("tool '{tool_name}': {e}"))),
+    }
+}
+
+/// Split a namespaced tool wire name `<server>__<tool>` into (server, tool).
+/// A bare name (no `__`) yields an empty server + the whole string. Shared by
+/// both agent hosts (was copy-pasted in `chat::agent_host::resolver` and
+/// `workflow::agent_dispatch`).
+pub(crate) fn split_tool_name(name: &str) -> (String, String) {
+    match name.find("__") {
+        Some(idx) => (name[..idx].to_string(), name[idx + 2..].to_string()),
+        None => (String::new(), name.to_string()),
+    }
+}
+
+/// Flatten an MCP `ToolResult` into an `agent_core::ToolResult`: concatenate its
+/// text blocks into one `Text` block, preserving `is_error` + `structured_content`.
+///
+/// `terminal` is the audience-only bypass (parity with the MCP extension's
+/// `execute_tool`): if any content block is annotated `audience: ["user"]`
+/// EXACTLY, the output is for the user, not the model — the turn ends with it (no
+/// continuation round). Both hosts share this ONE definition; the workflow host
+/// previously hardcoded `terminal: false`, which IGNORED the audience-terminal
+/// signal (a latent bug) — unified here on the correct audience-computed value.
+pub(crate) fn mcp_to_agent_result(
+    r: crate::modules::mcp::client::traits::ToolResult,
+) -> ToolResult {
+    let mut text = String::new();
+    for c in &r.content {
+        if c.content.get("type").and_then(|v| v.as_str()) == Some("text") {
+            if let Some(t) = c.content.get("text").and_then(|v| v.as_str()) {
+                if !text.is_empty() {
+                    text.push('\n');
+                }
+                text.push_str(t);
+            }
+        }
+    }
+    // No text blocks (e.g. an image-only or structured-only result) -> stringify
+    // the raw content so the model still sees something actionable.
+    if text.is_empty() && !r.content.is_empty() {
+        text = serde_json::to_string(&r.content).unwrap_or_default();
+    }
+    let terminal = r.content.iter().any(|c| {
+        c.content
+            .get("annotations")
+            .and_then(|a| a.get("audience"))
+            .and_then(|v| v.as_array())
+            .map(|arr| arr.len() == 1 && arr[0].as_str() == Some("user"))
+            .unwrap_or(false)
+    });
+    ToolResult {
+        content: vec![ContentBlock::Text { text }],
+        is_error: r.is_error,
+        structured_content: r.structured_content,
+        terminal,
+    }
+}
diff --git a/src-app/server/src/modules/mcp/mod.rs b/src-app/server/src/modules/mcp/mod.rs
index cc7b3871d..655fc8526 100644
--- a/src-app/server/src/modules/mcp/mod.rs
+++ b/src-app/server/src/modules/mcp/mod.rs
@@ -1,3 +1,4 @@
+pub(crate) mod agent_tool_call;
 pub mod chat_extension;
 pub mod client;
 pub mod connection_health;
diff --git a/src-app/server/src/modules/workflow/agent_dispatch.rs b/src-app/server/src/modules/workflow/agent_dispatch.rs
index f56ffe5a9..ffdbcedaf 100644
--- a/src-app/server/src/modules/workflow/agent_dispatch.rs
+++ b/src-app/server/src/modules/workflow/agent_dispatch.rs
@@ -42,10 +42,13 @@ use sqlx::PgPool;
 use uuid::Uuid;
 
 use crate::common::AppError;
-use crate::modules::workflow::dispatch::{
-    builtin_server_id_by_name, call_mcp_tool, resolve_prompt, McpCallScope, McpToolCallError,
-    StepDispatcher,
+// Shared MCP tool-call chokepoint now lives in `mcp::agent_tool_call` (§9 DAG:
+// shared infra, imported from `mcp/` by both this host and the chat host).
+use crate::modules::mcp::agent_tool_call::{
+    builtin_server_id_by_name, call_mcp_tool, mcp_to_agent_result, split_tool_name, McpCallScope,
+    McpToolCallError,
 };
+use crate::modules::workflow::dispatch::{resolve_prompt, StepDispatcher};
 use crate::modules::workflow::events::{
     ProgressEmitter, ProgressKind, ProgressTrack, SSEElicitationRequiredData, SSEStepProgressData,
     SSEWorkflowRunEvent,
@@ -197,43 +200,10 @@ struct McpToolProvider {
     classifications: Arc<Mutex<HashMap<String, String>>>,
 }
 
-/// Split a namespaced tool name `"<server>__<tool>"` (as emitted by
-/// [`McpToolProvider::list`]) back into `(server_name, tool_name)`. Splits on the
-/// FIRST `__` (mirrors the chat path's `server_id__name` scheme).
-fn split_tool_name(name: &str) -> (String, String) {
-    match name.find("__") {
-        Some(idx) => (name[..idx].to_string(), name[idx + 2..].to_string()),
-        None => (String::new(), name.to_string()),
-    }
-}
-
-/// Flatten an MCP `ToolResult` into an `agent_core::ToolResult`: concatenate its
-/// text blocks into one `Text` block (mirrors the workflow tool-step's
-/// `tool_result_text`), preserving `is_error` + `structured_content`.
-fn mcp_to_agent_result(r: crate::modules::mcp::client::traits::ToolResult) -> ToolResult {
-    let mut text = String::new();
-    for c in &r.content {
-        if c.content.get("type").and_then(|v| v.as_str()) == Some("text") {
-            if let Some(t) = c.content.get("text").and_then(|v| v.as_str()) {
-                if !text.is_empty() {
-                    text.push('\n');
-                }
-                text.push_str(t);
-            }
-        }
-    }
-    // No text blocks (e.g. an image-only or structured-only result) → stringify
-    // the raw content so the model still sees something actionable.
-    if text.is_empty() && !r.content.is_empty() {
-        text = serde_json::to_string(&r.content).unwrap_or_default();
-    }
-    ToolResult {
-        content: vec![ContentBlock::Text { text }],
-        is_error: r.is_error,
-        structured_content: r.structured_content,
-        terminal: false,
-    }
-}
+// `split_tool_name` + `mcp_to_agent_result` now live in the shared
+// `mcp::agent_tool_call` (de-duplicated with the chat host; the workflow host's
+// previous `terminal: false` hardcode — a latent bug that ignored the
+// audience-terminal signal — is reconciled there onto the audience-computed value).
 
 #[async_trait]
 impl ToolProvider for McpToolProvider {
diff --git a/src-app/server/src/modules/workflow/dispatch.rs b/src-app/server/src/modules/workflow/dispatch.rs
index 1e76179a8..9fca1ccae 100644
--- a/src-app/server/src/modules/workflow/dispatch.rs
+++ b/src-app/server/src/modules/workflow/dispatch.rs
@@ -1028,102 +1028,18 @@ fn tool_result_text(result: &crate::modules::mcp::client::traits::ToolResult) ->
     parts.join("\n")
 }
 
-/// Resolve a `tool` step's `server` NAME to a server id the running user may
-/// call. Built-ins resolve by stable name (their OWN permission still gates the
-/// call); user/system servers resolve within the user's accessible enabled set.
-/// Pure NAME → built-in server-id mapping used by `resolve_tool_server`.
-/// Extracted so the workflow↔built-in wiring (incl. the workflow→memory MCP
-/// seam) is unit-testable without a DB. `None` for non-built-in names.
-pub(crate) fn builtin_server_id_by_name(server_name: &str) -> Option<Uuid> {
-    match server_name {
-        "web_search" => Some(crate::modules::web_search::web_search_server_id()),
-        "bio" => Some(crate::modules::bio_mcp::bio_mcp_server_id()),
-        "lit_search" => Some(crate::modules::lit_search::lit_search_server_id()),
-        "citations" => Some(crate::modules::citations::citations_server_id()),
-        "memory" => Some(crate::modules::memory_mcp::memory_mcp_server_id()),
-        "files" => Some(crate::modules::files_mcp::files_mcp_server_id()),
-        "code_sandbox" => Some(crate::modules::code_sandbox::code_sandbox_server_id()),
-        _ => None,
-    }
-}
 
-pub(crate) async fn resolve_tool_server(
-    user_id: Uuid,
-    server_name: &str,
-) -> Result<Uuid, AppError> {
-    use crate::core::Repos;
-    let builtin = builtin_server_id_by_name(server_name);
-    if let Some(id) = builtin {
-        // Built-in: allowed iff registered + enabled. The server's own
-        // permission (bio::query / web_search::use / ...) gates the call.
-        match Repos.mcp.get_any_server(id).await? {
-            Some(s) if s.enabled => return Ok(id),
-            _ => {
-                return Err(AppError::forbidden(
-                    "WORKFLOW_TOOL_SERVER_NOT_ACCESSIBLE",
-                    format!("built-in server '{server_name}' is not enabled"),
-                ));
-            }
-        }
-    }
-    // User-owned / group-assigned system server, by name (enabled + accessible).
-    let servers = crate::modules::mcp::chat_extension::helpers::get_all_accessible_config(
-        Repos.pool(),
-        user_id,
-    )
-    .await?;
-    if let Some(s) = servers
-        .into_iter()
-        .find(|s| s.name == server_name && s.enabled)
-    {
-        return Ok(s.id);
-    }
-    Err(AppError::forbidden(
-        "WORKFLOW_TOOL_SERVER_NOT_ACCESSIBLE",
-        format!("server '{server_name}' is not accessible to this user"),
-    ))
-}
-
-/// The minimal run identity `call_mcp_tool` needs — extracted from `RunContext`
-/// so the shared MCP call path is reachable from BOTH the `ToolDispatcher` (which
-/// has a full `RunContext`) and the agent host's `McpToolProvider` (which holds
-/// only these three fields).
-#[derive(Debug, Clone, Copy)]
-pub(crate) struct McpCallScope {
-    pub user_id: Uuid,
-    pub conversation_id: Option<Uuid>,
-    pub run_id: Uuid,
-}
-
-/// Outcome of the shared MCP call path (DEC-17).
-pub(crate) enum McpToolCallError {
-    /// The run's cancel handle fired mid-call.
-    Cancelled,
-    /// Any other failure (server inaccessible, disabled, session/RPC error).
-    Failed(String),
-}
-
-/// The shared MCP tool-call path (ITEM-21 / DEC-17) — resolve the server NAME,
-/// apply the conversation/default disabled-server gate (only when
-/// `enforce_conversation_disabled`), open a recording session linked to the run,
-/// and invoke the tool with the given (already-rendered) `args`. Returns the
-/// resolved `server_id` (the caller needs it for `resource_link` persistence) +
-/// the raw MCP `ToolResult`.
-///
-/// `ToolDispatcher` calls this with `enforce_conversation_disabled = true`
-/// (behaviour-preserving — same gate it applied inline before the extraction).
-/// The workflow agent host's `McpToolProvider` also passes `true`; the chat host
-/// (a later stage) passes `false` to preserve its current non-enforcement.
-/// A cancellation signal `call_mcp_tool` can await, decoupled from the workflow
-/// `RunHandle` so a non-workflow host (chat's stop-generation) can supply its own
-/// token without owning a `RunHandle`. `RunHandle` implements it directly, so the
-/// workflow call sites are unchanged in behavior.
-#[async_trait]
-pub(crate) trait CancelSignal: Send + Sync {
-    /// Resolves when cancellation is requested (or immediately if already cancelled).
-    async fn cancelled(&self);
-}
+// The shared MCP tool-call chokepoint (call_mcp_tool + resolve_tool_server +
+// built-in name map + McpCallScope/McpToolCallError/CancelSignal/ChatCallCtx)
+// now lives in `mcp::agent_tool_call` (shared infra) so BOTH this dispatcher and
+// the chat agent host import it from `mcp/`, not from each other (§9 DAG).
+// Re-exported for this module's internal callers.
+pub(crate) use crate::modules::mcp::agent_tool_call::{
+    call_mcp_tool, resolve_tool_server, CancelSignal, McpCallScope, McpToolCallError,
+};
 
+// `RunHandle` (workflow-owned) implements the shared `CancelSignal` trait — the
+// one workflow-local binding kept here (orphan rule: workflow owns RunHandle).
 #[async_trait]
 impl CancelSignal for registry::RunHandle {
     async fn cancelled(&self) {
@@ -1131,276 +1047,6 @@ impl CancelSignal for registry::RunHandle {
     }
 }
 
-/// Chat-turn context threaded into the shared MCP call chokepoint by the
-/// agent-core CHAT host so it routes through the SAME machinery the legacy path
-/// uses: (a) a sampling server gets an ephemeral `new_with_sampling` session (so
-/// server→host sampling round-trips fire), and (b) the recorded `mcp_tool_calls`
-/// row carries the real `branch_id`/`message_id`/`tool_use_id` context (journaling
-/// parity). Workflow callers pass `None` → the pooled, chat-context-free path is
-/// byte-identical to before.
-pub(crate) struct ChatCallCtx {
-    pub branch_id: Uuid,
-    pub message_id: Uuid,
-    pub tool_use_id: String,
-    pub model_id: Uuid,
-}
-
-pub(crate) async fn call_mcp_tool(
-    scope: &McpCallScope,
-    server_name: &str,
-    tool_name: &str,
-    args: Value,
-    enforce_conversation_disabled: bool,
-    cancel: &dyn CancelSignal,
-    // Chat-only context (sampling session + journal linkage); `None` for workflow.
-    chat_ctx: Option<ChatCallCtx>,
-    // ITEM-12: reviewer risk classification (`low`/`high`/`critical`) stamped
-    // onto the recorded `mcp_tool_calls` row (`None` for the plain tool step).
-    review_classification: Option<String>,
-    // ITEM-16: stable per-call idempotency key `<run_id>:<turn>:<ordinal>` threaded
-    // into the MCP call context so an in-flight side-effecting call is identifiable
-    // on resume (`None` for the plain tool step).
-    idempotency_key: Option<String>,
-    // Which surface the recorded `mcp_tool_calls` row attributes this call to
-    // (`Workflow` for the workflow tool step + agent host; `Chat` for the chat
-    // agent host, so its rows read `chat` like today's chat tool calls).
-    source: crate::modules::mcp::tool_calls::models::McpToolCallSource,
-) -> Result<(Uuid, crate::modules::mcp::client::traits::ToolResult), McpToolCallError> {
-    // The namespaced prefix is either a server NAME (workflow's `McpToolProvider`
-    // list) or a server ID uuid (chat's MCP extension namespaces tools as
-    // `<server_id>__<tool>`). Accept both: a parseable uuid is the id directly;
-    // otherwise resolve the name.
-    let server_id = match uuid::Uuid::parse_str(server_name) {
-        Ok(id) => {
-            // SECURITY: re-validate the (model-supplied) server id against the acting
-            // user's accessible set — a built-in (enabled) OR a server the user's
-            // groups are assigned to. Without this the raw-uuid path would let a
-            // prompt-injected tool name reach an arbitrary server_id the user isn't
-            // assigned to, executing with that server's admin-configured secret
-            // headers (cross-group authz bypass). Mirrors the legacy accessible-set
-            // check at `mcp.rs` (execute_approved_tools_sync).
-            let accessible = match crate::core::Repos.mcp.get_any_server(id).await {
-                // A BUILT-IN server (control/skill/workflow/memory/… — `is_built_in`,
-                // some also `is_system`) is accessible to any user when enabled; its
-                // per-tool authz is enforced downstream at the JSON-RPC handler
-                // (`control::use`, etc.). Some built-ins (control) are NOT in the
-                // approval-bypass `is_builtin_server_id` set AND are `is_system` (so
-                // redacted out of `get_all_accessible_config`), which previously made
-                // them unreachable on the agent-core chat path — the legacy
-                // `execute_tool` path treats built-ins as accessible, so match it.
-                Ok(Some(s))
-                    if s.enabled
-                        && (s.is_built_in
-                            || crate::modules::mcp::chat_extension::mcp::is_builtin_server_id(id)) =>
-                {
-                    true
-                }
-                // External / user-registered server: require it in the user's
-                // accessible set (unchanged — the cross-group-authz-bypass guard).
-                _ => crate::modules::mcp::chat_extension::helpers::get_all_accessible_config(
-                    crate::core::Repos.pool(),
-                    scope.user_id,
-                )
-                .await
-                .map(|servers| servers.iter().any(|s| s.id == id && s.enabled))
-                .unwrap_or(false),
-            };
-            if !accessible {
-                return Err(McpToolCallError::Failed(format!(
-                    "server '{id}' is not accessible to this user"
-                )));
-            }
-            id
-        }
-        Err(_) => match resolve_tool_server(scope.user_id, server_name).await {
-            Ok(id) => id,
-            Err(e) => return Err(McpToolCallError::Failed(e.to_string())),
-        },
-    };
-
-    // E8: reject a server OR a specific tool the user disabled. Conversation-
-    // scoped when a conversation is present; otherwise the user's DEFAULT MCP
-    // disabled set (the scheduled/standalone case). Fail CLOSED on any DB error
-    // (security gate). Skipped entirely when `enforce_conversation_disabled` is
-    // false (the chat host's current behavior — DEC-17).
-    if enforce_conversation_disabled {
-        if let Some(conv_id) = scope.conversation_id {
-            let settings = match crate::core::repository::Repos
-                .mcp_settings
-                .get(crate::modules::mcp::settings::models::McpScope::Conversation(conv_id))
-                .await
-            {
-                Ok(s) => s,
-                Err(e) => {
-                    return Err(McpToolCallError::Failed(format!(
-                        "tool: could not resolve server policy: {e}"
-                    )));
-                }
-            };
-            if let Some(settings) = settings {
-                let disabled: Vec<
-                    crate::modules::mcp::chat_extension::approval::models::DisabledServer,
-                > = serde_json::from_value(settings.disabled_servers).unwrap_or_default();
-                if disabled.iter().any(|d| {
-                    d.server_id == server_id
-                        && (d.is_server_disabled() || d.is_tool_disabled(tool_name))
-                }) {
-                    return Err(McpToolCallError::Failed(format!(
-                        "tool '{tool_name}' on server '{server_name}' is disabled in this conversation"
-                    )));
-                }
-            }
-        } else {
-            let defaults = match crate::modules::mcp::chat_extension::defaults::repository::get_user_defaults(
-                crate::core::Repos.pool(),
-                scope.user_id,
-            )
-            .await
-            {
-                Ok(d) => d,
-                Err(e) => {
-                    return Err(McpToolCallError::Failed(format!(
-                        "tool: could not resolve user MCP defaults: {e}"
-                    )));
-                }
-            };
-            if let Some(defaults) = defaults {
-                let disabled = defaults.get_disabled_servers();
-                if disabled.iter().any(|d| {
-                    d.server_id == server_id
-                        && (d.is_server_disabled() || d.is_tool_disabled(tool_name))
-                }) {
-                    return Err(McpToolCallError::Failed(format!(
-                        "tool '{tool_name}' on server '{server_name}' is disabled in your default MCP settings"
-                    )));
-                }
-            }
-        }
-    }
-
-    let manager = match crate::modules::mcp::client::manager::global() {
-        Some(m) => m,
-        None => {
-            return Err(McpToolCallError::Failed(
-                "MCP session manager not initialized".into(),
-            ));
-        }
-    };
-    // Session selection. The CHAT host (chat_ctx = Some) routes through the SAME
-    // shared machinery the legacy path uses: a `supports_sampling` server gets an
-    // ephemeral `new_with_sampling` session (server→host sampling round-trips), and
-    // the pooled path carries the real branch/message/tool_use context (journaling
-    // parity). Workflow (chat_ctx = None) is unchanged — pooled, no chat context.
-    let session: std::sync::Arc<tokio::sync::RwLock<crate::modules::mcp::client::session::McpSession>> =
-        if let Some(cc) = &chat_ctx {
-            // Lightweight lookup (works for built-in loopback servers too, unlike
-            // `resolve_server_for_session` which is for external un-redacted URLs)
-            // to decide the sampling-vs-pooled branch WITHOUT an early-return that
-            // would skip `call_tool` (and thus the journal recording) for built-ins.
-            let supports_sampling = crate::core::Repos
-                .mcp
-                .get_any_server(server_id)
-                .await
-                .ok()
-                .flatten()
-                .map(|s| s.supports_sampling)
-                .unwrap_or(false);
-            if supports_sampling {
-                let server_row = match manager.resolve_server_for_session(server_id).await {
-                    Ok(s) => s,
-                    Err(e) => return Err(McpToolCallError::Failed(format!("tool: resolve server: {e}"))),
-                };
-                // Sampling server → fresh ephemeral session WITH the host-LLM
-                // handler (parity with legacy `execute_tool`). No pooled fallback:
-                // a pooled sampling session deadlocks the SSE round-trip.
-                let handler: std::sync::Arc<dyn crate::modules::mcp::sampling::handler::SamplingHandler> =
-                    match crate::modules::mcp::sampling::handler::ChatSamplingHandler::new(cc.model_id, scope.user_id).await {
-                        Ok(h) => std::sync::Arc::new(h),
-                        Err(e) => return Err(McpToolCallError::Failed(format!("tool: sampling handler init: {e}"))),
-                    };
-                let mut s = match crate::modules::mcp::client::session::McpSession::new_with_sampling(server_row.clone(), handler).await {
-                    Ok(s) => s,
-                    Err(e) => return Err(McpToolCallError::Failed(format!("tool: sampling session: {e}"))),
-                };
-                s.set_call_context(crate::modules::mcp::tool_calls::models::McpCallContext {
-                    user_id: Some(scope.user_id),
-                    conversation_id: scope.conversation_id,
-                    branch_id: Some(cc.branch_id),
-                    message_id: Some(cc.message_id),
-                    tool_use_id: Some(cc.tool_use_id.clone()),
-                    source,
-                    server_name: server_row.name.clone(),
-                    is_built_in: server_row.is_built_in,
-                    ..Default::default()
-                });
-                std::sync::Arc::new(tokio::sync::RwLock::new(s))
-            } else {
-                match manager
-                    .get_or_create_with_context(
-                        server_id,
-                        scope.user_id,
-                        scope.conversation_id,
-                        Some(cc.branch_id),
-                        Some(cc.message_id),
-                        Some(cc.tool_use_id.clone()),
-                        source,
-                    )
-                    .await
-                {
-                    Ok(s) => s,
-                    Err(e) => return Err(McpToolCallError::Failed(format!("tool: open session: {e}"))),
-                }
-            }
-        } else {
-            match manager
-                .get_or_create_with_context(
-                    server_id,
-                    scope.user_id,
-                    scope.conversation_id,
-                    None, // branch_id
-                    None, // message_id — a workflow run has no chat message
-                    None, // tool_use_id — not an LLM ContentBlock::ToolUse
-                    source,
-                )
-                .await
-            {
-                Ok(s) => s,
-                Err(e) => return Err(McpToolCallError::Failed(format!("tool: open session: {e}"))),
-            }
-        };
-
-    let call = async {
-        let mut guard = session.write().await;
-        // E4: link the recorded `mcp_tool_calls` row to this run — WORKFLOW only.
-        // `mcp_tool_calls.workflow_run_id` FKs `workflow_runs`; a CHAT `run_id` is
-        // the assistant message id, not a workflow run, so setting it would
-        // FK-violate the insert (and silently drop the recording). The chat row is
-        // instead owner/conversation-scoped via the session context.
-        if matches!(
-            source,
-            crate::modules::mcp::tool_calls::models::McpToolCallSource::Workflow
-        ) {
-            guard.set_workflow_run(scope.run_id);
-            // ITEM-16: the idempotency key is persisted as the row's `tool_use_id`
-            // (unused for a workflow call). A chat call's real tool_use_id comes
-            // from the LLM ContentBlock, so don't overwrite it with the key here.
-            if let Some(key) = idempotency_key {
-                guard.set_idempotency_key(key);
-            }
-        }
-        // ITEM-12: carry the reviewer classification onto the journal row.
-        guard.set_review_classification(review_classification);
-        guard.call_tool(tool_name, args, None, None, None).await
-    };
-    let result = tokio::select! {
-        r = call => r,
-        _ = cancel.cancelled() => return Err(McpToolCallError::Cancelled),
-    };
-    match result {
-        Ok(r) => Ok((server_id, r)),
-        Err(e) => Err(McpToolCallError::Failed(format!("tool '{tool_name}': {e}"))),
-    }
-}
 
 #[async_trait]
 impl StepDispatcher for ToolDispatcher {
@@ -1996,6 +1642,7 @@ async fn clear_pending(ctx: &RunContext) -> Result<(), AppError> {
 #[cfg(test)]
 mod tests {
     use super::*;
+    use crate::modules::mcp::agent_tool_call::builtin_server_id_by_name;
     use std::collections::HashMap;
     use std::path::PathBuf;
 
