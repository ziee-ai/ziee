# TESTS — enumerated up front (every ITEM ↔ ≥1 TEST)

Backend-only diff (no `src-app/ui/**` or `src-app/desktop/ui/**` touched) → no `tier: e2e`
required by the frontend gate. No new permission introduced → no `[negative-perm]` spec.

- **TEST-1** (tier: unit) [covers: ITEM-1, ITEM-2, ITEM-4] file: `src-app/server/src/modules/chat/core/services/streaming.rs` — asserts: a co-located assistant message `[thinking, text, tool_use A, tool_use B, tool_use C, tool_result A]` (only 1 of 3 parallel results present) groups to exactly `[Assistant{thinking,text,A,B,C}, Tool{result A + synthesized is_error results for B and C}]`; every tool_use id appears in the following Tool turn; thinking/text stay on the assistant side; synthesized results carry `is_error: Some(true)` and the matching tool_use `name`.
- **TEST-2** (tier: unit) [covers: ITEM-1] file: `src-app/server/src/modules/chat/core/services/streaming.rs` — asserts: a fully-matched parallel batch `[tool_use A, tool_use B, tool_result A, tool_result B]` still groups to exactly one `[Assistant{A,B}, Tool{rA,rB}]` pair (no synthesized results, no regression of the all-matched path).
- **TEST-3** (tier: unit) [covers: ITEM-2] file: `src-app/server/src/modules/chat/core/services/streaming.rs` — asserts: a real failed-tool `tool_result` with `is_error: Some(true)` is preserved verbatim in the Tool turn and paired to its tool_use (failed tools never left unpaired).
- **TEST-4** (tier: unit) [covers: ITEM-3] file: `src-app/server/src/modules/chat/core/services/streaming.rs` — asserts: an assistant message containing ONLY an orphan `tool_result` (no matching tool_use) emits no orphan Tool turn (the result is dropped), and no Assistant-with-tool_use is left dangling.
- **TEST-5** (tier: integration) [covers: ITEM-1, ITEM-3, ITEM-4] file: `src-app/server/tests/chat/assistant_block_grouping_test.rs` — asserts: driving `group_assistant_blocks` via `test_internals`, `assert_valid_tool_pairing` holds for (a) the parallel+partial batch and (b) a multi-iteration-in-one-message history (iter1 use/result, iter2 use×2/result×2) — every Assistant tool_use turn is immediately followed by a Tool turn resolving exactly its ids.
- **TEST-6** (tier: unit) [covers: ITEM-5] file: `src-app/server/src/modules/summarization/engine/summarizer.rs` — asserts: given an outbound array `[System, Assistant{tool_use}, Tool{tool_result}, User, …]` and a `summary.message_count` whose naive `drain` would cut between the Assistant tool_use and its Tool result, `apply_summary_block` snaps the cut forward so the retained history never starts with a `Role::Tool` message (no orphan tool_result) and no retained tool_use loses its result.
- **TEST-7** (tier: unit) [covers: ITEM-3] file: `src-app/server/src/modules/chat/core/services/streaming.rs` — asserts: an orphan `tool_result` arriving MID-STREAM (before its batch completes, `[use A, result X, result A]`) is dropped from the flushed Tool turn (only `A` remains) — the per-flush orphan drop, which the trailing-only orphan test does not exercise (proves DRIFT-1.2; fails pre-fix, which emitted the orphan inside the Tool turn).
- **TEST-8** (tier: unit) [covers: ITEM-1] file: `src-app/server/src/modules/chat/core/services/streaming.rs` — asserts: a duplicate `tool_result` for the same id keeps the FIRST and drops the duplicate, so exactly one result per tool_use reaches the provider.
