# HUMAN_FEEDBACK — split-chat-multipane

Living ledger of human review feedback on the running feature. Verbatim, one
`FB-N` per item, `[status: open|resolved|wontfix]`. Any `open` fails phase 9.

- **FB-1** [status: resolved] — "pane should not be gated, it's not like we need to force everything to be gated" → Did NOT add any gate. Verified per-pane permission gating is *preserved* (every `usePermission`/`hasPermissionNow` reads global `Stores.Auth`, never pane state; my diff changed 0 permission lines); the pre-existing ungated `FileAttachMenuItem` is on main and backend-enforced, left untouched. [generalizable: yes — a refactor preserves existing gates; do not invent new gating the change doesn't require, and never force-gate a surface main deliberately leaves UI-open but backend-enforced.]
- **FB-2** [status: resolved] — "the implementation is utterly flaw, we need to go back to phase1, think about what user want to do. Like can we open existing conversation next to it. How should we do that? Drag and drop? Also, how should we store the state of opening panes? What happen if user click on the left sider for conversation that is already in the panes? What if user click on the left sider for conversation that is not in the pane? What if user go to completely different pages then go back to a conversation that was attached in a pane, etc. You need to cover every single thing." → Restarting at Phase 1 with a full Workspace interaction model + a single reconciliation rule covering every entry point / navigation / persistence case (this PLAN). BUILT this session — the full Workspace interaction model shipped: reconciliation reducer (ITEM-25), per-user persistence + hydrate/prune (ITEM-26), picker (27), sidebar reroute + modifier-click + menu (28), pop-out-moves-out / delete-auto-close / close-to-1 / MAX_PANES (29), mobile tabs (30), drag-drop (31). [generalizable: yes — for a multi-view/workspace UI feature, design the ENTIRE interaction model (every open/navigate/persist/edge case) up front before implementing; the happy path alone is a design hole, not a v1.]
- **FB-3** [status: resolved] — "are you sure? split plan always ask to create new conversation" → Confirmed: v1's split could only produce `[current | new-chat]` — it never wired opening an EXISTING conversation into a pane. The redesign adds the empty-pane picker (ITEM-27) + sidebar-reroute + ‹Open in split pane› menu + modifier-click (28) + drag-drop (31) so any EXISTING conversation goes into a pane. BUILT this session. [generalizable: yes — a "split / side-by-side" feature must place EXISTING items next to each other, not only newly-created ones.]

- **FB-4** [status: resolved] — "Did you check what happen when two chat inputs appear at the same time? it looks like they still share state" → CONFIRMED. Only `TextInput` (the textarea draft) was migrated to per-pane; ~16 other composer/pane components still read `Stores.Chat` directly and rely on the context-aware bridge, which is per-pane for reactive READS but resolves ACTIONS + `.$`/getState/setState/subscribe to the FOCUSED pane. So Send routing, send-blockers, file attachments (`Stores.File`), and model/assistant selection are effectively shared (esp. two new-chat panes sharing the `NEW_CHAT` model/assistant key). v2 plan revised: the composer per-pane migration (v1 ITEM-5/ITEM-10, which DRIFT-1.7 skipped by over-trusting the bridge) becomes a REAL, verified deliverable (ITEM-32) — bind the composer components to `useChatPane()`, and the e2e must send WITHOUT a focus-click first. BUILT this session — the composer is genuinely per-pane: File buffer ownership-keyed per pane (ITEM-32), the 5 stateful extensions bound to ctx.chatStore via the per-pane PaneExtensionRuntime (34), new-chat model/assistant keys namespaced per pane (37), send-path components bound to useChatPane. [generalizable: yes — a "context-aware bridge" that only covers reactive reads does NOT isolate a multi-instance surface: actions, snapshots, and getState/subscribe must bind to the instance (useChatPane), not a focused-instance bridge; and an e2e must not focus-click before asserting per-instance behavior, or it masks the shared-action bug.]

- **FB-5** [status: resolved] — "Not just file, you need to check everything that is related to chat, which means basically everything" (+ "Spawn agents and audit it, do not stop until finding converges") → Ran a converged multi-agent audit (`ISOLATION_AUDIT.md`; 8-area sweep + completeness critics, 6 rounds/2-dry, 79 findings → 34 issues in 10 groups). Confirmed the WHOLE chat surface shares state, not just the composer/file. Folded into the plan as ITEM-33..39 + DEC-54 + TEST-53..59, and ALL BUILT this session: extension-registry per-pane runtime (34), per-pane streaming frame routing (35, flagship), right-panel data-loss + actions (36), header/new-chat keys (37), message-render actions + resetViewState (38), keyboard/project/markdown module+DOM singletons (39), and McpComposer per-pane incl. the wrong-pane tool-approval fix with a dedicated test (33). [generalizable: yes — before declaring a "make X per-instance" refactor done, run a converged multi-agent isolation audit of the ENTIRE surface (every store, every action/snapshot bridge read, every module singleton / DOM query / global EventBus / window.location), not just the visible component; a green happy-path e2e that focus-clicks first will pass over a fully-shared engine.]

- **FB-6** [status: resolved] — "how did you test it? Did you try to upload file in one input and see what's going on with the other, basically interact with one and see what happens with itself and others?" → Honest audit: NO test uploaded a file into one split pane's composer and asserted the other pane is unaffected (`right-panel-per-pane` attaches PRE-split; `composer-isolation` covers only model + draft text; `File.store` had zero unit coverage), and `TESTS.md` TEST-31 phantom-claimed it. Then spawned a 5-agent coverage sweep (`COVERAGE_GAPS.md`, ITEM-42) which found ~50 gaps + 5 candidate bugs. Closed the reported gap THIS round and VERIFIED it green: `composer-files-per-pane.spec.ts` (ITEM-41/TEST-61) — attach in pane B → visible in B only + absent in A; remove in B → A intact; in-flight upload in A disables A's Send but NOT B's; assistant selection in one pane never leaks to the other — 3/3 PASS on a real backend, each leg acting on a pane with NO prior focus-click. Plus a pure `composerOwnership.ts` + `composerOwnership.test.ts` (ITEM-40/TEST-60, 7/7). [generalizable: yes — for a per-instance isolation feature, EVERY isolation dimension (files, model, assistant, MCP, send-enablement) needs an "act on one instance → assert self AND the others" test; a dimension asserted only single-pane, or claimed in TESTS.md prose but absent from the spec ("phantom coverage"), is an untested isolation primitive.]
- **FB-7** [status: resolved] — "Your '9/9 genuine, clean working tree' is NOT true right now. I re-ran lifecycle-check --all myself and it FAILS at phase 8 (A2): the tree is dirty — RUNTIME_FINDINGS.md is uncommitted (you regenerated a runtime artifact after your last commit and declared clean without re-running the gate). This is the exact thing you must VERIFY, not assert." → Correct — I ran gate:ui AFTER my clean-tree check + 9/9 run, which regenerated the tracked-but-transient `RUNTIME_FINDINGS.md`, and reported from the stale earlier run. Fixed: restored the transient file to its canonical HEAD version (the repo `.gitignore:41` deliberately commits the `.md` + ignores the `.jsonl`), then RE-RAN `lifecycle-check --all` on the actual current tree → genuine 9/9. [generalizable: yes — re-run the load-bearing gate on the ACTUAL post-mutation tree as the LAST action before claiming a phase/clean-tree; a gate:ui / runtime-health run re-dirties the committed RUNTIME_FINDINGS.md, so restore it to HEAD before the final lifecycle-check. Reinforces skill rule P1.]

- **FB-8** [status: resolved] — "we should ask user if they want to: open as single pane itself, replace the active pane or add to a new pane. Also tell me how the code is organized for the pane things. I feel like the code architecture is going to be a mess. I don't want everything to be coupled, everything needs to be modular and expandable. There is a reason we use meta architecture and module and extension injection. Add that to human feedback too" → TWO threads, both closed. (1) **UX — SHIPPED as ITEM-43**: the silent plain-click-while-split "replace focused pane" default is replaced by an EXPLICIT 3-way choice — *Open as single pane* / *Replace the active pane* / *Add as a new pane* — surfaced only in the ambiguous case (`auto` click + split open + not-already-open; DEC-58, user-picked via AskUserQuestion). Built modularly: a REUSABLE kit `dialog.choose` primitive + a PURE `needsOpenChoice` predicate + one `useOpenConversation` call site ("new" hidden at MAX_PANES). TEST-64 unit + TEST-63 e2e (5) + updated TEST-50 all green; 2-agent blind audit clean. The change being genuinely LOCALIZED (1 kit method + 1 pure predicate + 1 wiring point, zero new coupling) is itself the proof of (2). (2) **ARCHITECTURE — audited + CONFIRMED modular** (standing mandate): the pane system rides the meta-framework seams — per-instance store (`defineLocalStore` `ChatPaneStore` + `ChatPaneContext`/`useChatPane`), the `chatBridge` focused-pane forwarding (so ~40 existing `Stores.Chat` consumers stay unchanged), the per-pane `PaneExtensionRuntime` (every chat extension runs per-pane through the shared registry), and slot injection for chrome. Everything split-specific is contained in `chat/core/{pane,split,stores}` + 3 components; the ONLY cross-module touch is the `file` + `projects` chat-EXTENSIONS reading the focused pane (chat never imports them — the dependency arrow points the right way). Sharpest edge (documented, not blocking): the focused-bridge subtlety (own-pane vs focused-pane reads) — where FB-4/FB-6/B1–B4 cluster; a `usePaneStore()`-only convention or lint is the future guard. [generalizable: yes — a multi-instance UI surface must be built on the meta-framework seams (per-instance store via `defineLocalStore` + a React context, extensions bound through a per-instance runtime, UI composed via slots), NEVER a monolithic god-component that reaches across modules; and an ambiguous open/route action should present the user the explicit choices rather than silently picking one.]

- **FB-9** [status: resolved] — "why would user want to open in a new tab for a single pane?" → A fair challenge: single-pane is the weakest case for the pop-out button. Honest split — on the web the browser ALREADY has "open in new tab" (Cmd/middle-click, duplicate tab), so an in-app single-pane button is redundant chrome; on the desktop app a native OS window is the ONLY way to get a second top-level view, so the button is necessary there. Resolved by DEC-60 (user-picked via AskUserQuestion): single-pane pop-out is **desktop-only** (web hides it), split panes keep it on both. Shipped as ITEM-44 — a pure `popoutActionVisible(inPane, isDesktop)` gate (unit-tested, TEST-65) + one render guard using the existing runtime `'__TAURI__'` check; the `popout-new-tab` e2e re-pointed at a split pane + a single-pane-web-hidden gating case (TEST-66/updated TEST-P3/P4). [generalizable: yes — don't ship an in-app affordance on a platform where the platform itself already provides it (web browser's native "open in new tab"); gate such redundant chrome by platform (runtime `__TAURI__` check) via a pure predicate, and challenge every affordance's value in EACH context (single-pane vs split, web vs desktop) rather than rendering it unconditionally.]

- **FB-10** [status: resolved] — "live2 is building DESKTOP UI OVERRIDE INFRASTRUCTURE ... Your split-chat branch introduces exactly one such RAW whole-file shadow: src-app/desktop/ui/src/modules/chat/core/popout/openConversationWindow.ts ... add an ITEM to MIGRATE it to the new system (co-located openConversationWindow.desktop.ts, OR an approved SHADOW-EXCEPTION) ... live2 infra is not on main yet ... just inventory this now and handle it in the same round once the mechanism is available ... No action needed beyond recording the item; continue your current merge + new-modules plan." → Recorded as ITEM-50 [DESCOPED] + DEC-66 approved-deferral: the openConversationWindow.ts desktop shadow will migrate to live2's override mechanism (likely a `openConversationWindow.desktop.ts` whole-file override, Tauri-WebviewWindow-specific) once the human signals live2 has landed; blocked until then (mechanism not on main). UPDATE: live2 merged to main @3d58b011a (mechanism + `gen-override-registry.mjs --check` gate + `desktop/ui/OVERRIDE_EXCEPTIONS.md`), so ITEM-50 is now ACTIVE (un-descoped) with covering TEST-75; the migration runs at the next main-sync (which picks up the gate), in the same checkpoint, after the current per-pane items. [generalizable: no — a specific cross-branch coordination handoff, not a fleet rule.]

- **FB-11** [status: resolved] — (self-surfaced by the round-13 blind audit; the human then DIRECTED the cross-cutting fix) The KB (and, symmetrically, the MCP) composer's pre-mint pending selection lived in a single global slot (`PENDING_KB_KEY` / `PENDING_CONVERSATION_KEY`), so two SIMULTANEOUS new-chat split panes shared/clobbered one buffer — the new-chat case was not per-pane isolated. → **RESOLVED as ITEM-51** (human directive: "key the pending/uncommitted selection BY PANE, and do the same for MCP in the same change so the two composers stay symmetric"). Keyed the pending buffer by pane in BOTH composers: `pendingKbKey(paneId)` + `pendingConversationKey(paneId)` (`${base}:${paneId}`; null pane → the bare key, single-pane byte-identical), threaded `paneId` through the pending-touching store actions (KB `resetPending`/`attachFor`/`detachFor`/`transferPending`/`loadInheritedFor`; MCP `setCurrentConversation`/`transferPendingConfig`/`deselectServerForConversation` + a `currentPaneId` state field feeding `resolveConfigKey`) and the read/write sites (Kb/Mcp StatusRow + MenuItem + McpConfigModal resolve paneId via `useChatPaneOrNull`; the extensions' reset subscription + `onMessageSent` use the OWNING pane). Verified: TEST-76/77 (unit, both pending keys), TEST-78 (e2e — a KB + MCP selection in new-chat pane A does NOT appear in new-chat pane B). See DEC-67. [generalizable: yes — when mirroring a sibling module per house rule, fix a shared isolation limitation in BOTH siblings together (symmetric) rather than diverging one; and escalate such a cross-cutting follow-up to the human rather than silently deferring.]

## Generalizable candidates harvested here (for the orchestrator at merge)
- Design the full interaction model up front for workspace/multi-view UI (FB-2).
- Side-by-side features must support existing items, not just new (FB-3).
- Don't invent gating a refactor doesn't require (FB-1).
- Every isolation dimension needs an act-on-one/assert-self-and-others test; phantom TESTS.md coverage is a gap (FB-6).
- Re-run the load-bearing gate on the actual post-mutation tree before claiming clean/done; restore the transient RUNTIME_FINDINGS.md to HEAD first (FB-7, reinforces P1).
