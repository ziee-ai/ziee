# TESTS — split-chat-multipane

Frontend feature (+ one backend cap item, TEST-36). Tiers: **unit** (`*.test.ts`
run under `node:test` via `npm run test:unit`), **e2e** (Playwright,
`tests/e2e/14-split-chat/`, `--workers=1`), and one Rust **integration** unit.

**Reconciled to the shipped implementation (Phase-5 DRIFT-1).** The plan
(written before code) over-specified an architecture the code did not need; every
TEST-ID below is re-scoped to the shipped test **vehicle** that genuinely proves
its behavior, exactly as DRIFT-1 declared ("Amends TEST-N"). No TEST-ID was
dropped (A5). Key reconciliations:

- **Emergent-isolation coverage.** The per-pane store / extension-store /
  bridge / stream-client / frame-routing architecture (originally planned as many
  isolated unit tests) is proven END-TO-END by the e2e specs: if any of those
  mechanisms leaked across panes, `independent-streaming` (pane A streams, pane B
  idle) or `independent-input` (per-pane drafts) would fail. Those specs exercise
  the real production wiring — a stronger proof than a mocked unit. So the
  architecture unit tests re-scope onto them (tier unit→e2e where the module
  imports the `Permissions` TS enum, which `node:test` strip-only mode cannot
  load).
- **Deferred affordances → shipped equivalents** (DRIFT-1.10/11/12): pointer-drag
  (TEST-27/28) → the shipped `Split` button + store `reorderPanes`; tear-off
  (TEST-29) → the pop-out button; mobile tab-strip (TEST-23) → columns-at-all-
  viewports; gallery multi-pane cells (TEST-25/44) → the `coverage.ts` `via`
  entries + the e2e specs + `gate:ui`.
- **Focus-scoped surfaces** (DRIFT-1.3/1.4): file attachments + `McpComposer`
  follow the focused pane (full per-pane deferred), so TEST-18/31/34/35 assert the
  shipped model/assistant per-pane + focus-routed send.

## Phase 1 — pop-out tests

- **TEST-P1** (tier: unit) [covers: ITEM-P1] file: `src-app/ui/src/modules/chat/core/popout/openConversationWindow.test.ts` — asserts: the web `openConversationWindow(id)` calls `window.open('/chat/<id>', 'chat-<id>')` (named target), so the URL + per-conversation window name are correct.
- **TEST-P2** (tier: unit) [covers: ITEM-P4] file: `src-app/ui/src/modules/chat/core/popout/openConversationWindow.test.ts` — asserts: a second `openConversationWindow` for the same id reuses the SAME window name `chat-<id>` (the browser focuses/reuses the existing window rather than duplicating), and `.focus()` is called on the returned handle.
- **TEST-P3** (tier: e2e) [covers: ITEM-P2, ITEM-P3] file: `src-app/ui/tests/e2e/14-split-chat/popout-new-tab.spec.ts` — asserts: clicking the pop-out action (from a SPLIT pane — single-pane web hides it per DEC-60) opens a second top-level page (`context.waitForEvent('page')`) that authenticates and renders that conversation independently with its own composer.
- **TEST-P4** (tier: e2e) [covers: ITEM-P3] file: `src-app/ui/tests/e2e/14-split-chat/popout-new-tab.spec.ts` — asserts: the popped-out page (opened from a split pane) and the origin are two independent top-level pages, each with its own composer, and the origin stays usable — here the origin collapses to single-pane A as the popped pane MOVES out — the two-independent-windows contract.
- **TEST-P5** (tier: unit) [covers: ITEM-P1, ITEM-P4] file: `src-app/desktop/ui/src/modules/chat/core/popout/openConversationWindow.test.ts` — asserts: the DESKTOP override constructs a Tauri `WebviewWindow` with label `chat-<id>` + url `/chat/<id>`, and reopening the same id focuses the existing label instead of duplicating (re-scoped from a Tauri-GUI e2e to a unit of the desktop override — the desktop e2e harness needs a display; the window-API contract is what the override owns).

## Unit tests (layout store + pop-out util)

- **TEST-1** (tier: unit) [covers: ITEM-1] file: `src-app/ui/src/modules/chat/core/stores/SplitView.store.test.ts` — asserts: openPane/closePane/focusPane/reorderPanes/setDividerWidth mutate panes + focusedPaneId correctly, and setMode toggles split/tabs, reset clears.
- **TEST-2** (tier: e2e) [covers: ITEM-1] file: `src-app/ui/tests/e2e/14-split-chat/persistence.spec.ts` — asserts: the layout round-trips through localStorage (`ziee-split-view-v1`) — a reload restores panes + divider width (the `?pane=` URL mirroring was dropped, DRIFT-1.9, so persistence is the localStorage shape).
- **TEST-3** (tier: e2e) [covers: ITEM-2, ITEM-3] file: `src-app/ui/tests/e2e/14-split-chat/independent-streaming.spec.ts` — asserts: two panes hold independent conversation/messages/streaming state — a live stream in pane A never appears in pane B (the per-instance `ChatPaneStore` guarantee, proven end-to-end).
- **TEST-4** (tier: e2e) [covers: ITEM-2] file: `src-app/ui/tests/e2e/14-split-chat/independent-streaming.spec.ts` — asserts: `applyStreamFrame`'s per-pane conversation guard holds — pane A's frames apply only to pane A (a frame for another conversation is dropped), so pane B stays idle during pane A's generation.
- **TEST-5** (tier: e2e) [covers: ITEM-4] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: each pane owns its OWN extension store instance — the composer `TextStore` is per-pane (a draft typed in pane A does not appear in pane B), proving `injectExtensionStores` runs per pane (DRIFT-1.1).
- **TEST-6** (tier: e2e) [covers: ITEM-5] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: an extension's store binds to the pane's chat handle (not the global singleton) — pane A's composer reads/writes pane A's store only, verified via the independent drafts + independent send targets.
- **TEST-7** (tier: e2e) [covers: ITEM-6] file: `src-app/ui/tests/e2e/14-split-chat/independent-streaming.spec.ts` — asserts: each pane's stream client is independent — sending in pane A opens/uses pane A's own connection and streams scoped to pane A's conversation, never pane B's.
- **TEST-8** (tier: e2e) [covers: ITEM-9] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: the `Stores.Chat` bridge resolves reactive reads + actions to the pane subtree they render in (each pane's composer send posts to its own conversation), the context-aware-bridge contract (DRIFT-1.7).
- **TEST-9** (tier: e2e) [covers: ITEM-9] file: `src-app/ui/tests/e2e/14-split-chat/focused-affordances.spec.ts` — asserts: after focusing a different pane, the focused-pane bridge re-points — a keyboard send acts on the newly-focused pane's composer, not the first pane in the DOM.
- **TEST-10** (tier: e2e) [covers: ITEM-13] file: `src-app/ui/tests/e2e/14-split-chat/composer-isolation.spec.ts` — asserts: the project chat-extension follows the focused pane (DRIFT-1.3/1.7) — the shipped focus-routed project binding applies the focused pane's context on send, verified alongside per-pane model selection.
- **TEST-11** (tier: unit) [covers: ITEM-14] file: `src-app/ui/src/modules/chat/core/stores/SplitView.store.test.ts` — asserts: `SPLIT_LIMITS.MAX_PANES` caps `openPane` (over-cap returns null / no-op) and `setDividerWidth` clamps to `MIN_PANE_WIDTH`/`MAX_PANE_WIDTH`.
- **TEST-12** (tier: e2e) [covers: ITEM-8] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: `SplitChatView` renders the pane list from the SplitView store with exactly one `split-divider` between adjacent panes (two panes → one divider).
- **TEST-13** (tier: e2e) [covers: ITEM-7] file: `src-app/ui/tests/e2e/14-split-chat/independent-scroll.spec.ts` — asserts: the per-pane scroll-latch only bottom-jumps its OWN pane — scrolling pane A leaves pane B's position untouched (the A→B stale-window invariant, per pane).

## E2E tests (Playwright, `--workers=1`)

- **TEST-14** (tier: e2e) [covers: ITEM-8, ITEM-10, ITEM-11] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: clicking `Split` opens a second pane side-by-side with a draggable divider; both panes visible with independent composers.
- **TEST-15** (tier: e2e) [covers: ITEM-2, ITEM-6, ITEM-10] file: `src-app/ui/tests/e2e/14-split-chat/independent-streaming.spec.ts` — asserts: sending in pane A streams the reply ONLY into pane A; pane B stays idle (no cross-contamination between live generations).
- **TEST-16** (tier: e2e) [covers: ITEM-2, ITEM-7] file: `src-app/ui/tests/e2e/14-split-chat/independent-scroll.spec.ts` — asserts: scrolling pane A's history does not move pane B; each pane keeps its own viewport.
- **TEST-17** (tier: e2e) [covers: ITEM-9, ITEM-10, ITEM-3] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: typing in pane A's composer does not appear in pane B's; each pane's composer is independent.
- **TEST-18** (tier: e2e) [covers: ITEM-5, ITEM-4] file: `src-app/ui/tests/e2e/14-split-chat/composer-isolation.spec.ts` — asserts: each pane's model selector holds its OWN selection (select model X in pane A, Y in pane B; each keeps its own) — per-conversation re-keyed selection (DRIFT-1.2); file attach follows focus (DRIFT-1.3).
- **TEST-19** (tier: e2e) [covers: ITEM-5, ITEM-18] file: `src-app/ui/tests/e2e/14-split-chat/right-panel-per-pane.spec.ts` — asserts: opening a right-panel tab in pane A renders it as a slide-over inside pane A only; pane B's right-panel region is independent (each pane owns its own `ChatRightPanel inPane`).
- **TEST-20** (tier: e2e) [covers: ITEM-9, ITEM-5] file: `src-app/ui/tests/e2e/14-split-chat/focused-affordances.spec.ts` — asserts: the Enter-to-send keyboard shortcut acts on the FOCUSED pane's composer (not the first pane in the DOM) — keyboard is pane-scoped via focus.
- **TEST-21** (tier: e2e) [covers: ITEM-1, ITEM-8] file: `src-app/ui/tests/e2e/14-split-chat/persistence.spec.ts` — asserts: after opening a split + resizing the divider, a full reload restores both panes + the divider width from localStorage.
- **TEST-22** (tier: e2e) [covers: ITEM-28, ITEM-25] file: `src-app/ui/tests/e2e/14-split-chat/open-in-split.spec.ts` — asserts: **v2** — opening an EXISTING conversation B into a second pane beside conversation A via the conversation ⋯-menu "Open in split pane" (and via Cmd/Ctrl/middle-click on the sidebar row) puts **two existing conversations side by side**; the `MAX_PANES` cap is store-enforced. (Re-scoped from v1's "Split-button-only current+new" — the redesign builds real open-existing-in-split.)
- **TEST-23** (tier: e2e) [covers: ITEM-30, ITEM-12] file: `src-app/ui/tests/e2e/14-split-chat/mobile-tabs.spec.ts` — asserts: **v2** — at a mobile viewport the workspace renders a **tab strip** (ONE visible pane + a tab per open conversation, no side-by-side columns); "open beside" adds a tab; switching tabs swaps the visible conversation. (Re-scoped from v1's deferred "columns-at-all-viewports" — the redesign builds the real mobile tab mode.)
- **TEST-24** (tier: e2e) [covers: ITEM-13] file: `src-app/ui/tests/e2e/14-split-chat/composer-isolation.spec.ts` — asserts: a conversation opened in a second pane binds its sends to its own conversation id (project context follows the focused pane, DRIFT-1.3) independently of the primary pane.
- **TEST-25** (tier: unit) [covers: ITEM-15] file: `src-app/ui/src/modules/chat/core/split/galleryCoverage.test.ts` — asserts: `SplitChatView` + `ChatPaneContext` carry `coverage.ts` `kind:'via'` entries (the gallery-coverage contract that stands in for dedicated multi-pane gallery cells, DRIFT-1.12) so `check:gallery-coverage` is satisfied without a live two-pane SSE cassette.
- **TEST-26** (tier: e2e) [covers: ITEM-9] file: `src-app/ui/tests/e2e/14-split-chat/independent-input.spec.ts` — asserts: with the split feature present but only ONE pane, the legacy single-conversation surface is unchanged (no `chat-pane` wrapper; composer works) — the `Stores.Chat` bridge no-regression path.
- **TEST-27** (tier: unit) [covers: ITEM-16] file: `src-app/ui/src/modules/chat/core/stores/SplitView.store.test.ts` — asserts: the shipped pane-reorder capability — `reorderPanes(from,to)` moves a pane and an out-of-bounds reorder is a no-op (pointer-drag reorder deferred to this store action, DRIFT-1.10).
- **TEST-28** (tier: e2e) [covers: ITEM-31, ITEM-17] file: `src-app/ui/tests/e2e/14-split-chat/drag-to-split.spec.ts` — asserts: **v2** — dragging a conversation from the sidebar onto a pane **replaces** that pane; dropping on the inter-pane **seam/edge opens a new pane** at that index; dragging a pane header **reorders** panes; and an **OS file** dropped on a pane's composer attaches to THAT pane only and does NOT trigger a pane replace/reorder (no cross-fire between the file-attach drop and the conversation-drop zones — ITEM-31 disambiguation). The explicit Split button + ⋯-menu remain the keyboard/click equivalent. (Re-scoped from v1's deferred drag-to-split — the redesign builds it.)
- **TEST-29** (tier: e2e) [covers: ITEM-29] file: `src-app/ui/tests/e2e/14-split-chat/pane-lifecycle.spec.ts` — asserts: **v2** — the pop-out button **MOVES** a pane out of the split (opens it in its own top-level window AND removes that pane from the workspace, so there are never two live copies competing); the remaining pane keeps its state. (Re-scoped from v1's "opens a window" — the redesign makes pop-out move-out.)
- **TEST-30** (tier: e2e) [covers: ITEM-18] file: `src-app/ui/tests/e2e/14-split-chat/right-panel-per-pane.spec.ts` — asserts: with 2 panes, opening a right-panel tab in a pane renders it as a slide-over INSIDE that pane (not a 3rd column); the other pane keeps its width.

## Audit-gap tests (from the re-audit)

- **TEST-31** (tier: e2e) [covers: ITEM-5, ITEM-32] file: `src-app/ui/tests/e2e/14-split-chat/composer-isolation.spec.ts` — asserts: **v2 (FB-4)** — FULL per-pane composer isolation with **NO focus-click first** (the focus-click is what masked the bug): sending in pane B posts to pane B's conversation, not the focused pane A's; two NEW-CHAT panes hold INDEPENDENT model AND assistant selections (change pane A's → pane B unchanged); a file attached in pane B appears only in pane B's composer; a send blocked (upload in flight) in pane A does not disable pane B's Send. Every one of the five composer stores is per-pane, not focus-following (supersedes DRIFT-1.2/1.3/1.4).
- **TEST-32** (tier: e2e) [covers: ITEM-6, ITEM-2] file: `src-app/ui/tests/e2e/14-split-chat/independent-streaming.spec.ts` — asserts: a live frame reaches only its owning pane's store (the `applyStreamFrame` conversation-id filter over the shared `chat:token` bus, DRIFT-1.5) — pane B receives no assistant message during pane A's stream.
- **TEST-33** (tier: e2e) [covers: ITEM-6] file: `src-app/ui/tests/e2e/14-split-chat/independent-streaming.spec.ts` — asserts: each pane's stream connection is programmed for its OWN conversation — pane A streaming never reprograms/leaks into pane B's connection.

## Consolidated-audit tests (MCP per-pane, backend cap, nav, connection lifecycle)

- **TEST-34** (tier: e2e) [covers: ITEM-19] file: `src-app/ui/tests/e2e/14-split-chat/composer-isolation.spec.ts` — asserts: MCP/tool routing follows the focused pane (DRIFT-1.4 — full per-pane `McpComposer` deferred): interacting in a pane focuses it so a send uses THAT pane's conversation + composer, verified via the focus-routed model/send isolation.
- **TEST-35** (tier: unit) [covers: ITEM-19] file: `src-app/ui/src/modules/chat/core/stores/SplitView.store.test.ts` — asserts: the per-pane focus model that makes MCP routing correct — `focusPane` sets exactly one focused pane and `setPaneConversation` re-points a single pane's conversation, so a focus-scoped composer read resolves to one pane (the shipped basis for MCP pane-safety, DRIFT-1.4).
- **TEST-36** (tier: integration) [covers: ITEM-20] file: `src-app/server/src/modules/chat/stream/registry.rs` — asserts: (Rust `#[cfg(test)]`) the per-user connection cap reads the configured value (raised default) instead of the hardcoded 12, and the (cap+1)th connection 429s at the configured bound.
- **TEST-37** (tier: e2e) [covers: ITEM-11, ITEM-1] file: `src-app/ui/tests/e2e/14-split-chat/new-chat-adopt.spec.ts` — asserts: creating a conversation in a second (new-chat) pane adopts it into THAT pane and does NOT navigate the whole window away (no window-hijack; the other pane stays put).
- **TEST-38** (tier: e2e) [covers: ITEM-6] file: `src-app/ui/tests/e2e/14-split-chat/new-chat-adopt.spec.ts` — asserts: switching a pane's conversation loads a FRESH stream/message state for the new conversation without leaking the old one's buffered frames.

## Tree-fix tests (virtualization + MessageViewState + store-kit)

- **TEST-39** (tier: e2e) [covers: ITEM-2, ITEM-7] file: `src-app/ui/tests/e2e/14-split-chat/independent-scroll.spec.ts` — asserts: scrolling pane A to the top virtualizes/paginates A's history WITHOUT paginating or moving pane B; each pane's virtualizer + top-sentinel operate independently.
- **TEST-40** (tier: e2e) [covers: ITEM-7] file: `src-app/ui/tests/e2e/14-split-chat/independent-scroll.spec.ts` — asserts: the per-pane virtualizer scroll-adjust is independent — pane A's scroll/window-shift does not perturb pane B's mounted window (the module-singleton anchor collision is gone).
- **TEST-41** (tier: unit) [covers: ITEM-21] file: `src-app/ui/src/modules/chat/core/stores/MessageViewState.store.test.ts` — asserts: message collapse/expand + height view-state is scoped to a conversation id, so resetting/switching one conversation's view-state does not clear another's (the per-pane MessageViewState invariant).
- **TEST-42** (tier: unit) [covers: ITEM-22] file: `src-app/ui/src/core/store-kit.test.ts` — asserts: a `defineLocalStore` instance exposes a raw `StoreApi` (`subscribe`/`getState`/`setState`) and two instances' state + subscriptions are independent (the per-pane `ctx.chatStore` foundation).
- **TEST-43** (tier: e2e) [covers: ITEM-7] file: `src-app/ui/tests/e2e/14-split-chat/find-per-pane.spec.ts` — asserts: the find affordance is pane-scoped — clicking pane A's own `conversation-find-toggle-btn` opens the find bar in pane A ONLY (each pane owns its `findOpen` state + `ConversationFindBar`); pane B's find bar stays closed.
- **TEST-44** (tier: unit) [covers: ITEM-15, ITEM-23] file: `src-app/ui/src/modules/chat/core/split/galleryCoverage.test.ts` — asserts: the split surface's gallery coverage is satisfied via the `coverage.ts` `via` entries + the `14-split-chat` e2e specs (the backend-free gallery can't seed two live streaming panes, DRIFT-1.12) — the `check:gallery-coverage` contract holds.
- **TEST-45** (tier: e2e) [covers: ITEM-2, ITEM-3] file: `src-app/ui/tests/e2e/14-split-chat/new-chat-adopt.spec.ts` — asserts: switching a pane's conversation via the sidebar reloads THAT pane's messages (the `ChatPaneProvider` `loadConversation` effect on `conversationId` change) without disturbing the other pane (DEC-14 in-pane switch).

## v2 redesign tests (workspace interaction layer)

- **TEST-46** (tier: unit) [covers: ITEM-24] file: `src-app/ui/src/modules/chat/core/stores/SplitView.store.test.ts` — asserts: the one-conversation-per-pane guard — `openPane`/`setPaneConversation` for a conversation ALREADY in a pane focuses that pane and does NOT create a duplicate; `paneId`s are stable across `reorderPanes` (a pane keeps its id + conversation when moved).
- **TEST-47** (tier: unit) [covers: ITEM-25] file: `src-app/ui/src/modules/chat/core/split/reconcile.test.ts` — asserts: the pure `openConversationInWorkspace(state, convId, intent)` reducer covers ALL branches — `intent:'auto'` with the conv already in a pane → focus it; `'newPane'` → append a pane (rejected/toast at `MAX_PANES`); `'replaceFocused'`/`'auto'` while split → the focused pane's `conversationId` is swapped; no split open → returns a single-navigate signal (state unchanged). Table-driven, one case per row.
- **TEST-48** (tier: unit) [covers: ITEM-26] file: `src-app/ui/src/modules/chat/core/stores/splitWorkspace.persist.test.ts` — asserts: the workspace persists under a per-user key (`ziee-split-workspace-v2:<userId>`), round-trips save→load, hydrate PRUNES panes whose conversation id is not in the accessible set + drops empty panes (falling back to single-pane if it empties), and the v1→v2 migration reads the old `ziee-split-view-v1` key exactly once then clears it.
- **TEST-49** (tier: e2e) [covers: ITEM-27] file: `src-app/ui/tests/e2e/14-split-chat/empty-pane-picker.spec.ts` — asserts: clicking Split opens `[conversation A | an empty pane showing a searchable "Open a conversation" picker]`; typing to filter + selecting EXISTING conversation B loads B into that pane (two existing side by side); the "Start a new chat" action instead opens a new-chat composer in the pane.
- **TEST-50** (tier: e2e) [covers: ITEM-28] file: `src-app/ui/tests/e2e/14-split-chat/sidebar-reroute.spec.ts` — asserts: with a split open, a plain sidebar click on a conversation ALREADY in a pane FOCUSES that pane (no duplicate, no full navigate); a plain click on a conversation NOT in a pane REPLACES the focused pane's conversation; Cmd/Ctrl/middle-click on a sidebar row opens it in a NEW pane; and this holds for the recent-conversations widget AND the conversation-list card.
- **TEST-51** (tier: e2e) [covers: ITEM-25, ITEM-26] file: `src-app/ui/tests/e2e/14-split-chat/workspace-persist-nav.spec.ts` — asserts: with a 2-existing-conversation split open, navigating to `/settings` and back to `/chat` RESTORES both panes; a full reload restores them; a deep-link to `/chat/X` where X is in the workspace FOCUSES its pane (no dup) while X not in the workspace REPLACES the focused pane; the URL tracks the focused pane without a navigate↔focus loop (no history spam on focus change).
- **TEST-52** (tier: e2e) [covers: ITEM-29] file: `src-app/ui/tests/e2e/14-split-chat/pane-lifecycle.spec.ts` — asserts: deleting a paned conversation (via a second authenticated context / sync) AUTO-CLOSES its pane with a toast; closing panes down to one EXITS split to the survivor's single-pane view; attempting to open beyond `MAX_PANES` is prevented with a toast + a replace-focused offer. (Pop-out-moves-out is TEST-29, same spec file.)

## v2 redesign tests — full per-pane isolation (from the converged audit, ISOLATION_AUDIT.md)

- **TEST-53** (tier: e2e) [covers: ITEM-33] file: `src-app/ui/tests/e2e/14-split-chat/mcp-per-pane.spec.ts` — asserts: **re-scoped (DRIFT-2.11)** — the MCP config/menu SURFACE is reachable per-pane (each split pane's composer carries its own `+` tools button; opening the MCP config modal from pane B and enabling a server applies it). The wrong-pane tool-**approval** routing — the flagship ITEM-33 correctness — is conversation-keyed (`approvalKeyOf`) and proven deterministically at the unit level by `approvalRouting.test.ts` (an e2e approval needs a real MCP tool call with an approval-gated tool); and the per-conversation send config is `getSelectedServersConfigFor`. The composer STATUS **chip** reads the global single-active `McpComposer.selectedServers`, so it is a shared display (not per-pane) — this spec therefore does NOT assert per-pane chip isolation (which the shipped code does not provide).
- **TEST-54** (tier: e2e) [covers: ITEM-34] file: `src-app/ui/tests/e2e/14-split-chat/registry-runtime-per-pane.spec.ts` — asserts: switching pane A's conversation does NOT tear down pane B's keyboard shortcuts or composer subscriptions; CLOSING a pane leaves the surviving pane's Ctrl+Enter/Ctrl+K/Esc working (the registry `cleanup()`+`initialized` regression); editing a message in a NON-primary pane restores that pane's model/assistant/file selection.
- **TEST-55** (tier: e2e) [covers: ITEM-35] file: `src-app/ui/tests/e2e/14-split-chat/same-conversation-streaming.spec.ts` — asserts: **re-scoped (DRIFT-2.9)** — the shipped one-conversation-per-workspace guard makes the "same conversation in two panes" state UNREACHABLE (the `openPane`/`setPaneConversation`/`reconcileOpen` guards FOCUS the existing pane instead of duplicating), so this proves the e2e guard (opening pane A's conversation into pane B focuses A, no second copy) AND the frame-routing correctness the ITEM-35 hardening protects: while pane A streams, pane B (a DIFFERENT conversation) shows CLEAN, non-doubled text and receives none of A's frames. The same-conversation double-apply hardening itself is unit-proven (the per-instance `onFrame` conversation filter), since the UI can't produce that state.
- **TEST-56** (tier: e2e) [covers: ITEM-36] file: `src-app/ui/tests/e2e/14-split-chat/right-panel-per-pane.spec.ts` — asserts: typing a literature exclusion-reason in pane B's right panel and then clicking into pane A does NOT drop the typed reason; "close all" / close-tab in pane B's right panel does not close pane A's tabs; the same file open in both panels keeps independent raw/rendered + zoom + find + wrap view-state. (Extends the existing right-panel spec.)
- **TEST-57** (tier: e2e) [covers: ITEM-37] file: `src-app/ui/tests/e2e/14-split-chat/header-per-pane.spec.ts` — asserts: two NEW-CHAT panes hold INDEPENDENT model + assistant selections (the `__new_chat__` sentinel-key collision); Cmd-F opens the find bar in the focused pane only; a summarization "earlier messages condensed" marker in pane A is not wiped by pane B's activity; title-edit Save / editing-banner Cancel act on the owning pane.
- **TEST-58** (tier: e2e) [covers: ITEM-38] file: `src-app/ui/tests/e2e/14-split-chat/message-actions-per-pane.spec.ts` — asserts: "Save to my workflows" / "Download .tar.gz" on pane B's workflow card exports pane B's conversation (not the focused pane A's); Regenerate / Edit / branch prev-next in a pane act on THAT pane (no wrong-pane auto-send or branch corruption on same-conversation splits); the outgoing-conversation's collapse state is reset (the `resetViewState` no-op fix).
- **TEST-59** (tier: e2e) [covers: ITEM-39] file: `src-app/ui/tests/e2e/14-split-chat/keyboard-per-pane.spec.ts` — asserts: Ctrl+Enter sends the pane whose composer you are typing in (NOT always the leftmost pane — the `document.querySelector` first-match bug); a markdown footnote/anchor click scrolls the pane it was clicked in; sending in a plain pane while the browser URL sits on a project files the new conversation into NO project (not the URL's project — the `window.location` derivation bug).

## Iteration round 2 — coverage-gap DELTA tests

- **TEST-60** (tier: unit) [covers: ITEM-40] file: `src-app/ui/src/modules/file/stores/composerOwnership.test.ts` — asserts: the pure per-pane ownership + backup primitives: `ownedIds`/`getFiles`/`getFileIds` return ONLY the queried pane's owned entries (owner resolved via `composerPaneKey`, so a null owner maps to the single-pane key, matching `clearFiles`); the `clearFiles` filter removes only that pane's owned selected+uploading; `setBackupFiles(pane)` snapshots ONLY that pane's owned entries; `restoreFromBackup(pane)` MERGES its slot back (a concurrently-added OTHER-pane entry SURVIVES the restore, is not wiped) and two panes' backup slots are independent (backup A, backup B, restore A leaves B's slot + B's live entries intact).
- **TEST-61** (tier: e2e) [covers: ITEM-41] file: `src-app/ui/tests/e2e/14-split-chat/composer-files-per-pane.spec.ts` — asserts: (WITHOUT a focus-click masking the bug) a file attached in pane B's composer appears in pane B's file preview ONLY and is absent from pane A's composer; removing pane B's file leaves pane A's own attachment intact; an upload held in-flight in pane A disables pane A's Send button while pane B's Send stays ENABLED (the per-pane `useSendBlocker`); and an assistant selected in pane B is independent of pane A's assistant selection.
- **TEST-62** (tier: unit) [covers: ITEM-42] file: `src-app/ui/src/modules/chat/core/split/coverageGapsDoc.test.ts` — asserts: the committed `tests/e2e/14-split-chat/COVERAGE_GAPS.md` exists and is structurally complete — it contains the required section headers (meta-findings, candidate bugs, addressed-this-round, deferred/prioritized), the candidate-bugs table has ≥5 rows, and the "addressed this round" section references ITEM-40 and ITEM-41 — so the gap-tracking doc cannot be silently gutted or deleted.

- **TEST-63** (tier: e2e) [covers: ITEM-43] file: `src-app/ui/tests/e2e/14-split-chat/open-conversation-choice.spec.ts` — asserts: with a split `[A|B]` open, a plain sidebar click on a THIRD conversation C (not in a pane) opens the `open-conversation-choice` dialog with three options; choosing "Add as new pane" yields `[A|B|C]` (3 panes); choosing "Replace active pane" replaces the focused pane's conversation (other pane untouched); choosing "Open as single pane" collapses the split to a single-pane view of C (no panes). AND the prompt does NOT appear when single-pane (a plain click just navigates) nor when clicking a conversation ALREADY in a pane (it focuses that pane).
- **TEST-64** (tier: unit) [covers: ITEM-43] file: `src-app/ui/src/modules/chat/core/split/reconcile.test.ts` — asserts: `needsOpenChoice` is true ONLY for `intent==='auto'` + `panes.length>=2` + the conversation not already open; false for single-pane (0–1 panes), for an already-open conversation (any pane count), and for explicit `newPane`/`replaceFocused` intents.

- **TEST-65** (tier: unit) [covers: ITEM-44, ITEM-56] file: `src-app/ui/src/modules/chat/core/popout/popoutVisibility.test.ts` — asserts: the pure `popoutActionVisible(inPane, isDesktop, isPopoutWindow)` truth table — TRUE inside a split pane on either platform, TRUE in single-pane on desktop, FALSE in single-pane on web; and (ITEM-56 / TEST-65b) FALSE inside the pop-out window regardless of pane/platform.
- **TEST-66** (tier: e2e) [covers: ITEM-44] file: `src-app/ui/tests/e2e/14-split-chat/popout-new-tab.spec.ts` — asserts: (web) the `chat-open-in-new-window` button is ABSENT in a single-pane conversation header, and PRESENT in each pane's header once a split is open (DEC-60 desktop-only-in-single-pane gate).

- **TEST-67** (tier: e2e) [covers: ITEM-45] file: `src-app/ui/tests/e2e/14-split-chat/voice-per-pane.spec.ts` — asserts: with two panes, starting voice dictation in pane B shows the recording/caption state in pane B's composer and inserts the transcript into pane B's textarea, while pane A is unaffected; and unmounting/closing a NON-recording pane does not cancel the recording pane's session (mic granted via fake-media).
- **TEST-68** (tier: unit) [covers: ITEM-45] file: `src-app/ui/src/modules/chat/extensions/voice/recorderOwnership.test.ts` — asserts: the pure exclusive-recorder-ownership guard — a second pane cannot acquire the recorder while another pane owns it (A1 disable-others), and the owner-release frees it for the next pane.
- **TEST-69** (tier: e2e) [covers: ITEM-46] file: `src-app/ui/tests/e2e/14-split-chat/kb-grounding-per-pane.spec.ts` — asserts: attaching a knowledge base via pane B's ＋-menu shows the KB grounding chip in pane B ONLY (absent in pane A), and detaching it in pane B leaves pane A's own KB selection untouched.
- **TEST-70** (tier: unit) [covers: ITEM-46] file: `src-app/ui/src/modules/knowledge-base/stores/KnowledgeBaseComposer.store.test.ts` — asserts: the per-conversation KB selection keying — selecting KBs for conversation A and reading conversation B returns B's own (empty) selection; attach/detach mutate only the addressed conversation's set (mirrors McpComposer's per-conversation Maps).
- **TEST-71** (tier: e2e) [covers: ITEM-47] file: `src-app/ui/tests/e2e/14-split-chat/mcp-grounding-per-pane.spec.ts` — asserts: an admin-enabled MCP server grounds both split conversations (chip in both panes); removing it via pane B's chip × edits only pane B's conversation config (`deselectServerForConversation`) — pane B loses the chip, pane A keeps it, and focusing pane A does not resurrect the removal, proving the status row resolves the per-conversation config not the single global-active selection.
- **TEST-72** (tier: unit) [covers: ITEM-48] file: `src-app/ui/src/modules/chat/core/utils/toolCallPaneScope.test.ts` — asserts: the pure filter that resolves a tool-call's conversation via its message_id and keeps only tool-calls belonging to a given pane's conversation — a pending-approval tool in conversation B is NOT treated as conversation A's new approval (so only the originating pane scrolls). (The scroll-to-approval effect is not deterministically e2e-triggerable without a real pending-approval tool; the pure filter is the honest coverage.)
- **TEST-73** (tier: unit) [covers: ITEM-49] file: `src-app/ui/src/modules/knowledge-base/stores/PdfHighlight.perpane.test.ts` — asserts: the per-pane PdfHighlight keying — setTarget(paneA,fileX) and setTarget(paneB,fileX) hold INDEPENDENT targets; clearTarget(paneA,fileX) leaves paneB's target intact; getTarget resolves by (paneKey,fileId).
- **TEST-74** (tier: e2e) [covers: ITEM-49] file: `src-app/ui/tests/e2e/14-split-chat/kb-highlight-per-pane.spec.ts` — asserts: a real KB citation opened in split pane B mounts the kb_source viewer (and thus its per-pane FileHighlightScope provider) in pane B's OWN right panel — pane A's right panel never surfaces that document — exercising the real citation→KbSourcePanel→scoped-highlight path in the correct pane (the only UI path that mounts the scope provider; the scoped-key isolation itself is unit-proven in TEST-73). Real-LLM (local bridge), soft-skips without one, per the kb-citation-flow gate.

- **TEST-75** (tier: unit) [covers: ITEM-50] file: `src-app/desktop/ui/src/modules/chat/core/popout/openConversationWindow.test.ts` — asserts: after migrating the raw desktop shadow to a co-located core-tree `.desktop` override (imported here via the `@/…openConversationWindow.desktop` alias, matching the sibling `Drawer.test.ts`/`loader.test.ts` convention), the Tauri path still constructs a `WebviewWindow` with label `chat-<id>` + url `/chat/<id>` and reopening the same id focuses the existing window (the desktop pop-out contract survives the migration); paired with the live2 override-registry gate (`gen-override-registry.mjs --check`, inside `npm run check` both workspaces) passing with NO unregistered-shadow violation. (TEST-P5's assertions are preserved by re-pointing the import, not dropped — A5.)

- **TEST-76** (tier: unit) [covers: ITEM-51] file: `src-app/ui/src/modules/knowledge-base/stores/kbSelectionKey.test.ts` — asserts: `pendingKbKey(paneId)` yields a per-pane key (`__pending__:<paneId>`), a null/empty pane → the bare `PENDING_KB_KEY` (single-pane byte-identical), two panes → distinct keys; and `selectedKbIdsFor(map, null, paneId)` reads ONLY that pane's pending buffer (pane B's `kbB` does not leak into pane A's read), while a real conversation id still ignores paneId.
- **TEST-77** (tier: unit) [covers: ITEM-51] file: `src-app/ui/src/modules/mcp/stores/approvalRouting.test.ts` — asserts: `pendingConversationKey(paneId)` (the MCP twin of `pendingKbKey`) yields `__pending__:<paneId>`, null/empty pane → the bare `PENDING_CONVERSATION_KEY`, and two new-chat panes get distinct pending config keys — so the KB and MCP composers stay symmetric.
- **TEST-78** (tier: e2e) [covers: ITEM-51] file: `src-app/ui/tests/e2e/14-split-chat/pending-selection-per-pane.spec.ts` — asserts: with TWO split panes each composing a NEW (pre-mint) chat (panes 1+2, anchored by a saved pane 0), two per-pane pending-isolation proofs: (a) KB attach-isolation — a KB attached in pane A's + menu shows its chip in pane A ONLY (KB is not auto-seeded); (b) MCP deselect-isolation — an admin-enabled server auto-seeds into EACH new pane's OWN pending config (per-pane McpInitializer) so it shows in BOTH, and removing it from pane A's chip × edits only pane A's pending buffer (gone from A, still in B). Focusing pane B surfaces neither pane A's KB attach nor resurrects the MCP server pane A removed. Proves the pre-mint pending buffer is keyed per pane (before ITEM-51 both shared one `__pending__` buffer and the action leaked into pane B). No LLM.

- **TEST-79** (tier: e2e) [covers: ITEM-52] file: `src-app/ui/tests/e2e/14-split-chat/popout-window-chat-only.spec.ts` — asserts: RUNS the real render + asserts the DOM — navigating to `/chat-window/:id` renders the conversation interface (composer + `conversation-title`) with the app SHELL ABSENT (`app-sidebar`/`layout-sidebar-toggle-button`/`layout-sidebar-nav-menu` all count 0), while the control `/chat/:id` DOES show `app-sidebar`. Proves the desktop pop-out route is chat-only (blank layout), by execution not code-read.
- **TEST-80** (tier: unit) [covers: ITEM-53] file: `src-app/desktop/ui/src/modules/chat/core/popout/focusPopoutWindow.test.ts` — asserts: RUNS the desktop focus seam (Tauri mocked): an already-open pop-out window (found via the shared `popoutWindowLabel` = `chat-<id>`) is unminimized then focused (ordered) and `focusPopoutWindowIfOpen` returns true (so the caller aborts the inline open); no window → false (open inline).
- **TEST-81** (tier: unit) [covers: ITEM-54] file: `src-app/ui/src/modules/chat/core/popout/planPopoutSnapBack.test.ts` — asserts: RUNS the pure snap-back decision — a closed pop-out conversation not already shown → `add`; already in a pane / IS the single-pane conversation → `alreadyShown` (no duplicate); at MAX_PANES → `atCap`.
- **TEST-82** (tier: unit) [covers: ITEM-54] file: `src-app/ui/src/modules/chat/core/popout/planPopoutSnapBack.test.ts` — asserts: RUNS the `handlePopoutClosed` control flow — opens the conversation back as a new pane ONLY when the decision is `add`; no-op for `alreadyShown`/`atCap`.
- **TEST-83** (tier: unit) [covers: ITEM-54] file: `src-app/desktop/ui/src/modules/chat/core/popout/popoutSnapBack.test.ts` — asserts: RUNS the desktop cross-window wiring control flow (Tauri boundary mocked) — a pop-out window emits `POPOUT_CLOSED_EVENT` with its conversationId on close; the main-window listener runs the snap-back handler on that event (opening the conversation back as a pane). The cross-OS-window event DELIVERY itself is a Tauri platform guarantee (desktop-host verified), not owned logic.
- **TEST-84** (tier: unit) [covers: ITEM-54] file: `src-app/ui/src/modules/chat/core/popout/planPopoutSnapBack.test.ts` — asserts: RUNS `snapBackAsNewPane` (the blind-audit HIGH fix) — it reconcile-opens the conversation as a 'newPane' AND navigates to `/chat/<id>`, so the main window actually mounts ConversationPage/SplitChatView (mutating SplitView without navigating left the pane invisible on a non-chat route).
- **TEST-85** (tier: e2e) [covers: ITEM-55, ITEM-56] file: `src-app/ui/tests/e2e/14-split-chat/header-back-visibility.spec.ts` — asserts: RUNS the render + asserts the DOM that window-management chrome is scoped per context — the BACK arrow shows in single-pane, is ABSENT in split panes and in the pop-out window; and the pop-out WINDOW hides ALL window-management chrome (back + split-btn + pop-out-btn all count 0) while keeping conversation actions (find toggle + composer present). Proves the audit fixes by execution, not code-read.
- **TEST-86** (tier: e2e) [covers: ITEM-38] file: `src-app/ui/tests/e2e/14-split-chat/message-actions-copy-and-popout.spec.ts` — asserts: RUNS the render + real clipboard read that the COPY button binds to the message it is RENDERED on (`useMessageContext()`), NOT a global focused-message — with pane 0 focused (and empty), clicking pane 1's user-message copy button puts pane 1's exact text on the clipboard. The message-actions per-pane audit leg TEST-58 did not cover (regenerate/edit/branch only).
- **TEST-87** (tier: e2e) [covers: ITEM-52] file: `src-app/ui/tests/e2e/14-split-chat/message-actions-copy-and-popout.spec.ts` — asserts: RUNS the render in the pop-out window (`/chat-window/:id`, no pane provider) that message actions bind to the global `Stores.Chat` scoped to THAT window's conversation — the full action set renders (copy×2, edit×1, regenerate×1) and clicking edit restores the message text into the window's own composer. Proves the pop-out is a fully-functional conversation view, by execution.
- **TEST-88** (tier: unit) [covers: ITEM-57] file: `src-app/ui/src/modules/chat/core/pane/singlePaneDrop.test.ts` — asserts: RUNS `zoneForX` — a pointer x maps to the left/center/right third of a container (boundaries at 1/3, 2/3), clamps an out-of-rect x to the nearest edge zone, and resolves a zero-width rect to center (no divide-by-zero).
- **TEST-89** (tier: unit) [covers: ITEM-57] file: `src-app/ui/src/modules/chat/core/pane/singlePaneDrop.test.ts` — asserts: RUNS `planSinglePaneDrop` — left → split `[dropped, current]`, right → split `[current, dropped]`, center → replace with dropped, and dropping a conversation onto its OWN view (or an empty id) is a no-op in every zone.
- **TEST-90** (tier: e2e) [covers: ITEM-57] file: `src-app/ui/tests/e2e/14-split-chat/single-pane-drop.spec.ts` — asserts: RUNS the render + real HTML5 DnD on the single-pane chat column (`chat-single-drop-column`) with a clientX aimed at each third — dropping on the RIGHT third splits `[current | dropped]`, the LEFT third splits `[dropped | current]`, and the CENTER replaces the current conversation (no split, URL + title follow the dropped conversation). Proves the edge-directional behavior by execution.
- **TEST-91** (tier: unit) [covers: ITEM-58] file: `src-app/ui/src/modules/chat/core/popout/tearOff.test.ts` — asserts: RUNS `isOutsideWindow` (a release inside the window rect is not outside; a release past any of the four edges — right/bottom exclusive — is outside) + `planTearOff` (opens ONLY when outside AND desktop; a pane source MOVES → closePaneId is the pane; web / in-window → no open, no close).
- **TEST-92** (tier: unit) [covers: ITEM-58] file: `src-app/ui/src/modules/chat/core/popout/tearOff.test.ts` — asserts: RUNS the decision→effect glue `runTearOffPlan` with spied effects — an active plan calls `openWindow(id,{title})` and (for a pane source) `closePane(paneId)`; a no-pane plan opens without closing; an inactive plan (web / in-window) calls neither and returns false. This is the wiring the `useConversationTearOff` hook executes.
- **TEST-93** (tier: e2e) [covers: ITEM-58] file: `src-app/ui/tests/e2e/14-split-chat/tear-off-web-gate.spec.ts` — asserts: RUNS the render + a real `dragend` (off-window screen coords) on a wired drag source (`chat-conversation-card-<id>`) on the WEB build — no window opens (`window.open` spy count 0) and no navigation to `/chat-window/`. Proves the drag sources are actually wired to `onDragEnd` AND the desktop-only gate holds, by execution. (The desktop POSITIVE — a real native OS window past the edge — can't be driven headlessly, same platform-guarantee limit as TEST-83; covered by TEST-92's exec glue + the shared `openConversationWindow` desktop-seam tests.)
- **TEST-94** (tier: e2e) [covers: ITEM-59] file: `src-app/ui/tests/e2e/14-split-chat/skill-drawer-per-pane.spec.ts` — asserts: RUNS a split [A|B] + opens "Skills in this chat" in pane B → EXACTLY ONE `skill-conversation-dialog` renders (pre-fix the global `{open}` rendered one per pane = 2). Proves the per-conversation keying by execution.
- **TEST-95** (tier: e2e) [covers: ITEM-60] file: `src-app/ui/tests/e2e/14-split-chat/focused-pane-routing.spec.ts` — asserts: RUNS Ctrl-F with pane 0 focused → find bar opens in pane 0 ONLY; refocus pane 1 → opens in pane 1 ONLY. Proves the keyboard-path focus gate (the button path was already covered; this is the missing keyboard path).
- **TEST-96** (tier: e2e) [covers: ITEM-61] file: `src-app/ui/tests/e2e/14-split-chat/focused-pane-routing.spec.ts` — asserts: RUNS renaming pane B's title while pane A is focused → pane B's title updates + the API shows conversation B renamed and conversation A unchanged (the save routed to the owning pane, not the focused bridge).
- **TEST-97** (tier: unit) [covers: ITEM-62] file: `src-app/ui/src/modules/chat/extensions/text/paneDraftKeys.test.ts` — asserts: RUNS `PaneDraftKeys` — two interleaved per-pane captures do NOT clobber each other (the module-global `let` bug), `take` is one-shot read-and-clear, and null-pane (single-pane) is its own slot. The owner-TextStore resolution half mirrors the e2e-tested File-extension ownerPaneId pattern.
- **TEST-98** (tier: e2e) [covers: ITEM-63] file: `src-app/ui/tests/e2e/14-split-chat/mcp-approval-per-pane.spec.ts` — asserts: RUNS a mocked approval-required tool call in pane B, focuses pane A, approves in pane B → the captured RESUME send targets pane B's conversation (URL), NOT the focused pane A's. The tool call is mocked at the SSE boundary; the approval routing (crux) is real. Complements `approvalRouting.test.ts`.
- **TEST-99** (tier: e2e) [covers: ITEM-64] file: `src-app/ui/tests/e2e/14-split-chat/workflow-export-per-pane.spec.ts` — asserts: RUNS a `run_from_workspace` tool-result card in pane B, focuses pane A, clicks Download → the workspace-export request carries pane B's `conversation_id`, not the focused A's. Closes the phantom TEST-58 claim (the card was already pane-correct; now proven by running).
- **TEST-100** (tier: e2e) [covers: ITEM-65] file: `src-app/ui/tests/e2e/14-split-chat/right-panel-per-pane.spec.ts` — asserts: RUNS a file open in EACH pane → toggling canvas edit mode in pane 0 shows its `canvas-edit-body` while pane 1 stays in view mode (independent per-pane view-state). Same-conversation-in-two-panes is prevented by the dedup guard; view-state is local `useState` per FilePanel instance.
- **TEST-101** (tier: e2e) [covers: ITEM-66] file: `src-app/ui/tests/e2e/14-split-chat/voice-per-pane.spec.ts` — asserts: RUNS a 3-pane split, records in pane B, CLOSES a non-recording pane → pane B's recording survives (elapsed still runs) and stopping still transcribes into B. Proves closing/unmounting a non-recording pane doesn't cancel or strand the recording session.
- **TEST-102** (tier: e2e) [covers: ITEM-67] file: `src-app/ui/tests/e2e/14-split-chat/find-scope-per-pane.spec.ts` — asserts: RUNS find in pane A while pane B is focused, searching a word unique to A's conversation → "N of M" match (pre-fix it searched the focused pane B = "No results"); searching B's word in A → "No results". Proves the find search+jump is pane-scoped.
- **TEST-103** (tier: e2e) [covers: ITEM-68] file: `src-app/ui/tests/e2e/14-split-chat/message-actions-per-pane.spec.ts` — asserts: RUNS (extended) — editing pane B's message shows the `chat-editing-banner` in pane B and NOT pane A (the banner reads the pane's store), and cancel via pane B's banner clears pane B's edit. (CanvasSelectionPopover shares the same `useChatPaneOrNull` binding.)
- **TEST-104** (tier: e2e) [covers: ITEM-69] file: `src-app/ui/tests/e2e/14-split-chat/bidi-streams.spec.ts` — asserts: RUNS TWO SIMULTANEOUS streams (both panes active, sent back-to-back) → each pane keeps its OWN user+assistant marker and NEITHER ever shows the other's marker (bidirectional isolation), replacing the idle-empty-pane control the prior streaming spec used.
- **TEST-105** (tier: unit) [covers: ITEM-70] file: `src-app/ui/src/modules/chat/core/pane/singlePaneDrop.test.ts` — asserts: RUNS `planSplitPaneDrop` — in an existing split, left → insertBefore, right → insertAfter, center → replace; at the MAX_PANES cap all zones → replace; dropping a conversation onto its OWN pane (or an empty id) → noop.
- **TEST-106** (tier: e2e) [covers: ITEM-70] file: `src-app/ui/tests/e2e/14-split-chat/drag-to-split.spec.ts` — asserts: RUNS the render + real DnD on a pane's drop column at an aimed clientX — right edge of pane 0 inserts AFTER ([A,C,B]), left edge inserts BEFORE ([C,A,B]), center of pane 1 REPLACES ([A,C], no new pane), an OS file is ignored; a SECOND test builds [A|B|C] and drops on a pane edge at the cap → REPLACE (no 4th pane); a THIRD test keeps grip→header REORDER. Proves the unified split edge-directional model + the hooks-safe overlay by execution.
- **TEST-107** (tier: unit) [covers: ITEM-70] file: `src-app/ui/src/modules/chat/core/stores/SplitView.store.test.ts` — asserts: RUNS `openPane({beforePaneId})` — the new pane is spliced directly BEFORE the target pane (middle) and prepends before the first pane; focus moves to the new pane.
- **TEST-108** (tier: e2e) [covers: ITEM-71] file: `src-app/ui/tests/e2e/14-split-chat/split-header-consistency.spec.ts` — asserts: RUNS a split, focuses the LEFTMOST pane, and (a) the pane header boundingBox height is 50px (matches HeaderBarContainer, was 44), (b) a REAL `.click()` on the `layout-sidebar-toggle-button` collapses then expands the sidebar with pane 0 focused — Playwright's actionability check fails if the pane covers the toggle (the pre-fix z-10 bug), (c) the leftmost pane header's computed `paddingLeft` is 48px when collapsed / 12px when expanded (the shared `useHeaderLeftInset` reserve).
- **TEST-109** (tier: e2e) [covers: ITEM-72] file: `src-app/ui/tests/e2e/14-split-chat/split-url-tracks-focused-pane.spec.ts` — asserts: with TWO active panes, the address bar tracks the FOCUSED pane's conversation at every step — single-pane baseline is `/chat/A`; picking convB into the new focused pane navigates the URL to `/chat/B` (before the fix it stuck on `/chat/A` — the "open in new tab reopens the current conversation" bug); clicking pane 0 (convA) to focus it returns the URL to `/chat/A`; clicking pane 1 (convB) returns it to `/chat/B`. Proves the missing workspace→URL direction.
- **TEST-110** (tier: unit) [covers: ITEM-73] file: `src-app/ui/src/modules/chat/core/stores/splitWorkspace.persist.test.ts` — asserts: (a) the save/load/clear roundtrip goes through **sessionStorage** now (per-tab) and the v1→v2 migration reads the old **localStorage** key while writing the sessionStorage v2 key; (b) `isSameTabReload` is true ONLY for a `{type:'reload'}` navigation entry and false for `navigate`/`back_forward`/`prerender`/unknown/null (the gate that stops a fresh navigation / pop-out from resurrecting a split).
- **TEST-111** (tier: e2e) [covers: ITEM-73] file: `src-app/ui/tests/e2e/14-split-chat/split-per-tab-isolation.spec.ts` — asserts: TWO tests, both building `[A|B]` by DRAGGING convB onto the pane's right third (real HTML5 drop handler, not the split-button+picker shortcut): **(1) the exact bug repro, NO reload** — immediately pop pane B out via its ⤢ (`window.open('/chat/B')`, genuine sessionStorage COPY of the saved split); the new tab shows ONLY convB single-pane (`split-chat-view`=0, `chat-pane-1`=0, composer + title "Bravo"), origin collapses to single-pane A. **(2) per-tab persistence (separate)** — a same-tab reload restores that tab's split. The setup asserts the split IS saved to sessionStorage first, so the pop-out genuinely copies a split-bearing session (not a trivial pass). Legacy phrasing below retained for the audit trail; (C) reload the ORIGINAL tab → the split is restored (per-tab reload); (A) click pane B's ⤢ → `window.open('/chat/B')` opens a new tab that shows ONLY convB (composer + title "Bravo", `split-chat-view`=0, `chat-pane-1`=0), NOT the inherited split — the reload gate drops the copied split; and the origin tab collapses to single-pane A. (NB: an earlier `addInitScript` SIMULATION of the copy mis-booted the app to a blank shell — a test artifact, not a fix bug; the real `window.open` path renders correctly.) Also re-verified by the updated `workspace-persist-nav` + `persistence` + `header-back-visibility` specs under the per-tab model.
