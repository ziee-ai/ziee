# DRIFT-1 — implementation vs plan

Audited the implemented diff against PLAN.md ITEM-by-ITEM.

- **DRIFT-1.1** — verdict: impl-wins — **The plan missed a third `Claims` construction site.** `McpSessionManager::generate_short_lived_jwt` (`mcp/client/manager.rs:279`) mints the internal service-to-service access token used by EVERY built-in MCP server call and by `resource_link` loopback fetches; it lives outside `jwt.rs`, so the plan's "2 mint paths" survey (based on grepping `jwt.rs`) did not see it. The compiler surfaced it (`missing field ver`). It matters: those loopback routes are gated by `RequirePermissions` → the new epoch check, so leaving `ver` defaulted to 0 would have **401'd every built-in tool call for any user who had ever logged out** (epoch ≥ 1) — a silent, severe regression. PLAN.md amended with **ITEM-13**; TESTS.md amended with **TEST-22** (extends the existing enumerate-every-built-in probe test). Phases 1–3 re-run green.
- **DRIFT-1.2** — verdict: impl-wins — `get_by_id_with_token_version` could not use `sqlx::query!` as planned: the macro rejects the `as "col: _"` wildcard overrides the `users` select needs ("wildcard overrides are only allowed with an explicit record type"). Implemented with `query_as!` into a **private, non-serializable `UserWithTokenVersion`** struct, split back to `(User, i32)` at the boundary. This is strictly better for the plan's actual goal (DEC-6: keep the epoch off the serializable `User`) — the row type is private and cannot be serialized at all. No plan ITEM change; the file and intent are unchanged.
- **DRIFT-1.3** — verdict: resolved — the plan said `clearedSession` would be typed `satisfies Omit<AuthState, keyof AuthActions>`. There is no `AuthActions` type — `AuthState` merges state and actions into one interface. Used `satisfies Partial<AuthState>`, which keeps the field-level typo protection the plan wanted. Cosmetic; no behavioral difference.
- **DRIFT-1.4** — verdict: resolved — the plan's TEST-11 sketch used `server.jwt_secret()`. No such accessor exists on `TestServer`, and adding one would edit the SHARED harness to suit this feature (rule B3). Used the harness's fixed secret literal instead, exactly as `tests/sync/subscribe_test.rs:228` already does.
- **ITEM-1..12** — verdict: none — implemented as planned, in the planned files, mirroring the planned references (`claim_rotation_and_register` for the transaction; the `jti` claim for the optional `ver`; `session_expiries` for the read-in-mint-path; `user/handlers/user.rs:461-469` for the `sync_publish`).

**Unresolved drifts:** 0
