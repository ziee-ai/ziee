# TESTS — org-migration-hub-ids

Every ITEM is covered by ≥1 TEST. Most tests are existing assertions retargeted
from `io.github.phibya` → `io.github.ziee-ai` (the rename is not observable
unless the tests track it); TEST-5 adds genuine new coverage for migration 131.

- **TEST-1** (tier: unit) [covers: ITEM-1, ITEM-3, ITEM-5] file: `src-app/server/src/modules/hub/hub_manager.rs` — asserts: `HUB_SEED.get_file("models/io.github.ziee-ai/llama-3-1-8b-instruct/1.0.0.json")` and the `assistants/io.github.ziee-ai/code-reviewer/1.0.0.json` resolve, and each manifest's `name` equals its `io.github.ziee-ai/*` id (proves the dir rename + manifest `name` rewrite are baked into the embedded seed).
- **TEST-2** (tier: unit) [covers: ITEM-1, ITEM-5] file: `src-app/server/src/modules/hub/hub_manager.rs` — asserts: `is_safe_name("io.github.ziee-ai/llama-3-1-8b-instruct")` is `true` (the rebranded namespace, hyphen included, is a valid safe name).
- **TEST-3** (tier: integration) [covers: ITEM-2, ITEM-6, ITEM-10] file: `src-app/server/tests/hub/catalog_v1.rs` — asserts: the catalog lists `io.github.ziee-ai/code-reviewer` + `io.github.ziee-ai/llama-3-1-8b-instruct`, the manifest endpoint resolves `io.github.ziee-ai%2Fllama-3-1-8b-instruct`, a tracked install surfaces `hub_id = io.github.ziee-ai/code-reviewer`, and (ITEM-10) the version/index/manifest reads succeed under `hub::catalog::read` with the corrected 29-item / 10-workflow seed counts.
- **TEST-4** (tier: integration) [covers: ITEM-2, ITEM-6] file: `src-app/server/tests/hub/mod.rs` — asserts: model-manifest lookups keyed on `io.github.ziee-ai/llama-3-1-8b-instruct` (5 sites) resolve against the rebranded catalog/seed, incl. a tracked `hub_id = io.github.ziee-ai/llama-3-1-8b-instruct` install row.

> ITEM-6 also touches `tests/llm_model/download_test.rs` (a 1-line `INCOMPATIBLE_FIXTURE_NAMES` fixture rename to `io.github.ziee-ai/deepseek-r1-70b`). That fixture is consumed only by the HF-download suite, which is **secret-token-gated** (no `HUGGINGFACE_API_KEY` on this host — the one legitimate skip category). Its rebrand correctness — the const matching a real seed model name — is covered by the seed-content assertions in TEST-1; no separate runnable lifecycle test is claimed for it.
- **TEST-5** (tier: integration) [covers: ITEM-4, ITEM-7] file: `src-app/server/tests/hub/migration_test.rs` — asserts: after all migrations run, a seeded slug-origin row ends at `io.github.ziee-ai/code-reviewer` (92→131 chain), migration 131 rewrites a directly-inserted `io.github.phibya/*` `hub_entities.hub_id` to `io.github.ziee-ai/*`, and re-running the rewrite is a no-op (idempotent).
- **TEST-6** (tier: unit) [covers: ITEM-8] file: `src-app/server/src/modules/workflow_mcp/tools.rs` — asserts: `slug_for_name("io.github.ziee-ai/research-summarize-write")` == `wf_io_github_ziee-ai_research-summarize-write` and the install-slug-length check accepts it.
- **TEST-8** (tier: integration) [covers: ITEM-9] file: `hub-clone/scripts/test-pages-build.sh` — asserts: after the 12 source-YAML renames, the hub repo's `python3 scripts/validate.py` + `scripts/test-pages-build.sh` pass (reverse-DNS name check, cross-ref resolution, Pages build emits `assistants/io.github.ziee-ai/...` + `models/io.github.ziee-ai/...`).
