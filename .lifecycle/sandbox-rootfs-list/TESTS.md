# TESTS — sandbox-rootfs-list

Every ITEM is covered by ≥1 TEST; frontend work carries an e2e spec.

- **TEST-1** (tier: unit) [covers: ITEM-1] file: `src-app/server/src/modules/code_sandbox/config.rs` — asserts: `init_status()` defaults to `NotInitialized`, `set_init_status` records a value, and `SandboxAvailability` serializes to snake_case (`disabled_in_config`, `host_unsupported`, …).
- **TEST-2** (tier: unit) [covers: ITEM-3] file: `src-app/server/src/modules/code_sandbox/version_manager.rs` — asserts: `build_degraded(availability, avail)` returns a `VersionStatus` with the passed `availability`, empty `installed`/`draining`, `pinned_version==None`, zero counts, and non-empty `host_arch`/`host_package`.
- **TEST-3** (tier: integration) [covers: ITEM-2, ITEM-4] file: `src-app/server/tests/code_sandbox/tier3_versions.rs` — asserts: a reader with `environments::read` gets **200** from `GET /code-sandbox/rootfs/versions` with `availability == "disabled_in_config"` (test config leaves code_sandbox off) and `available` present as an array; 401/403 gates unchanged.
- **TEST-4** (tier: integration) [covers: ITEM-4] file: `src-app/server/tests/code_sandbox/tier3_versions.rs` — asserts: the degraded LIST response carries `installed == []` and `pinned_version == null`, while `POST …/versions/install` still returns 503 `SANDBOX_NOT_INITIALIZED` (only the LIST path degrades).
- **TEST-5** (tier: e2e) [covers: ITEM-5, ITEM-6] file: `src-app/ui/tests/e2e/settings/sandbox-rootfs-versions.spec.ts` — asserts: with `GET …/rootfs/versions` mocked to 200 `{availability:"disabled_in_config", available:[…], installed:[]}`, the `sandbox-rootfs-degraded-alert` warning renders, available versions are listed, install buttons are disabled, and the destructive `sandbox-rootfs-error` does NOT appear.
- **TEST-6** (tier: integration) [covers: ITEM-7, ITEM-8, ITEM-9] file: `docker-compose.sandbox.yaml` — asserts: on the rebuilt `ziee-web` image the runtime ships `bwrap`/`squashfuse`/`fusermount`; before enabling, Settings→Sandbox degrades gracefully (available catalog + reason notice, no full-page error); after `-f docker-compose.yml -f docker-compose.sandbox.yaml up`, the startup log shows `code_sandbox: registered` and the list shows real available versions. (Live-stack verification — recorded in TEST_RESULTS.)
- **TEST-7** (tier: unit) [covers: ITEM-10] file: `src-app/server/src/openapi/emit_ts.rs` — asserts: `types_ts_parity` golden test passes — the regenerated `types.ts` matches the committed one for the new `availability` field + `SandboxAvailability` union.
- **TEST-8** (tier: unit) [covers: ITEM-11] file: `src-app/ui/src/modules/code-sandbox/stores/installTaskReconcile.test.ts` — asserts: `reconcileInitialTask` seeds the initial task when none is tracked, does NOT downgrade an SSE-advanced (`downloading`) task with the late phase-null POST reply, and does not resurrect a terminal SSE task — the anti-clobber invariant that keeps a long download from appearing stuck on "queued".
- **TEST-9** (tier: unit) [covers: ITEM-12] file: `src-app/ui/plugins/vite-plugin-testid-unique.js` — asserts: the build-time `testid-unique` gate passes on the full tree once the 4 intentional elicitation shares are allowlisted (verified by a green `npm run build` — `[testid-unique] ✓ All data-testid literals unique (1425 ids)`), and the allowlist is scoped so a stray dup elsewhere still fails.
