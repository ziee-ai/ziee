# HUMAN_FEEDBACK — local-voice-dictation (phase 9)

Living ledger of human review of the running feature. Verbatim feedback, then
resolution. Any `[status: open]` fails the gate.

- **FB-1** [status: resolved] — "confirm that if a user group is NOT granted your voice-dictation feature's permission, that group's members do NOT see the feature's UI AT ALL (not just 403 on use) … the composer MIC BUTTON — is it gated so an unpermitted user doesn't see the mic/push-to-talk affordance at all? … If any renders without the permission check, FIX before merge." → The mic button previously hid for unpermitted users only INDIRECTLY (the store withheld `capability`); no explicit render-level permission check, one refactor from leaking. Added the explicit layer-4 gate `if (!usePermission(Permissions.VoiceTranscribe)) return null` at the top of `MicButton` (independent of the feature/binary gate), and added TEST-38 (restricted-user e2e) proving a user lacking `voice::transcribe` sees no mic/indicator/live-region even with a ready capability mocked. Admin layers (slot `permission`, route gate, `<Can>`/`usePermission` on controls) verified already-gated. [generalizable: yes — every new user-facing permission needs an EXPLICIT render-level gate at each affordance PLUS a `(tier: e2e) [negative-perm]` restricted-user spec walking all four PERMISSION_GATING layers; an indirect "data withheld → nothing renders" gate is insufficient. This is the A10 rule now in the lifecycle skill.]

- **FB-2** [status: resolved] — "real-LLM/transcribe specs soft-skip not `#[ignore]`. If the download e2e genuinely couldn't run (release not published), that's a legit external gate — mark it clearly, don't claim full green over it." → Converted the real-download e2e (TEST-37) from `#[ignore]` to a SOFT-SKIP: it runs in the default suite, probes the GitHub release API, and early-returns with a clear `SOFT-SKIP [external gate: whisper-release]` marker only when the release is unpublished/unreachable/rate-limited; once the release is confirmed present, every step is a hard assertion. Verified green (`0 ignored; 1 passed`) against the live `v1.9.1`. [generalizable: yes — an external-dependency test (published binary release, live upstream) must be a marked soft-skip that auto-runs when the dependency is present, never a compile-time `#[ignore]` that hides it; the skip marker makes the gate honest without claiming green over an unmet dependency.]

- **FB-3** [status: resolved] — "the whisper.cpp release publish is a LEGIT external gate … do ALL the non-blocked work NOW without asking — the retry hardening, the self-heal CI logic … for the parts that DO need the published binary, clearly mark them as externally-blocked-on-whisper-release in TEST_RESULTS." → Did all non-blocked work: published `v1.9.1` (full matrix green), added step-level `choco`/`curl` retries + a job-level `auto-rerun-release` self-heal workflow to the fork (validated in a real Windows-CUDA build), and documented the explicit DONE-vs-BLOCKED split in TEST_RESULTS (gate CLEARED → nothing remained blocked). [generalizable: yes — grind every non-externally-blocked item to completion without asking; reserve "blocked" for a SPECIFIC unmet external dependency and mark it explicitly, never as a catch-all deferral.]
