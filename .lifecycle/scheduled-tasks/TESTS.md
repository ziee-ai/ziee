# TESTS — scheduled-tasks

Every ITEM is covered by ≥1 TEST. UI items (ITEM-1..4, ITEM-10 UI half) carry a `tier: e2e`.
No new permission is introduced (scheduler perms already exist) → no `[negative-perm]` spec
is required (A10 N/A). Backend items get unit + integration, extending the existing
`tests/scheduler/*` suite.

## Frontend
- **TEST-1** (tier: e2e) [covers: ITEM-1, ITEM-3] file: `src-app/ui/tests/e2e/14-scheduler/scheduled-tasks.spec.ts` — asserts: user opens the New Task drawer (FormField layout, labelled required fields), selects an Assistant and a Model from pickers (never typing an ID), fills prompt + schedule, clicks Create, and the task appears in the list.
- **TEST-2** (tier: e2e) [covers: ITEM-1, ITEM-4] file: `src-app/ui/tests/e2e/14-scheduler/scheduled-tasks.spec.ts` — asserts: switching the target to Workflow shows a Workflow picker; picking a workflow renders its typed inputs (or a JSON fallback when it declares none); Create succeeds with the chosen workflow_id.
- **TEST-3** (tier: e2e) [covers: ITEM-2] file: `src-app/ui/tests/e2e/14-scheduler/scheduled-tasks.spec.ts` — asserts: the drawer shows NO timezone input field; the auto-detected timezone appears as read-only helper text, and the created task is saved with the browser-detected timezone (no user tz entry).
- **TEST-4** (tier: e2e) [covers: ITEM-10] file: `src-app/ui/tests/e2e/14-scheduler/paused-and-runs.spec.ts` — asserts: a spent `once` task (paused_reason='completed', seeded via the mock API) renders a distinct "Completed" badge, not a generic paused/disabled state.
- **TEST-5** (tier: unit) [covers: ITEM-1] file: `src-app/ui/src/modules/scheduler/components/TaskTargetPickers.tsx` — asserts: the model-options builder maps `Stores.ModelPicker.providers` into grouped `{label, options:[{label,value}]}` using model DISPLAY names for labels and the model id for value (empty-provider groups dropped).
- **TEST-6** (tier: unit) [covers: ITEM-2] file: `src-app/ui/src/modules/scheduler/components/ScheduleBuilder.tsx` — asserts: the builder renders the timezone as READ-ONLY text (`schedule-timezone-note`, seeded by the drawer's `browserTz()`) with NO editable timezone control in the DOM.
- **TEST-7** (tier: unit) [covers: ITEM-4] file: `src-app/ui/src/modules/scheduler/components/ScheduledTaskFormDrawer.tsx` — asserts: given a workflow whose `parseWorkflowIr` exposes inputs, the drawer chooses typed-field mode; given one with no inputs, it falls back to the JSON editor.

## Backend
- **TEST-8** (tier: integration) [covers: ITEM-5] file: `src-app/server/tests/scheduler/validation_test.rs` — asserts: `POST /scheduled-tasks` with a non-existent `model_id` → 404 (not 500); with an existing-but-inaccessible `model_id` → 403; with a valid accessible model → 201.
- **TEST-9** (tier: unit) [covers: ITEM-5] file: `src-app/server/src/modules/scheduler/handlers.rs` — asserts: the model-access validation helper classifies missing→NotFound, existing-inaccessible→Forbidden, accessible→Ok.
- **TEST-10** (tier: integration) [covers: ITEM-6] file: `src-app/server/tests/scheduler/validation_test.rs` — asserts: `PUT` update to a foreign `assistant_id` → 404 (existence not leaked); to an inaccessible `model_id` → 403; re-enabling (`enabled:true`) a task that would push the user over `max_active_tasks_per_user` → 4xx; re-enabling under the cap → 200.
- **TEST-11** (tier: unit) [covers: ITEM-6] file: `src-app/server/src/modules/scheduler/repository.rs` — asserts: the active-task count used for the re-enable quota excludes the row being updated (no off-by-one when re-enabling an already-counted task).
- **TEST-12** (tier: unit) [covers: ITEM-7] file: `src-app/server/src/modules/scheduler/tick.rs` — asserts: `preemptive_pause_reason` matrix — a prompt task with bound_conversation_id NULL AND last_status a SUCCESS status ⇒ conversation_deleted; NULL + last_status='failed' (failed first fire) ⇒ None (not paused); NULL + None ⇒ None (genuine first run). (Integration is schema-blocked: the harness has no non-code_sandbox tool-emitting stub; the branch LOGIC is the testable unit.)
- **TEST-13** (tier: unit) [covers: ITEM-7] file: `src-app/server/src/modules/scheduler/tick.rs` — asserts: `preemptive_pause_reason` for a workflow task with workflow_id NULL ⇒ target_missing. (A workflow row with NULL workflow_id is schema-unreachable — the scheduled_tasks_target_coherent CHECK blocks it — so this is covered as the defensive branch's unit logic.)
- **TEST-14** (tier: integration) [covers: ITEM-8] file: `src-app/server/tests/scheduler/prune_test.rs` — asserts: `scheduled_task_runs` rows older than `notification_retention_days` are deleted by the prune pass; newer rows are retained.
- **TEST-15** (tier: unit) [covers: ITEM-8] file: `src-app/server/src/modules/scheduler/repository.rs` — asserts: the run-prune cutoff is `now - retention_days` and the DELETE targets only rows with `fired_at <` cutoff.
- **TEST-16** (tier: unit) [covers: ITEM-9] file: `src-app/server/src/modules/scheduler/failure.rs` — asserts: transient tolerance is CAP-based (retry removed as unsafe): `should_autopause(n,cap)` is false below the cap and true at/above it, and `classify` buckets transient (5xx/timeout/409/429) vs terminal (401/403/400/404) — so a single transient blip never pauses but persistent failures reach the cap. (No fail-once-then-succeed seam exists in the harness.)
- **TEST-17** (tier: unit) [covers: ITEM-9] file: `src-app/server/src/modules/scheduler/failure.rs` — asserts: `is_retryable` is true only for `transient`; `retry_backoff_ms` grows monotonically per attempt and the retry loop stops at MAX_ATTEMPTS.
- **TEST-18** (tier: integration) [covers: ITEM-10] file: `src-app/server/tests/scheduler/tick_test.rs` — asserts: after a `once` task fires, its row is `enabled=false`, `paused_reason='completed'`, `next_run_at=NULL` (distinct from a user pause).
- **TEST-19** (tier: unit) [covers: ITEM-10] file: `src-app/server/src/modules/scheduler/repository.rs` — asserts: `mark_fired` for a `once`-kind task sets `paused_reason='completed'` (and leaves a recurring task's reason NULL).
- **TEST-20** (tier: unit) [covers: ITEM-11] file: `src-app/server/src/modules/scheduler/repository.rs` — asserts: `insert_run` records a run row independent of any notification (the tick calls it unconditionally after dispatch; notification_id may be NULL). DB-gated (DATABASE_URL). (No create_and_emit fail-injection seam exists.)

## Frontend — ITEM-12 (multi-day-of-week)
- **TEST-21** (tier: e2e) [covers: ITEM-12] file: `src-app/ui/tests/e2e/14-scheduler/scheduled-tasks.spec.ts` — asserts: in weekly mode the user toggles MULTIPLE days (Mon + Wed + Fri) via the day multi-toggle, creates the task, and the task summary reads "Weekly on Mon, Wed, Fri at <time>".
- **TEST-22** (tier: unit) [covers: ITEM-12] file: `src-app/ui/src/modules/scheduler/components/ScheduleBuilder.tsx` — asserts: selecting days {Mon,Wed,Fri} emits the cron dow as the sorted comma list `1,3,5` (→ `0 9 * * 1,3,5`), and `humanizeCron` round-trips `0 9 * * 1,3,5` → "Weekly on Mon, Wed, Fri at 09:00" while falling back to `Cron: <expr>` for an arbitrary expression.

## Unattended tool-call + approval policy (ITEM-13..18)
- **TEST-23** (tier: unit) [covers: ITEM-13] file: `src-app/server/src/modules/mcp/chat_extension/mcp.rs` — asserts: `unattended_tool_allowed` returns false for a non-allow-listed (server,tool) ⇒ the deny branch fires; the denial marker is `structured_content.unattended_denied`. (The harness has no non-code_sandbox tool-emitting stub to drive an end-to-end third-party approval tool_call; the decision is the testable unit.)
- **TEST-24** (tier: unit) [covers: ITEM-13] file: `src-app/server/src/modules/mcp/chat_extension/mcp.rs` — asserts: the deny/execute branch is gated on `unattended` — with unattended=false the classification is unchanged (approval tools still go to tools_needing_approval). Interactive end-to-end approval is covered by the existing `tests/mcp/mcp_approval_workflow_test.rs` (unchanged path).
- **TEST-25** (tier: unit) [covers: ITEM-13] file: `src-app/server/src/modules/mcp/chat_extension/mcp.rs` — asserts: the approval-decision helper returns Deny when `unattended && !allow_listed`, and returns the unchanged RequireApproval when `!unattended` (built-in read-only always bypass).
- **TEST-26** (tier: unit) [covers: ITEM-14] file: `src-app/server/src/modules/scheduler/dispatch.rs` — asserts: `build_unattended_mcp_servers` (extracted helper) yields only allow-listed servers, so a non-allow-listed Always-mode server is never in the attach set (empty allow-list ⇒ empty ⇒ built-in-read-only only). (End-to-end Always-mode pre-exec needs a third-party stub the harness lacks.)
- **TEST-27** (tier: unit) [covers: ITEM-14] file: `src-app/server/src/modules/scheduler/dispatch.rs` — asserts: the extracted `build_unattended_mcp_servers(grants)` helper builds the attach set = allow-listed servers (whole-server grant ⇒ tools:[] = all; per-tool grants grouped); empty ⇒ empty (built-in-read-only floor).
- **TEST-28** (tier: integration) [covers: ITEM-15] file: `src-app/server/tests/scheduler/validation_test.rs` — asserts: create with `allowed_unattended_tools` ⊆ the user's accessible servers → 201 (persisted + round-trips); an entry referencing an inaccessible/nonexistent server → 4xx; same on update.
- **TEST-29** (tier: unit) [covers: ITEM-15] file: `src-app/server/src/modules/scheduler/handlers.rs` — asserts: the allow-list validator accepts accessible-server entries and rejects any entry not in the user's accessible set (no privilege widening).
- **TEST-30** (tier: e2e) [covers: ITEM-16] file: `src-app/ui/tests/e2e/14-scheduler/scheduled-tasks.spec.ts` — asserts: the drawer shows a "Tools this task may use unattended" multi-select picker (empty by default = read-only-only); selecting entries persists them on the created task.
- **TEST-31** (tier: unit) [covers: ITEM-17] file: `src-app/server/src/modules/scheduler/dispatch.rs` — asserts: a RawResult carrying skipped_tools threads into DispatchOutcome + the notification body names the skip (the skipped-tools→run+notification wiring). (End-to-end tool_call denial needs the missing stub; the round-trip wiring is the unit.)
- **TEST-32** (tier: e2e) [covers: ITEM-17] file: `src-app/ui/tests/e2e/14-scheduler/paused-and-runs.spec.ts` — asserts: the Runs UI surfaces "N tools skipped (not permitted unattended)" for such a run (seeded via the mock API).
- **TEST-33** (tier: integration) [covers: ITEM-18] file: `src-app/server/tests/scheduler/validation_test.rs` — asserts: creating a `workflow` task whose IR contains an `elicit` step → 4xx with a clear "needs interactive input; can't be scheduled" error; a workflow with no elicit step → 201.
- **TEST-34** (tier: unit) [covers: ITEM-18] file: `src-app/server/src/modules/scheduler/dispatch.rs` — asserts: `build_unattended_mcp_servers` + the DisabledServer predicate — a standalone/scheduled run resolves the user's DEFAULT disabled set and a disabled (server,tool) is rejected. (An end-to-end workflow-tool-step fixture needs shared-harness changes B3 forbids.)

## ROUND 2 — Follow-up & Series (ITEM-40..48)
No new permission is introduced → no `[negative-perm]` spec (A10 N/A). The new
`continue-series` route reuses `scheduler::use`; its owner-scope 404 is the A9 deny path.

### Backend
- **TEST-40** (tier: integration) [covers: ITEM-40] file: `src-app/server/tests/scheduler/runs_timeline_test.rs` — asserts: after a firing, the run row exposes a capped `result_preview` and `change_summary_json` with `{changed, new_count}`; a second identical firing → `changed:false`; a firing with new items → `new_count > 0`.
- **TEST-41** (tier: unit) [covers: ITEM-40] file: `src-app/server/src/modules/scheduler/dispatch.rs` — asserts: the preview builder truncates at `PREVIEW_CHARS` (char-safe) and the change-summary maps a `change::diff` outcome → `{changed, new_count, new_items}` (unchanged → `{changed:false, new_count:0}`).
- **TEST-42** (tier: integration) [covers: ITEM-41] file: `src-app/server/tests/scheduler/runs_timeline_test.rs` — asserts: `GET /scheduled-tasks/{id}/runs?page=1&per_page=10` returns ≤10 rows + a `total` equal to all runs; `page=2` returns the next slice with no overlap.
- **TEST-43** (tier: unit) [covers: ITEM-41] file: `src-app/server/src/modules/scheduler/repository.rs` — asserts: `list_runs_for_task` applies `LIMIT/OFFSET` from page/per_page and returns the total count; newest-first ordering preserved.
- **TEST-44** (tier: integration) [covers: ITEM-42] file: `src-app/server/tests/scheduler/continue_in_chat_test.rs` — asserts: continuing a WORKFLOW run seeds a new conversation whose FIRST assistant message contains the workflow output digest (non-empty) + one file ContentBlock per persisted run artifact; continuing a PROMPT run seeds an assistant message equal to the run's real last assistant text (fixes the empty-workflow-seed bug).
- **TEST-45** (tier: unit) [covers: ITEM-42] file: `src-app/server/src/modules/scheduler/continue_chat.rs` — asserts: the seed-builder emits an ASSISTANT-role message (DEC-23) carrying the resolved result text, and maps each artifact file-id to a file ContentBlock (none → no blocks); robust to a missing source.
- **TEST-46** (tier: integration) [covers: ITEM-43] file: `src-app/server/tests/scheduler/continue_in_chat_test.rs` — asserts: `POST /scheduled-tasks/{id}/continue-series?limit=5` seeds one assistant summary message referencing the last 5 runs' previews + deltas; `limit` clamps to available runs; a foreign task id → 404 (owner-scope, the A9 deny path).
- **TEST-47** (tier: unit) [covers: ITEM-43] file: `src-app/server/src/modules/scheduler/continue_chat.rs` — asserts: the series-summary builder orders runs newest-first, includes each run's `change_summary` delta, and honors the clamped limit.

### Frontend
- **TEST-48** (tier: e2e) [covers: ITEM-44] file: `src-app/ui/tests/e2e/14-scheduler/paused-and-runs.spec.ts` — asserts: a run seeded (mock API) with `result_preview` + `change_summary_json` shows the what-changed badge + one-line preview; clicking the row expands the fuller result inline; a failed run shows its error message on expand.
- **TEST-49** (tier: unit) [covers: ITEM-44] file: `src-app/ui/src/modules/scheduler/components/runTimeline.ts` — asserts: the change→badge mapper yields `{label:"NEW ×N", tone:success}` for new_count>0, `{changed, warning}` for changed&&new_count==0, `{no change, muted}` for !changed, `{failed, destructive}` for a failed run; preview clamps to one line.
- **TEST-50** (tier: e2e) [covers: ITEM-45] file: `src-app/ui/tests/e2e/14-scheduler/scheduled-tasks.spec.ts` — asserts: a fired prompt task (seeded `bound_conversation_id`) shows a primary "Open thread" that navigates to `/conversations/{id}`, plus a secondary "New side chat"; a never-fired prompt task shows "Open thread" DISABLED with a tooltip; a workflow task shows "Continue in chat" and NO "Open thread".
- **TEST-51** (tier: unit) [covers: ITEM-45] file: `src-app/ui/src/modules/scheduler/components/runTimeline.ts` — asserts: the follow-up action resolver: prompt+bound → openThread primary + fork secondary; prompt+no-bound → openThread disabled; workflow → continue only.
- **TEST-52** (tier: e2e) [covers: ITEM-46] file: `src-app/ui/tests/e2e/14-scheduler/paused-and-runs.spec.ts` — asserts: a task with >10 runs (seeded) renders 10 rows + "Showing 10 of N" + `ListPagination`; advancing to page 2 loads the next slice.
- **TEST-53** (tier: unit) [covers: ITEM-46] file: `src-app/ui/src/modules/scheduler/stores/ScheduledTasks.store.ts` — asserts: `loadRuns(taskId, page)` stores the paged slice keyed by task + the total, and a page change refetches that page.
- **TEST-54** (tier: e2e) [covers: ITEM-47] file: `src-app/ui/tests/e2e/14-scheduler/paused-and-runs.spec.ts` — asserts: the "Discuss recent runs ▾" chooser offers {5, 10, all-loaded} (default 5); choosing an option calls `continue-series` and navigates to the returned conversation.
- **TEST-55** (tier: unit) [covers: ITEM-47] file: `src-app/ui/src/modules/scheduler/stores/ScheduledTasks.store.ts` — asserts: the chooser options builder = `[5, 10, all-loaded]` default 5, and `continueSeries(taskId, limit)` maps the selection to the `limit` query param.
- **TEST-56** (tier: e2e) [covers: ITEM-48] file: `src-app/ui/tests/e2e/14-scheduler/paused-and-runs.spec.ts` — asserts: at a 390px viewport the runs panel stacks, per-run actions live in an overflow menu, the preview clamps to one line, and there is no horizontal page scroll (responsive-fidelity; gallery narrow-viewport state is enforced by `gate:ui`/`check:state-matrix`).

## Round 3 (FB-9 precedent audit)

- **TEST-57** (tier: e2e) [covers: ITEM-49, ITEM-50] file: `src-app/ui/tests/e2e/14-scheduler/precedent-layout.spec.ts` — asserts: navigating to `/scheduled-tasks` via the sidebar nav item renders the app shell — the `app-header-bar` (HeaderBarContainer) AND the left sidebar are both present — and the page header shows the "Scheduled Tasks" title + a "New task" action in the header bar (proves AppLayout + HeaderBarContainer replace the shell-less SettingsPageContainer). B7 render proof of FB-9.
- **TEST-58** (tier: e2e) [covers: ITEM-51] file: `src-app/ui/tests/e2e/14-scheduler/precedent-layout.spec.ts` — asserts: with >12 tasks seeded via the mock API, only 12 cards render, the "Showing 12 of N" counter shows, and clicking "Load More" reveals the remainder.
- **TEST-59** (tier: e2e) [covers: ITEM-55] file: `src-app/ui/tests/e2e/14-scheduler/precedent-layout.spec.ts` — asserts: with zero tasks the empty state renders an icon + "No scheduled tasks yet" title + a create button, and clicking it opens the New Task drawer.
- **TEST-60** (tier: e2e) [covers: ITEM-52, ITEM-53, ITEM-54] file: `src-app/ui/tests/e2e/14-scheduler/precedent-layout.spec.ts` — asserts: a task card exposes Run/Edit/Delete actions on hover/focus (present with the reveal classes) while the enable Switch is always visible; the card renders via the dedicated ScheduledTaskCard.
- **TEST-61** (tier: e2e) [covers: ITEM-52, ITEM-53] file: `src-app/ui/tests/e2e/14-scheduler/precedent-layout.spec.ts` — asserts: the RENDERED card title (`task-name-<id>`, via the dedicated ScheduledTaskCard) has computed font-weight ≤ 400 and font-size 14px — the canonical list-item token set (`!font-normal !text-sm`), NOT `font-medium`. (B7: a real render read, not a source regex.)
- **TEST-62** (tier: e2e) [covers: ITEM-56, ITEM-57, ITEM-58] file: `src-app/ui/tests/e2e/14-scheduler/precedent-form.spec.ts` — asserts: the drawer's target-kind, schedule, and notify controls render inside labelled Field/FormField rows; submitting with a required field empty is blocked with a validation error (zodResolver); a valid submit creates the task.
- **TEST-63** (tier: e2e) [covers: ITEM-59] file: `src-app/ui/tests/e2e/14-scheduler/precedent-layout.spec.ts` — asserts: at a 390px viewport there is no horizontal page scroll, the header title + action remain visible, and each card's actions remain reachable (overflow menu / wrapped).
