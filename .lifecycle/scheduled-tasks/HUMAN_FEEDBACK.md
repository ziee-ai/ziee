# Human Feedback — scheduled-tasks

Living ledger (Phase 9 gate). Each human feedback item is recorded VERBATIM as it
arrives, with a status and — where it generalizes beyond this feature — a candidate
fleet-wide rule. Any `[status: open]` fails the Phase 9 gate.

- **FB-1** [status: resolved] — "it did NOT reuse existing page/drawer layouts (chat page, project page, etc.) -- match those, do not invent new ones" → rebuilt `ScheduledTaskFormDrawer` on the shared `FormField`/kit primitives, mirroring the `ProjectFormDrawer`/chat-drawer structure (labelled Field rows, not raw flex-gap), and the list page reuses the settings-page container layout; no bespoke layout remains [generalizable: yes — a new feature's page/drawer/settings surfaces MUST mirror the closest existing layout (chat page, project page, settings card/drawer); never invent a bespoke layout when an established one fits]
- **FB-2** [status: resolved] — "the Drawer asks the user to TYPE the ID of an assistant and the ID of a workflow to run -- users never see those IDs and never would; replace every raw-ID text input with a proper selection picker" → added `TaskTargetPickers.tsx`: `AssistantField`/`WorkflowField` are name-searchable Comboboxes and `ModelField` is a grouped Select, each populated from the picker stores' cached lists and bound to the form via `FormField`; zero raw-ID text inputs remain [generalizable: yes — never ask a user to type a raw entity ID (assistant/workflow/model/provider/etc.); any field whose value is an entity reference MUST be a selection picker (dropdown/searchable list) populated from that entity's list endpoint, showing human-readable names]
- **FB-3** [status: resolved] — "who would try to input timezone? we need to auto detect timezone from user client" → the timezone input was removed; the browser IANA zone (`Intl.DateTimeFormat().resolvedOptions().timeZone`) is auto-detected, submitted with the task, and rendered read-only as `schedule-timezone-note` (no editable control) [generalizable: yes — never ask the user to input a value the client already knows (timezone, locale, etc.); auto-detect it from the browser/environment and, if it must be visible, show it read-only — do not render it as an input field]
- **FB-4** [status: resolved] — "we need to allow user to select what days (multiple) in the week to run it" → `ScheduleBuilder` weekly mode is now a Mon–Sun multi-toggle emitting a comma-separated cron DOW field (`0 9 * * 1,3,5`); `humanizeCron` renders the multi-day summary [generalizable: yes — a recurring-schedule builder must support selecting MULTIPLE days of the week (multi-toggle), not a single day; when a field naturally accepts multiple values, offer multi-select rather than single-select]
- **FB-5** [status: resolved] — "the Inputs of workflow, it needs to based on workflow, not just the json. the UX is horrible" → the drawer parses the selected workflow's IR (`parseWorkflowIr`) and renders one typed `FormField` per declared input (with required-input validation); the raw-JSON textarea survives only as the last-resort fallback when a workflow exposes no declared inputs [generalizable: yes — collect structured input via a form generated from the TARGET's declared schema/inputs (one typed field per input), never a raw JSON blob; a JSON textarea for structured input is a UX failure and is only acceptable as a last-resort fallback when no schema is available]
- **FB-6** [status: resolved] — "when implement anything ,we need to check every item to see how user experience it and see how it relates to all current infastructure and see each of the infastructure if they have specific things that we need to pay attention to. add that to the human feedback" → authored `INFRA_INTEGRATION.md` doing both walks (per-item UX walk + per-subsystem infrastructure walk over chat pipeline, MCP tool-call/approval, permissions, notifications, sync, streaming, workflow runner, settings); the unattended tool-call approval gap it surfaced drove the safe-default + per-task allow-list design (DEC-17, ITEM-13/14/16/17) [generalizable: yes — for EVERY plan item, before/while implementing, do two mandatory walks: (1) a USER-EXPERIENCE walk (how does a real user actually encounter, trigger, and live with this item end-to-end), and (2) an INFRASTRUCTURE-INTEGRATION walk (enumerate every existing subsystem the item touches — chat pipeline, MCP tool-call + approval flow, permissions, notifications, sync, streaming, workflow runner, settings, etc. — and for EACH, check whether it has specific behaviors/constraints that must be handled, not assumed). Record the findings as an item×infrastructure analysis artifact. Applies fleet-wide; this is now a lifecycle sub-step of Phase 5.]
- **FB-7** [status: resolved] — "add that to human feedback first, all features must have a carefully design of what a human user would want to do, it needs to be comprehensive for all components, interfaces, etc." (context — "that" = the follow-up/series design walk: today a scheduled task exposes only a run-row `timestamp — status` + a "Continue in chat" button that forks a NEW conversation seeded with a truncated paraphrase, and for a workflow run seeds NO actual output. The job-to-be-done is different: a recurring task is a standing assignment whose value is the EVOLVING STREAM of results and what CHANGED across it, so a user wants to (1) read each result inline without forking, (2) see the series as a timeline with what-changed markers, (3) reply into the ONE ongoing thread that already holds the whole series — for prompt tasks that is the existing `bound_conversation_id`, which the UI currently hides — (4) follow up on the REAL result incl. workflow artifacts, not a snippet, and (5) ask across multiple runs.) → authored `UX_DESIGN_followup_series.md` (comprehensive per-surface JTBD design: J1–J5 across task row, runs panel, run row, drawer, notifications, thread/continue-seed, and empty/failed/loading/mobile states); the human selected all four capabilities (ongoing thread, inline results + what-changed timeline, series-level follow-up, real-content fidelity). IMPLEMENTED as ITEM-40..48 / TEST-40..56: run timeline (what-changed badge + preview + expand), "Open thread" resuming the bound conversation + kept fork, `ListPagination` runs panel, "Discuss recent runs" series follow-up, and the assistant-turn seed carrying the REAL result incl. workflow output + artifacts (migration 155). All four surfaces reconciled against `UX_DESIGN_followup_series.md`. [generalizable: yes — every feature MUST include an explicit, comprehensive JOBS-TO-BE-DONE / user-experience design that states what a real human wants to DO, enumerated across EVERY component and interface the feature exposes (list page, detail, drawer/form, notifications, thread/conversation, empty/error states, mobile), and each surface must be reconciled against that design before implementation — a code-mechanism description is NOT a UX design. This is the whole-feature complement to FB-6's per-item UX walk, and is a mandatory Phase-1 deliverable (feeds the existing UI-surface plan checklist).]
- **FB-9** [status: resolved] — "why is the Scheduled Tasks page not using AppLayout with the header bar and other stuff just like the chat page or project page?" (+ directive: there are MANY such problems — do NOT fix only this one; FIRST run a thorough PRECEDENT AUDIT of EVERY scheduler UI surface (page + drawer + list + form) against its closest sibling (chat page, project page, settings page) enumerating every divergence in layout/structure/typography/tokens/pagination/empty-loading-error/responsive/reuse; STOP and send the full list as the iteration plan for approval BEFORE implementing) → PRECEDENT AUDIT COMPLETE; full divergence list sent to human for approval (this iteration's PLAN scope). Confirmed core gap: `/scheduled-tasks` route has NO `layout:` field so it renders with NO AppLayout shell (no left sidebar, no header bar) and the page root uses `SettingsPageContainer` (a settings-sub-page container) instead of the top-level `HeaderBarContainer` idiom that chat/projects/knowledge-base all use. Human approved the audit scope (chose: single-column rows/DEC-24, hover-reveal actions/DEC-25, full FormField+zodResolver tidy/DEC-26, detail-page deferred to its own lifecycle/DEC-27). IMPLEMENTED ITEM-49..59: `/scheduled-tasks` now uses `layout: AppLayoutDef`; the page was rebuilt on the top-level idiom (`HeaderBarContainer` + `useNativeScroll` + Load-More + actionable empty state) replacing `SettingsPageContainer`; extracted `ScheduledTaskCard` with the canonical `!font-normal !text-sm` list-item title + hover-revealed `outline`/Tooltip actions (Switch stays visible as state) + chips in the body (not the truncating title); the drawer adopted `zodResolver` + `FormField`/`Field`-wrapped controls. Verified: `npm run check` PASS, gate:ui scheduler surfaces 0 gating-HIGH, and the full 14-scheduler e2e suite (23 tests incl. new TEST-57..63 asserting the AppLayout shell/header, Load-More, empty state, hover-reveal opacity, title tokens, zod-blocked submit, and mobile no-overflow) PASSED with no regression. [generalizable: yes — a NEW top-level nav page MUST be built on the same shell idiom its sibling top-level pages use — set `layout: AppLayoutDef` on the route AND render the page body starting with `HeaderBarContainer` (title + right-aligned primary action) + `useNativeScroll`; `SettingsPageContainer` is ONLY for settings sub-pages under `SettingsLayoutDef`, never a top-level nav destination. More broadly: when a human flags ONE precedent divergence, treat it as a signal to run a FULL precedent audit of EVERY surface of that feature against its closest sibling (chat/projects/knowledge-base list+card+drawer) — layout, typography tokens, pagination idiom (top-level feed = Load-More; settings/detail list = numbered ListPagination), empty/loading/error states, responsive behavior, and reuse vs hand-rolled — and fix the whole class, not just the reported instance.]
- **FB-8** [status: resolved] — "give me options to choose. Add this to human feedback: for all features, must think of all options needed for that feature, if they need human input to decide what to modify then ask human" → enumerated this round's full decision set (DEC-A fixed-vs-admin-configurable constants, DEC-B prompt-task primary follow-up action, DEC-C series-follow-up scope, DEC-D follow-up-seed shape) and surfaced each as an explicit `AskUserQuestion` option picker for the human to choose, rather than recommend-and-proceed; adopted as the working mode for the rest of this feature [generalizable: yes — for EVERY feature, exhaustively enumerate the full set of options/decisions the feature requires (surfaces, defaults, behaviors, tunables), and for any decision that is a genuine product/human choice about WHAT to build or modify, PRESENT IT AS AN EXPLICIT OPTION PICKER (AskUserQuestion) for the human to choose — never silently pick a default and proceed. Resolve by convention only decisions with an unambiguous codebase precedent; escalate the rest as options. Strengthens Phase-4 DECISIONS: human-input decisions become up-front pickers, not recommend-and-proceed.]
