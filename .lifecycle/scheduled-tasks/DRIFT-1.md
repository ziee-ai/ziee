# DRIFT-1 — implementation vs plan reconciliation

Audit of the implemented diff (`git diff origin/main...HEAD`) against PLAN.md/DECISIONS.md.

- **DRIFT-1.1** — verdict: resolved — Migration number: `origin/main` advanced from 145 to 152 (knowledge-base took 146, voice/UI-fixes 147–152) while this branch was in flight. The plan's migration was renumbered `146 → 153` (next free above main's max) and `origin/main` merged in; the migration content is unchanged (two JSONB columns). No functional impact (sqlx applies by filename order).
- **DRIFT-1.2** — verdict: none — ITEM-13 threads the `unattended` signal exactly as planned: `dispatch_prompt` sets it on the request; `before_llm_call` (which has `send_request`) stashes it into `context.metadata`; `after_llm_call` (which does not) reads it there. This is the concrete realization of the plan's "SendMessageRequest → StreamContext.metadata → after_llm_call" — the `context.metadata` HashMap is the metadata vehicle the codebase already uses (model_id, executed_tool_use_ids).
- **DRIFT-1.3** — verdict: resolved — The `unattended` + `unattended_allowed_tools` fields land ON the OpenAPI `SendMessageRequest` schema (via `SendMessageRequestFields`, which is `JsonSchema`), whereas BASE.md hoped to keep the flag off the public request type. The request→extension mechanism REQUIRES schema fields (identical to the existing `enable_mcp`/`mcp_config`), so this is unavoidable and harmless (additive, default-false; no interactive client sets it). BASE note superseded; the plan itself imposed no such constraint.
- **DRIFT-1.4** — verdict: resolved — ITEM-18b applies the user's DEFAULT `disabled_servers` to ANY no-conversation workflow run (the scheduled case PLUS standalone workflow-tool runs), because the `WorkflowRun` row struct does not expose `invocation_source` (it is on the `CreateWorkflowRun` insert struct only), and the user's default preference correctly applies to all standalone runs. The plan's requirement — "a scheduled workflow tool-step honors the user's disabled set" — is fully met (scheduled is a subset); the broadening is a correct incidental improvement (a prior latent gap). TEST-34 (the scheduled subcase) is unaffected.
- **DRIFT-1.5** — verdict: resolved — `ScheduledTask.allowed_unattended_tools` is stored/read as `serde_json::Value` (JSONB row), so the generated OpenAPI response type is loosely-typed (`{}`); the frontend casts it to `AllowedUnattendedTool[]` on edit-seed (runtime shape is always that). `CreateScheduledTask`/`UpdateScheduledTask` use the typed `Vec<AllowedTool>`, so the write path is fully typed. Chosen to avoid a bespoke sqlx JSONB→Vec decode on the row struct; the cast is documented at the call site.

**Unresolved drifts:** 0
