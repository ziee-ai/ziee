# DRIFT-1 — implementation vs plan

Reconciling the implemented diff against PLAN.md / DECISIONS.md.

- **DRIFT-1.1** — verdict: resolved — ITEM-6 said "change SSE complete / reloadOpen to MERGE the tail". Implemented as a dedicated `reconcileTail` action that MERGES when the window already abuts the tail (`!hasMoreAfter`) but RESETS to the tail when the window is anchored mid-conversation (`hasMoreAfter`, e.g. after an around= jump) — a merge there would splice the tail on after a gap. This is a correctness refinement WITHIN ITEM-6's intent (matches DEC-6); no plan change needed.
- **DRIFT-1.2** — verdict: resolved — the plan/DEC-6 implied dropping the optimistic `conversation.messageCountChanged` emit (it used `messages.size`, which under lazy-load is only the loaded window, not the true total). Removed; the sidebar `message_count` self-heals via the `Conversation` sync the backend emits on turn completion (`streaming.rs:895`) → ChatHistory's `sync:conversation` refetch. Consistent with DEC-6; captured in the store comment.
- **DRIFT-1.3** — verdict: impl-wins — ITEM-13 said `findMatches.ts` would be "reduced to the pure snippet/highlight helper". Kept `findMatches.ts` INTACT: its `messageText` export is still consumed by `ChatMessage.tsx`, and `findMatches` + its `findMatches.test.ts` remain a valid tested pure util. The find bar simply no longer imports `findMatches` (match set is now server-side). No behavior regression; leaving the tested helper in place is cleaner than deleting a still-referenced module. Plan intent (match set moves server-side) is met.
- **DRIFT-1.4** — verdict: none — `reconcileTail` is a new named action not spelled out in the plan, but it is the concrete mechanism the plan called for ("merge the tail"); reused by the complete handler, the receiving-device path, and the branch-aware reloadOpen. Not a divergence.
- **DRIFT-1.5** — verdict: resolved — `computeForkPoints` now iterates only the LOADED window (not the full history). This is the accepted degradation already flagged in PLAN_AUDIT: the BranchNavigator anchors at a visible fork bubble; if a fork point is scrolled far out of the window it simply isn't shown until paged into view. No plan change; behavior is intentional under windowing.
- **DRIFT-1.6** — verdict: none — the `around` window returns `floor(limit/2)` older + the target + `floor(limit/2)` newer, so with the default `limit=30` it yields 31 rows (15+1+15), exactly matching DEC-3's "around = 31 (15 + target + 15)". Implemented as designed.
- **DRIFT-1.7** — verdict: none — scroll-anchor restore uses the anchor-element + `useLayoutEffect` + short-lived `ResizeObserver` + `overflow-anchor:none` exactly per DEC-2, with the native-window fallback (`getViewport` → `document`/`window`) flagged in PLAN_AUDIT/DEC-7. Matches plan.

**Unresolved drifts:** 0
