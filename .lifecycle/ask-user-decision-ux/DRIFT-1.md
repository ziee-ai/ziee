# DRIFT-1 — implementation vs plan

Compared the implemented diff against PLAN.md ITEM-1..10 + DECISIONS after
`cargo check`, `tsc`, `npm run check`, and the unit tiers all passed.

- **DRIFT-1.1** — verdict: impl-wins — PLAN listed 3 frontend source files; implementation added a 4th, `elicitationFields.tsx` (`renderInputField`), to (a) DRY the non-choice input controls shared by the legacy renderer + the wizard and (b) avoid a circular import between `ElicitationFormContent` and `AskUserWizardContent`. PLAN.md Files-to-touch amended to list it; ITEM-3 already covers "extract shared helpers", so no new ITEM is needed. Behavior of the legacy non-choice path is unchanged (same JSX, just relocated).
- **DRIFT-1.2** — verdict: impl-wins — ITEM-10's gallery work touched more files than PLAN itemized: the fixture bundle `fixtures/chat-deep.ts`, and the manual coverage registries `coverage.ts` (+2 `kind:'via'` entries) and `stateCoverage.ts` (the generator flagged an `:error` signal on `fieldState.error?.message`, registered as `skip` — it's a per-field validation message, not a load-error surface, and is exercised by the wizard e2e). Plus the mechanical regens (`galleryCoverage.generated.ts`, `stateMatrix.generated.ts`, `STATE_MATRIX.md`, `testIds.generated.ts`). PLAN.md Files-to-touch amended. All are the standard cost of registering a new gallery surface; no behavioral change.
- **DRIFT-1.3** — verdict: none — ITEM-3 said the legacy `renderField` stays "byte-identical"; it is now behaviorally-identical but not byte-identical (its non-choice tail delegates to `renderInputField`). This is the same relocation as DRIFT-1.1 and is intentional (DRY); the choice/Select branches and every resolved-state branch are untouched. The two generic (unstamped) elicitation e2e specs still exercise the legacy path unchanged.
- **DRIFT-1.4** — verdict: none — DECISIONS DEC-3/4/6 conventions (`enumDescriptions`/`enumPreviews`/`x-ziee-recommended`) implemented exactly; the marker was named `x-ziee-askuser` (DEC-2), stamped after `cap_requested_schema` (DEC-13), and the response envelope stays the flat `{prop:value}` object (DEC-1/8) — verified by TEST-3 (backend) + TEST-12 (e2e both answers in one content object). No divergence.
- **DRIFT-1.5** — verdict: none — TESTS.md TEST-1..8 all authored and PASS locally (7 Rust unit + 11 TS unit); TEST-9..15 authored as enumerated; TEST-16 satisfied by the gallery cell + `check:state-matrix`. `sse-mock-helpers.ts` needed no change (it already forwards an arbitrary `requestedSchema`), matching the "may" hedge in the plan.

**Unresolved drifts:** 0
