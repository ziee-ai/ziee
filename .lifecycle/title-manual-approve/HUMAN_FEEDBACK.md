# HUMAN_FEEDBACK

Every critique the human gave during this feature, and what changed because of it.

- **FB-1** [status: resolved] — "Investigate and fix, you can copy the data (the LLM provider for local providers and the GPT OSS model to your new container to test) and copy the Biognosia MCP server as well (add it as system MCP server, do not add as user's mcp server)" → Item 2 was investigated live rather than deferred as I recommended; the review container was provisioned from the running `:8080` stack's data, with BioGnosia seeded as `is_system=true, is_built_in=false`. The investigation is what proved the routing already works (DEC-20). [generalizable: yes — when the human overrides a "defer" recommendation, the investigation usually produces the decisive evidence; do it before proposing scope cuts]

- **FB-2** [status: resolved] — "What is this item, please let me know and ask the question again" → I had asked the human to decide on item 4 without explaining what it was. Explained it concretely (the 9 duplicated fallback sites, the 2 search filters matching the placeholder literal) and re-asked. [generalizable: yes — never ask for a decision on an item by its number; state what it IS and what changes, then ask]

- **FB-3** [status: resolved] — "Bundle it in" → item 4 entered scope. When phase-2 exploration then proved it needed a backend field, I re-surfaced the scope change instead of absorbing it silently, and the human approved `first_message_preview`. [generalizable: yes — if an approved item turns out materially bigger than described when approved, re-ask; the original approval does not cover the new shape]

- **FB-4** [status: resolved] — "Please re-present your full plan via ExitPlanMode … so the user can review and approve it" → I had exited plan mode and begun environment setup before the plan was formally approved. Re-presented the plan with the added verification specifics and disclosed the setup already performed. [generalizable: yes — do not start work off the back of an inline approval; use the approval mechanism the harness provides]

- **FB-5** [status: resolved] — "I thought the skill in the ziee folder tells that you should strip the files and folders in the lifecycle before commiting and pushing?" → Checked the source: artifacts stay ON the feature branch (the hook reads them) and are stripped at MERGE to main. Also surfaced that deleting `.lifecycle/` would silently disable the pre-push gate, which is why it is not a legitimate way past a red gate. [generalizable: yes — when challenged on process, quote the rule from the file rather than restating memory]

- **FB-6** [status: resolved] — "Why the e2e failed, did you touch any UI?" → I had asserted the e2e failures were harness-only. I HAD touched 5 `.tsx` files, so the claim needed proof. An A/B (UI reverted to `origin/khoi`, spec kept) cleared my UI of the login timeouts — AND exposed that TEST-13 failed identically with and without my change, i.e. it was testing the wrong surface entirely. Rewritten to drive `PaneManagerDrawer`. [generalizable: yes — "the failure is not mine" is a claim requiring an A/B, not an argument; a test that fails identically with the feature present and absent is not testing the feature]

- **FB-7** [status: resolved] — "Only fix errors that caused by your fixes, keep following the lifecycle skill" → Scoped repair to my own defects (flaky SSE assertion, missing `model_id`, wrong-surface TEST-13, unbounded/unescaped log line) and explicitly left alone the pre-existing `loginAsAdmin` timeouts and the 5 gallery/registry drift checks, each with evidence for why they are not mine. [generalizable: yes — separate "my regression" from "pre-existing" with a baseline run before touching anything]

- **FB-8** [status: resolved] — "Get me the PR link again" / "Give me the port to test the website again" → No PR existed (never pushed) and the stack was API-only on :18140. Said so plainly instead of producing a plausible link, then built the SPA and served it on :18141 with seeded demo rows. [generalizable: yes — never synthesize a URL for something not created; verify with `git ls-remote` / `gh pr list` and report the real state]
