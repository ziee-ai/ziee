# DRIFT-1 — implementation vs plan (follow-up)

- **DRIFT-1.1** — verdict: none — Issues 1 & 2 implemented as planned: `shouldWrapRun` in `toolRun.ts` (reusing `runToolUseIds`/`hasArtifactInRun`), routed through BOTH `McpToolUseGroup` and `contentSpan`; single-tool header (name + server label); `countToolUses` (now unused) removed from `extension.tsx`; scroll-into-view in `ToolCallPendingApprovalContent` (once-per-approval Set, reduced-motion, `block:'nearest'`).

- **DRIFT-1.2** — verdict: impl-wins — the scroll e2e (TEST-5) was planned as a `toBeInViewport` assertion, but that is confounded by the chat's own auto-scroll-to-bottom and the post-stream loadMessages remount (a viewport assertion can pass/fail for reasons unrelated to the feature). Replaced with a robust `scrollIntoView` **spy** (`page.addInitScript` records the target element's `data-testid` + `behavior`) that directly asserts the approval scrolled ITSELF into view with `behavior:'smooth'`. Stronger and deterministic. PLAN's "scroll assertion if feasible" latitude covers this; no plan amendment needed (the intent is unchanged).

- **DRIFT-1.3** — verdict: resolved — reconciling the existing single-tool-artifact e2e (ITEM-5) surfaced a #133 latent regression: `seedAssistantWithMultipleToolResults` in `tests/e2e/chat/fixtures/mock-tool-result.ts` clicked the group toggle to expand, but since #133 an artifact group AUTO-OPENS, so the click now COLLAPSED it (the `mcp-resource-links-positioning` multi-tool tests were not run in #133's scoped phase-8). Removed the toggle-click (auto-open shows the files). Also `mcp-resource-links-positioning.spec.ts:189` ("mixed file types") — now that the single tool wraps, the 3rd (markdown) file sits lower and `InlineFilePreview`'s viewport-gate (`seen` within ~800px) defers its body; added a `scrollIntoViewIfNeeded()` on the md preview before asserting its `<h1>`. Both are legitimate updates reflecting intended behavior (auto-open + viewport-gating), verified green.

- **DRIFT-1.4** — verdict: none — the OPTIONAL `deep-chat-tool-single-artifact` gallery state (PLAN "Files to touch") is deliberately NOT added: the single-tool wrapper is a small header variation of the already-galleried `deep-chat-tool-group` card and is thoroughly covered by the new e2e; skipping it keeps the follow-up focused and avoids extra gallery-coverage/state-matrix churn.

**Unresolved drifts:** 0
